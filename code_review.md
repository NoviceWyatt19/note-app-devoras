# Devoras 코드 리뷰 — Step 1~7 이후 (v0.8.49)

> **작성**: 2026-08-31 | **대상**: `v0.8.49` (`81971a2`) | **직전 리뷰**: [`code_review_20260831_194736.md`](claude-history/review/code_review_20260831_194736.md) (v0.8.5 기준 카탈로그)
> **범위**: 냉시작 전수 감사가 아니라 **Step 1~7 이 바꾼 표면**에 집중한다. 근거는 §0.
> **선행 문서**: [`DEBUG_STEP_PLAN.md`](DEBUG_STEP_PLAN.md) · [`ARCHITECTURE_FINDINGS.md`](ARCHITECTURE_FINDINGS.md) · [`project/VERIFY_BY_HUMAN.md`](project/VERIFY_BY_HUMAN.md)

---

## 0. 이 리뷰의 범위를 이렇게 잡은 이유

이전 리뷰는 v0.8.5 코드베이스를 냉시작으로 훑은 카탈로그였다. 그 뒤 **버전이 아니라 구조가 바뀌었다** — 전역 싱글턴 소멸, 이펙트 2개→조정자 1개, 문서 전체 치환→최소 diff, 정체성 재파생→2-패스 매칭 상속.

범위를 "바뀐 표면"으로 좁힌 근거는 **관측된 사실**이다. 이번 사이클에서 나온 심각한 결함 3건이 **전부 리팩터 자신이 만든 것**이었다:

| 결함 | 만든 단계 | 성격 |
|---|---|---|
| `devoras_read_dropped_file` 무검증 임의 읽기 | Step 1-A | scope 축소가 만든 회귀를 scope 우회로 메움 |
| 부모 리네임 시 자식 id 미보존 · 리네임+재정렬 시 id 스왑 | Step 4 | 최초 구현의 테스트가 통과시킨 밖 |
| 타이핑 시 캐럿 이탈 (S1/S2) | Step 5 | 조정자가 명령을 상태로 읽음 |

**셋 다 자동 테스트가 잡지 못했다.** 신규 코드가 위험이 집중된 곳이므로 거기를 본다.

---

## 1. 요약 — 우선순위

| 순위 | ID | 항목 | 유형 | 상태 |
|---|---|---|---|---|
| 1 | **R-1** | 검증 불가 영역의 구조적 확대 (React 테스팅 라이브러리 부재) | 🔴 프로세스 | **신규** |
| 2 | **A6-후속** | 마운트 비용 초선형 — **동시 생존 인스턴스 수** | 🟢 성능 | **원인 확정 (2026-08-31)** · 조치 미착수 |
| 3 | **R-2** | `updateBlockContent` 가 키 입력마다 트리 전체를 재생성 | 🟢 성능 | **신규** |
| 4 | **R-3** | `generateId` 충돌이 이제 오매칭을 만든다 | 🟡 버그 | 기존(P2-5) · 등급 상승 |
| 5 | **R-6** | `createEditorTheme()` 이 블록마다 새 `StyleModule` 을 만들고 회수되지 않는다 | 🟡 누수 | **신규** |
| 6 | **R-4** | `TabDocumentProvider` 가 렌더 중에 스토어를 만든다 | 🟡 구조 | **신규** |
| 6 | **R-5** | `tabStoreRegistry` 는 tabId 당 스토어 1개를 가정한다 | 🟡 제약 | 문서화됨 |
| 7 | **P1-9** | `.cm-line *` 위젯 타이포그래피 붕괴 | 🟡 표시 | 기존 · 미착수 |
| 8 | — | B2 실기 검증 잔여 | 🔒 보안 | 사람 손 |
| 9 | — | P2-1·P2-2·P2-4·P2-6~P2-8 | 🟢 품질 | 기존 · 미착수 |

**해소 확인** — 이전 리뷰의 P0-1·P0-3(A-2·Stage B)·P0-5·P1-1~P1-7·P2-3, 그리고 구조 결함 A1·A2·A5·A7. 상세는 아카이브된 이전 리뷰의 「Step 1~6 사이클」 절.

---

## 2. R-1 — 검증 불가 영역이 구조적으로 커지고 있다 🔴

**가장 중요한 발견이며, 개별 버그가 아니라 프로세스 결함이다.**

**[사실]** T1 하네스는 **14종**으로 늘었고 품질도 좋다(`test:blockkey` 10, `test:caretstability` 10, `test:paneownership`, `test:tabstore`, `test:diff` …). 그런데 `package.json` 에 **React 테스팅 라이브러리가 없다**(`testing-library` 의존성 0건). 따라서 다음은 **어떤 자동 게이트로도 도달할 수 없다**:

- 실제 키 입력 → 캐럿 렌더 결과
- 버튼 클릭 (viewMode 토글, 저장 버튼 dirty 표시)
- 한글 IME 조합 (합성 이벤트는 IMK 경로를 타지 않는다)
- 패널·탭 전환의 실제 DOM 결과

**[증거 — 이것이 이론이 아닌 이유]**

1. **캐럿 버그(S1/S2)는 이 공백에서 나왔다.** `focusOffset` 을 스토어 로직만으로 검사하면 정상이다. "한 글자 치고 캐럿이 어디 있나"를 보는 게이트가 없어서 통과했다.
2. **`FormatToolbar` 누락도 같은 공백이었다.** 7-C 계획서의 C-3 대상 목록에서 빠졌는데, 구현자가 손으로 찾지 않았다면 Provider 마운트 즉시 토글 버튼이 조용히 죽었을 것이다. **계획에서 빠진 소비자와 테스트가 없는 소비자가 정확히 같은 집합이었다.**
3. `VERIFY_BY_HUMAN.md` 가 **11개 절**로 불었다. 매 배치가 여기에 항목을 더하고, 확인은 사람 한 명의 실기에 몰려 있다.

**[조치 방안]** `@testing-library/react` + `jsdom` 도입을 **별도 배치로 안건화**한다. 전부를 자동화하자는 게 아니다 — IME 실조합과 번들 앱 육안 확인은 원리적으로 사람 몫이다. 그러나 **클릭·캐럿·마운트 결과는 jsdom 으로 충분히 도달한다**. 지금 구조는 "테스트가 닿지 않는 곳에 버그가 쌓이고, 그 목록이 사람에게 전가되는" 형태로 굳어지고 있다.

**[우선순위 근거]** A6 보다 위에 둔 이유 — A6 는 성능 하나지만 R-1 은 **앞으로 나올 모든 결함의 발견 시점을 좌우한다.**

---

## 3. R-2 — `updateBlockContent` 가 키 입력마다 블록 트리 전체를 재생성한다 🟢 **신규**

**[파일]** `project/src/entities/document/model/tabStore.ts:108-116`

```typescript
const updateNode = (blocks: EditorBlock[]): EditorBlock[] =>
  blocks.map((block) => {
    if (block.id === id) return { ...block, content };
    if (block.children.length > 0) return { ...block, children: updateNode(block.children) };
    return block;
  });
set({ blocks: updateNode(get().blocks) });
```

**[문제]** 자식이 있는 블록은 **대상이 그 서브트리 안에 있든 없든 무조건** `{ ...block, children: updateNode(...) }` 로 새 객체가 된다. `.map()` 자체도 항상 새 배열을 만든다. 결과적으로 **키 입력 한 번에**:

- 최상위 `blocks` 배열이 새로 생기고
- **자식을 가진 모든 블록**(= 모든 헤딩 블록)의 객체 정체성이 바뀐다

`BlockNode` 는 `React.memo` 로 감싸져 있는데(`BlockEditor.tsx`), 정체성이 바뀌므로 **memo 가 무력화되어 헤딩 척추 전체가 매 키 입력마다 리렌더된다.**

**[A6 와의 관계]** A6 후속 조사(`DEBUG_STEP_PLAN` §3-A)가 인스턴스 수 축 초선형을 쫓고 있다. 이 항목은 **입력당 비용**이라 축이 다르지만, N 이 커질수록 함께 커지고 **같은 컴포넌트 트리를 건드린다.** §3-A 착수 시 이것도 함께 계측할 것 — 분리해서 재지 않으면 서로의 노이즈가 된다.

**[조치 방안]** 대상이 실제로 포함된 경로만 새 객체로 만든다(경로 복사). 자식 배열이 바뀌지 않았으면 **원래 참조를 그대로 반환**해 memo 를 살린다.

---

## 4. R-3 — `generateId` 충돌이 리마운트가 아니라 **오매칭**을 만든다 🟡 등급 상승

**[파일]** `project/src/entities/block/model/store.ts` (`generateId`)

**[상태 변화]** 이전 리뷰에서 P2(영향도 낮음)였다. **A7 수정으로 이 id 의 역할이 바뀌었다** — 이제 2-패스 매칭이 문서 재파싱을 가로질러 **운반하는 정체성 그 자체**다.

따라서 충돌의 결과가 악화됐다: 예전에는 "리마운트"(undo 히스토리 소실)였지만, 지금은 **충돌한 블록이 남의 `EditorView` 와 undo 히스토리를 물려받는다.** 데이터 소실보다 **잘못된 데이터 연결**이 더 나쁜 방향이다.

`Math.random().toString(36).substring(2,7)` 계열의 7자 base36 은 문서 하나 기준으로는 넉넉하지만, **한 세션에서 열리는 모든 문서 · 모든 재생성에 걸쳐 누적**된다. 여유를 계산해 본 적이 없다.

**[조치 방안]** `crypto.randomUUID()`. 교체 비용이 사실상 0이므로 방치할 이유가 없다. **P1 로 재분류를 권한다.**

---

## 5. R-4 — `TabDocumentProvider` 가 렌더 본문에서 스토어를 만든다 🟡 **신규**

**[파일]** `project/src/entities/document/model/TabDocumentProvider.tsx:28-33`

```typescript
if (storeRef.current === null || tabIdRef.current !== tabId) {
  storeRef.current = createTabStore(tabId, initialContent, initialNodes);
  tabIdRef.current = tabId;
}
```

**[문제]** 렌더 본문의 부수효과다. React 가 렌더를 **버릴 수 있는** 상황(StrictMode 이중 렌더, Suspense 재시도, 동시성 기능)에서 만들어진 스토어는 **등록도 정리도 되지 않고 버려진다.**

현재는 실피해가 없다 — 레지스트리 등록이 `useLayoutEffect` 에 있어 커밋된 것만 등록되고, 정리 클로저가 이전 `tabId`/`store` 를 정확히 잡는다(확인함). **지금 깨져 있다는 지적이 아니라, 동시성 기능을 켜는 순간 조용히 깨지는 형태라는 지적이다.**

**[조치 방안]** 우선순위는 낮다. 다만 이 파일은 이제 **문서 상태의 유일한 소스**이므로, React 18 동시성 기능(`startTransition` 등)을 도입할 때 **가장 먼저 점검할 지점**으로 표시해 둔다.

---

## 6. R-5 — `tabStoreRegistry` 는 tabId 당 스토어 1개를 가정한다 🟡 문서화된 제약

**[파일]** `project/src/entities/document/model/tabStoreRegistry.ts:29-32`

`registerTabStore` 는 평범한 `registry.set(tabId, api)` 다. **같은 `tabId` 로 두 번째 Provider 가 마운트되면 첫 번째를 덮어쓴다.** 해제는 `registry.get(tabId) === api` 가드가 있어 잘못된 삭제는 막지만, 덮어쓰기 자체는 막지 못한다.

지금은 문제가 되지 않는다 — 7-A 가 "같은 파일 중복 오픈 시 기존 패널로 포커스 이동"으로 회피하기 때문이다. 그러나 **그 회피를 걷어내려면 레퍼런스 카운팅이 선행**이며, 이는 이미 티켓(REF-20260831-01 open_questions)에 기록돼 있다. 리뷰에서는 **확인만** 하고 새 과제로 만들지 않는다.

---

## 7. 잘 된 부분 (유지할 것)

기록해 두지 않으면 다음 리팩터가 되돌린다.

1. **`decideCaretAction` 을 순수 함수로 분리한 것**(`shared/lib/editor/caretCoordinator.ts`). 하네스가 **실제 코드 경로를 그대로 호출**하므로 병렬 재구현으로 인한 드리프트가 원천 차단된다. R-1 이 지적한 공백을 **부분적으로 우회한 좋은 사례**이며, 앞으로 UI 로직을 검증 가능하게 만드는 표준 패턴으로 삼을 만하다.
2. **2-패스 매칭의 pass 2 가 콘텐츠 동일 우선 → 위치 폴백 순서인 것.** 순수 위치 기반으로 갔다면 드래그 재정렬이 깨졌을 것이다.
3. **`ensure_inside` 를 JS 인자가 아니라 Rust 상태에서 읽는 것.** XSS 가 `invoke` 인자를 위조할 수 있다는 전제가 코드에 박혀 있다.
4. **불변식을 개발 빌드 한정 + `console.error` 로 둔 것.** 편집을 중단시키지 않으면서 계측이 남는다.
5. **`5-B 건너뜀` 처럼 하지 않은 결정을 문서에 남긴 것.** 다음 사람이 재발굴하지 않는다.

---

## 7.5 R-6 — `createEditorTheme()` 이 블록마다 `StyleModule` 을 만들고 회수하지 않는다 🟡 **신규**

**[파일]** `project/src/widgets/BlockEditor/ui/BlockEditor.tsx:229` (생성) · `:144` (설정 변경 시 reconfigure)

`@codemirror/view` 의 `EditorView.theme()` 은 **호출마다** `StyleModule.newName()` 으로 새 프리픽스와 새 `StyleModule` 을 만든다(라이브러리 소스 `dist/index.js:8728` 확인). `BlockEditor.tsx:229` 는 이를 **`EditorView` 생성 이펙트 안에서** 호출하므로 **블록 하나당 StyleModule 하나**가 생긴다. `:144` 의 설정 변경 경로도 같다.

- N=200 이면 마운트에 200개, 설정을 한 번 바꿀 때마다 200개가 더 쌓인다.
- `EditorView.destroy()` 에 제거 경로가 없다 — **실측으로 인스턴스당 `<style>` +525자, 단조 증가, 파괴 후에도 줄지 않음**(누적 360 사이클까지 확인).
- 비교 대상: `markdownDecorationPlugin` 과 `decorationBaseTheme` 는 **모듈 레벨에서 1회 생성**돼 올바르게 공유된다. 같은 파일 안에서 한쪽만 어긋나 있다.

**[성능 영향 — 초선형은 아니다]** A6 판별 측정에서 테마를 얹은 조건은 절대 비용이 **약 50% 증가**했으나(N=100 에서 614→903ms) **성장 지수는 오히려 낮았다**. 즉 상수 배수이며 A6 의 초선형 원인이 아니다. 다만 **상수 50% 는 그 자체로 크고, 누수는 세션이 길어질수록 커진다.**

**[조치 방안]** 테마를 **모듈 레벨로 호이스팅**한다. 설정 의존 값이 있으므로 설정 조합별 캐시(또는 CSS 변수로 전환해 테마 자체를 상수화)가 필요하다. **가상화보다 훨씬 싸고 즉시 회수되는 항목이다.**

---

## 8. 다음 단계 권고

1. ~~A6-후속 원인 규명~~ — **완료(2026-08-31, §3-A-2).** 원인은 **동시 생존 `EditorView` 인스턴스 수**이며 데코레이터가 아니다. 조치 순서는 아래 §9.
2. **R-1 안건화** — `@testing-library/react` + `jsdom` 도입 여부 결정. 결정 자체를 미루면 `VERIFY_BY_HUMAN.md` 가 계속 자란다.
3. **R-3** — `crypto.randomUUID()` 교체. 비용 0, 언제든.
4. **P1-9** — 독립 배치, 사용자에게 즉시 보이는 결함, 실행 사양서 존재.
5. **`implementation_plan` Sprint 재개는 1번 이후.** 미해결 성능 축 위에 기능을 쌓으면 같은 계층의 소비자가 늘어 나중 수정이 비싸진다.

---

## 9. A6 판정 이후의 조치 순서 (2026-08-31)

원인 축이 확정됐으므로 순서를 다시 잡는다. **가상화가 정면 수단인 것은 맞지만 첫 번째는 아니다.**

| 순서 | 항목 | 근거 |
|---|---|---|
| 1 | **R-6 테마 호이스팅** | 마운트 비용 **상수 ~50% 즉시 회수** + 무한 누수 제거. 변경 범위가 좁고 가상화와 독립이다. 가상화를 넣어도 이 비용은 창 안에서 그대로 남으므로 **어차피 해야 한다.** |
| 2 | **R-2** `updateBlockContent` 경로 복사 | **다른 축(입력당 비용)이라 가상화가 고쳐주지 않는다.** 역시 변경 범위가 좁다. |
| 3 | **가상화(E7) + 높이 캐시** | 확정된 축(동시 생존 수)을 정면으로 공격하는 유일한 구조적 수단. **높이 캐시 없는 가상화는 BUG-20260828-01(수직 이동 줄 스킵) 계열을 재생산**하므로 동반 필수. 윈도 ±2 화면, 캐럿 근처 마운트/언마운트 금지. |

**착수 전 값어치 있는 실험 하나** — 초선형의 **메커니즘**(레이아웃/스타일 재계산 vs CodeMirror 뷰 관리 vs DOM 형제 수)이 아직 미확정이다. 만약 레이아웃 축이면 **`content-visibility: auto` 나 창 밖 블록의 DOM 분리만으로 상당 부분이 회수**될 수 있고, 그것은 완전한 가상화보다 훨씬 싸다. **3번 착수 전에 이 구분만 확인하면 설계가 단순해질 수 있다.**
