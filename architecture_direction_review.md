# 방향 판단 — 단일 CM 안건 · 외부 리뷰 대 기존 계획

> **작성**: 2026-09-02 | **기준선**: `f9f6716` · v0.8.50 · 트리 클린
> **작성 역할**: 계획 세션 (SESS-20260831-PLANNER 인수)
> **입력 문서**: [`single_cm_transition.md`](single_cm_transition.md) *(구 `project/MEMORY_OPTIMIZATION.md`)* · [외부 리뷰](claude-history/review/code_review_external_20260831_2221.md) *(구 `new_view-point.md`)*
> **결정 반영**: 2026-09-02 — D-1~D-6 전부 결정됨. §8 참조. 게이트 티켓 [`GATE-20260902-01`](ticket/project/20260902_0400_single_cm_gate_reopen.yml)
> **대조 대상**: [`code_review.md`](code_review.md) §9 · [`DEBUG_STEP_PLAN.md`](DEBUG_STEP_PLAN.md) §0.1·§3-A-2·§4 · [`implementation_plan.md`](implementation_plan.md) · [`function_roadmap.md`](function_roadmap.md)
> **성격**: 두 신규 문서를 **코드로 검증**한 뒤, 기존 계획과의 트레이드오프를 판정한다. 처방은 §7 에만 적는다.

---

## 0. 결론 먼저

1. **단일 CM 안건의 표제 근거(메모리)는 틀렸다. 그러나 겨눈 축은 정확하다.** 메모리는 이 프로젝트에서 **한 번도 실패한 예산이 아니었다** — A6 실측이 블록당 42~98KB(예산 150KB **이내**)로 확정했고, 실패한 것은 오직 **시간**이다(N=200 에서 7,024~17,423ms, 예산 300ms). 문서가 주장하는 "인스턴스당 ~600KB · 문서당 18MB · 90% 절감"은 **측정치의 6~14배 과장**이며 근거가 없다. 그럼에도 이 제안이 중요한 이유는 **N→1 이 §3-A-2 가 확정한 초선형의 원인 축(동시 생존 `EditorView` 수)을 정확히 정면으로 없애기 때문**이다. 맞는 답을 틀린 이유로 제시한 문서다.

2. **`DEBUG_STEP_PLAN.md` §0.1 의 "재개봉되지 않는다" 게이트는 이미 무효다.** 그 게이트가 근거로 삼은 문장은 "순수 CodeMirror 코어는 선형이고 38배 싸다 → **블록당 인스턴스 설계는 무죄**"다. 그런데 §3-A-2 가 이 귀속을 뒤집었다 — **데코레이터가 하나도 없는 베어 조건도 누적 마운트 시 지수 1.76**(N=100, 737ms)이고, **생성 횟수는 같고 누적만 없앤 조건은 0.938**(92ms)이다. 즉 "순수 CM 은 선형"이라는 관측은 **동시 생존 수를 늘리지 않은 조건**에서 나온 것이었다. 무죄 판결의 근거가 무죄를 증명하지 않았다. **§0.1 은 형식적으로 재개봉되어야 한다.**

3. **다만 그대로 착수할 수 없다.** 문서가 "기존 12개 데코레이터 전부 재사용(그대로 동작)"이라고 적은 부분이 **코드와 어긋난다.** `orchestrator.ts:27-46` 의 `buildAll` 은 **문서 전체**(`0 ~ state.doc.length`)를 12개 데코레이터로 훑으며, **`docChanged` 뿐 아니라 `tr.selection` 에서도 전량 재실행**된다(`:333`). 지금 이것이 감당되는 이유는 소스 주석이 스스로 밝힌다 — *"Devoras splits blocks into separate CodeMirror instances, doc.length is small enough"*. **단일 CM 으로 가는 순간 이 전제가 소멸하고, 키 입력 한 번·화살표 한 번마다 12개 데코레이터가 문서 전체를 훑는다.** 마운트의 O(N^1.76) 을 입력당 O(N)×12 로 바꾸는 거래가 된다. **오케스트레이터의 증분·뷰포트화가 선행 조건이며, 이는 문서가 말하는 "3~5일 프로토타입"이 아니다.**

4. **외부 리뷰는 신규 데이터 유실 결함 2건을 실제로 잡았다.** 나머지는 대부분 기존 문서가 이미 아는 것이거나, 성능 축과 무관한 위생 항목이다. **취할 것만 취하고 나머지는 지금 열지 않는다** — 이유는 §6.

5. **가장 중요한 순서 판단**: 단일 CM 과 기존 계획의 **가상화(E7)는 같은 원인에 대한 두 개의 경쟁 답**이다. **둘 다 지으면 안 된다.** 그리고 기존 계획의 1·2·3번(R-6 · R-2 · 메커니즘 실험)은 **전부 4번(E7)의 준비 작업이거나 그것에 흡수되는 항목**이다. 따라서 **"E7 이냐 단일 CM 이냐"가 나머지 전부를 가두는 결정**이며, 이 결정 전에는 R-2 와 메커니즘 실험에 착수하지 않는 것이 맞다.

---

## 1. 검증 방법

이 문서의 모든 "맞다/틀렸다"는 **소스 대조**로 확인했다. 추정에는 그렇다고 적었다.

| 확인 항목 | 근거 |
|---|---|
| 메모리 실측치 | `ARCHITECTURE_FINDINGS.md:205` — "메모리는 블록당 42~98KB 로 예산(150KB) 내이므로, 문제는 **오직 시간**이다" |
| 시간 실측치 | 같은 문서 A6 표 — N=200 에서 7,024~17,423ms |
| 동시 생존 축 확정 | `DEBUG_STEP_PLAN.md` §3-A-2 측정 2 (누적 1.763 vs 즉시파괴 0.938) |
| `buildAll` 범위 | `project/src/shared/lib/editor/decorators/orchestrator.ts:27-46, 322, 333` — 직접 읽음 |
| 셀렉션 재빌드 | 같은 파일 `:331` — `tr.docChanged \|\| tr.selection \|\| ...` |
| 다중 라인 replace 실재 | `impl/LatexDecorator.ts:161-172` — `fenceFrom`(여는 펜스) → `fenceTo`(닫는 펜스) 범위의 `Decoration.replace` |
| 블록 카드가 실제 중첩 DOM | `BlockEditor.tsx:398-459` `BlockNode` 재귀 + `getLevelStyles` (L2 `p-5 rounded-2xl`, L3 `p-4 rounded-xl`) |
| 블록 트리 소비자 | `flattenTree`/`resolveBlocksFromContent` 참조 전수 — `BlockEditor.tsx` · `ReadView.tsx` · `tabStore.ts` 3곳뿐 |
| MindView 독립성 | `MindView.tsx` 는 `blocks` 를 import 하지 않는다. `MindNode`(=`parseMarkdown(rawContent)`)만 쓴다 |
| `createEditorTheme` 의존값 | `BlockEditor.tsx:86-105` — `fontFamily` · `fontSize` **둘뿐** |
| ERD 유실 경로 | `ErdDesignerMainView.tsx:16-49` — 직접 읽음 |
| 빈 `catch {}` | `settings/model/store.ts:79` · `workspace/model/recentStore.ts:79` |
| ErrorBoundary 부재 | `grep -rn "ErrorBoundary\|componentDidCatch" src` → **0건** |
| T1 하네스 수 | `package.json:15` — `test:t1` 은 **13종**을 연결한다 |

---

## 2. 단일 CM 안건 사실 검증

### 2.1 틀린 것 — 메모리 전제

문서 §1 의 핵심 수치가 근거 없이 제시됐고, 프로젝트 자신의 실측과 충돌한다.

| 문서 주장 | 실측 (A6, `ARCHITECTURE_FINDINGS.md:205`) | 배수 |
|---|---|---|
| 인스턴스당 ~600KB | **42~98KB** | 6~14배 과장 |
| 30블록 문서 ~18MB | **1.3~2.9MB** | 6~14배 과장 |
| "메모리 절감이 전환의 이유" | **메모리는 예산 내였다. 실패한 것은 시간뿐이다** | 전제 오류 |

**함의**: 이 제안을 "메모리 최적화"로 부르면 **의사결정이 틀린 축에서 이루어진다.** 판단해야 할 것은 메모리가 아니라 **마운트 시간의 초선형**이다. 문서 제목과 §1·§8 의 프레이밍은 **정정 대상**이다.

> 다만 DOM 노드 수 주장(30×200 → 뷰포트 내 ~200)은 방향이 맞다. 그리고 §3-A-2 의 미확정 질문 중 하나가 **"그 비용이 DOM 형제 수인지"** 이므로, DOM 노드 수 축소가 시간 축에 직접 듣는 수단일 **가능성**은 열려 있다 — 확정된 것은 아니다.

### 2.2 맞는 것 — 겨눈 축

§3-A-2 의 판정은 **"동시에 살아 있는 `EditorView` 인스턴스 수"** 다. 단일 CM 은 이 수를 **N → 1 로 구조적으로 고정**한다. 이는 다음을 의미한다:

- 가상화(E7)가 "동시 생존 수를 **창 크기로 상한**"하는 것이라면, 단일 CM 은 "**1 로 고정**"하는 것이다. **같은 축을 더 강하게 공격한다.**
- 그리고 뷰포트 렌더링의 **높이 맵 관리를 CodeMirror 라이브러리가 대신 진다.** 기존 계획이 E7 의 필수 동반 항목으로 못 박은 **높이 캐시**(`code_review.md` §9, BUG-20260828-01 재생산 위험)를 **우리가 직접 구현하지 않아도 된다.** 이것이 단일 CM 의 가장 큰 실질 이점이며, 문서 자신은 이 점을 강조하지 않는다.

### 2.3 과장된 것 — 제거 목록

문서 §3 의 "제거" 표는 **ReadView 를 계산에 넣지 않았다**(문서 §5.2·§5.3 이 스스로 "ReadView 는 표시용 파싱을 유지"라고 인정하면서도 §3 표를 정정하지 않았다).

`ReadView.tsx` 는 블록 단위 **드래그 재정렬**을 제공하고, 재정렬은 `join → 재파싱 → 2-패스 재매칭`을 탄다(`DEBUG_STEP_PLAN` §3-B). 즉 다음은 **살아남는다**:

| 문서가 "제거"로 표시 | 실제 |
|---|---|
| `resolveBlocksFromContent()` | **잔존** — ReadView 재정렬의 id 안정성이 여기 걸려 있다 |
| `buildTreeFromChunks()` | **잔존** — 같은 이유 |
| `flattenTree()` | **잔존** — `ReadView.tsx:302,345,352,358` |
| `deriveBlockKey()` / 2-패스 매칭 | **잔존** — Step 4(A7)의 성과물 전체가 살아 있어야 한다 |
| `BlockNode` 컴포넌트 | 쓰기 모드분만 제거. `ReadBlockNode` 는 남는다 |

**정확한 표현은 "제거"가 아니라 "쓰기 경로에서 소비자 소멸, ReadView 전용으로 축소"** 다. 복잡성은 줄지만 **삭제되지 않는다.** 문서가 약속한 "복잡성의 구조적 제거"는 절반만 참이다.

진짜로 소멸하는 것(확인함): `CodeMirrorBlock`, 블록 간 포커스 이동, 블록 병합, `focusToken`/`consumedFocusTokenRef` A1-A2 조정자, `updateBlockContent`(→ **R-2 가 통째로 소멸**), `editorViewRegistry` 의 blockId 축.

### 2.4 빠진 것 ① — 오케스트레이터가 문서 전체를 훑는다 **(가장 중요)**

```ts
// orchestrator.ts:27-46
function buildAll(state, decorators) {
  // Since Devoras splits blocks into separate CodeMirror instances,
  // doc.length is small enough to parse the whole block synchronously.
  const from = 0;
  const to = state.doc.length;
  const sets = decorators.map((d) => d.createDecorations(state, from, to));
  return RangeSet.join(sets);
}

// :331 — 재빌드 조건
if (tr.docChanged || tr.selection || tr.effects.some(e => e.is(setImeEffect))) {
  return buildAll(tr.state, decorators);
}
```

**소스 주석이 명시적으로 "블록당 CM 이라 문서가 작아서 괜찮다"고 적어 두었다.** 단일 CM 전환은 이 주석의 전제를 직접 파괴한다. 전환 후 비용:

- **키 입력 1회** → 12개 데코레이터 × 문서 전체 스캔
- **화살표 1회** → 동일 (셀렉션도 재빌드 조건이다)
- 게다가 데코레이터들은 **커서 위치를 읽는다**(`LatexDecorator.ts:164` `cursorInside`, 마크업 노출/은닉 UX 의 본질). 즉 **셀렉션 재빌드를 그냥 끌 수 없다.**

문서 §4.1 의 예제 코드가 정확히 이 함정에 빠져 있다 — `scanBlockRanges(doc)` 로 **문서 전체를 스캔**한다. R-2(입력당 트리 전체 재생성)를 없애면서 **같은 형태의 입력당 O(N) 을 데코레이터 계층에 새로 만드는** 셈이다.

**해결 가능하지만 공짜가 아니다.** 그리고 여기에 CM6 의 구조적 제약이 걸린다:

> **뷰포트 한정 빌드는 `ViewPlugin`(`view.visibleRanges` 접근 가능)을 요구하는데, 다중 라인 `Decoration.replace` 는 `StateField` 를 요구한다.** 현행 오케스트레이터가 `StateField` 인 이유가 정확히 이것이고(`:317` 주석 "to support multi-line replacement"), 그 다중 라인 replace 는 **실재한다**(`LatexDecorator.ts:161-172` KaTeX 블록 펜스). CM6 는 뷰포트 계산 이전에 높이에 영향을 주는 데코레이션을 알아야 하므로 `ViewPlugin` 에서 이를 제공할 수 없다.

따라서 선행 작업은 "뷰포트로 감싸기" 한 줄이 아니라 **오케스트레이터를 2계층으로 분리**(구조 변경 replace 는 증분 `StateField`, 나머지는 뷰포트 `ViewPlugin`)하거나, **`StateField` 를 증분화**(`value.map(tr.changes)` 후 변경 구간 + 캐럿 인접 구간만 패치)하는 **설계 과제**다.

**이것이 이 제안의 진짜 비용이며, 문서는 언급하지 않는다.** 그리고 이것은 착수 여부와 무관하게 **`Open Q #5`(`protectedRegions` N² 부채)와 같은 지점**이다 — 단일 CM 은 그 부채를 문서 전체 규모로 확대한다.

### 2.5 빠진 것 ② — 블록 카드가 실제 중첩 DOM 이다

문서 §4 는 `Decoration.line()` 으로 카드 UI 를 재현할 수 있다고 하며 `CodeBlockDecorator` 를 선례로 든다. 그러나 현행 카드는 **재귀 중첩 DOM** 이다:

```tsx
// BlockEditor.tsx:398-459 (요약)
<div className={getLevelStyles(block.level)}>   // L2: p-5 rounded-2xl bg-[#141520] border
  <CodeMirrorBlock ... />
  <div className="block-children">
    <BlockNode ... />                            // L3: p-4 rounded-xl bg-[#1d1f30] border ← L2 안쪽
  </div>
</div>
```

`Decoration.line()` 이 만드는 것은 **평면 형제 `.cm-line` 들**이다. H2 카드 안에 H3 카드가 들어앉고 **H2 의 좌/우 테두리가 H3 구간을 관통해 연속**되어야 하는 그림을, 라인 단위 클래스만으로 재현하려면 좌우 마진 누적 + 다중 `box-shadow`/의사요소 조합이 필요하다. **불가능하지 않지만 `CodeBlockDecorator`(중첩 없는 단일 레벨 배경)가 증명한 패턴은 아니다.**

**판정**: 문서 §7 위험표의 "카드 시각화 품질 — 중간, CodeBlockDecorator 가 이미 증명"은 **근거가 약하다.** 이것은 프로토타입에서 **가장 먼저 확인해야 할 항목**이지, 낮은 위험이 아니다.

### 2.6 빠진 것 ③ — 회귀망과 IME

- **T1 하네스 13종 중 최소 5종**(`blockkey` · `caretstability` · `tabstore` · `diff` · `vertical_motion`(T2))이 **현행 아키텍처의 불변식을 인코딩**한다. 단일 CM 은 이 중 상당수를 무효화하거나 재작성을 요구한다.
- 그런데 지금은 **R-1**(React 테스팅 라이브러리 부재, `code_review.md` §2) 이 미해결이다. 즉 **회귀망이 가장 약한 시점에 가장 큰 구조 변경을 하는 형태**가 된다. 이번 사이클의 심각한 결함 3건이 **전부 리팩터 자신이 만들었고 전부 자동 테스트를 통과했다**는 §0 의 관측이 그대로 적용된다.
- **IME**: 3중 방어 래치(`ImeLatchPlugin` · `ImeIsolation` · `useImeInputManager`)는 **블록당 뷰**를 전제로 조립됐다. 문서는 "그대로 사용"이라 적었으나 검증된 바 없고, BUG-20260828-05(첫 조합 간헐 실패)가 여전히 열려 있다. **이 프로젝트에서 IME 회귀는 가장 비싼 결함군이다.**

---

## 3. §0.1 게이트 — 재개봉 판정

`DEBUG_STEP_PLAN.md` §0.1 은 다음과 같이 닫혀 있다:

> **✅ 게이트 통과 (2026-08-30, v0.8.43) — 이 결정은 재개봉되지 않는다.**
> … 초선형의 원인은 `EditorView` 생성이 아니라 **Devoras 자체 데코레이터 확장 계층**으로 특정됐다. 즉 **「블록당 CodeMirror 인스턴스」라는 설계 자체는 무죄**이며 …

> ### ⚠️ 추가 발견 (2026-09-02, 문서 정리 중) — **이 구조는 두 번 기각됐다**
>
> `architecture_stages.md:56` (2026-08-27):
>
> | **Option A(문서 전체 단일 CM) 를 택하지 않은 이유** | H2/H3 를 중첩 라운드 카드로 렌더하는 현재 시각 디자인을 **평면 라인 목록으로는 표현할 수 없다** |
>
> **이 기록은 앞선 분석(§0~§2)에도 없었다.** 그리고 두 기각의 성격이 다르다:
>
> - **§0.1(2026-08-30)** — 측정에 기반했으나 **그 측정이 무효화**됐다(아래)
> - **Option A(2026-08-27)** — **애초에 측정된 적이 없는 단언**이다
>
> 후자는 이 프로젝트가 §3-C 에 스스로 적어둔 규칙("처방을 측정보다 먼저 쓰지 말 것")을 어긴 사례다.
> **스파이크 A 가 그 부채를 갚는다** — 그리고 단언이 맞는 것으로 나오면 그것도 유효한 결과다.
> 안건이 근거를 갖고 종결된다.
>
> **실무적 함의**: 스파이크 A 는 단순한 프로토타입이 아니라 **2026-08-27 판단의 사후 검증**이다.
> A → B 순서(사용자 결정 D-2)가 옳은 또 하나의 이유이기도 하다.

**§0.1 의 근거는 §3-A-2 가 소멸시켰다.** 두 가지를 구분해야 한다:

| 명제 | 상태 |
|---|---|
| "`EditorView` **생성 행위** 자체는 선형이고 싸다" | **여전히 참** (즉시파괴 조건 지수 0.938, 92ms) |
| "따라서 **블록당 인스턴스 설계**는 무죄다" | **거짓** — 그 설계의 정의는 "인스턴스를 **동시에 N개 살려 둔다**"이고, 초선형은 정확히 거기서 나온다(누적 조건 1.763, 737ms) |

§0.1 은 **생성 비용의 무죄를 설계의 무죄로 잘못 확대**했다. §3-A-2 가 스스로 적었듯 "Step 6 의 판정은 귀속이 틀렸다". 그 판정 위에 세워진 게이트도 함께 무효다.

**추가로 짚을 것**: §0.1 이 리라이트를 기각한 논거 3개 중 두 번째와 세 번째는 **`MEMORY_OPTIMIZATION.md` 에 적용되지 않는다.**

- §0.1 이 검토한 것은 **"Notion 형태"** = **블록 트리를 진실로 삼는** 구조다. 그래서 "마크다운 파일이 디스크의 진실이라는 전제를 깨는 것은 리팩터링이 아니라 제품 교체"라는 반박이 성립했다.
- **`MEMORY_OPTIMIZATION.md` 는 정반대 방향이다** — `rawContent` 를 유일한 진실로 삼는다. 이는 §0.1 이 지키려 한 전제(MindView·ERD·`headingId`·Rust 전역 검색이 의존하는 "마크다운이 진실")를 **더 충실히** 만족한다. MindView 가 `blocks` 를 전혀 참조하지 않는다는 사실(§1 확인)이 이를 뒷받침한다.
- 남는 논거는 첫 번째뿐이다 — "A1~A7 중 4건은 에디터 스택과 무관하다." **이건 여전히 유효하고 중요하다.** 스택을 바꿔도 A1(이중 진실)·A4(검증 티어)·A5(불변식) 계열은 따라온다. **즉 단일 CM 은 만병통치가 아니다.**

> **판정: §0.1 의 "재개봉되지 않는다"는 철회하고, 게이트를 다시 연다.** 다만 재개봉된 선택지는 §0.1 이 기각한 "Notion 리라이트"가 아니라 **"단일 CM + rawContent 단일 진실"** 이라는 **다른 안건**이다. 이 구분을 문서에 명시해야 다음 세션이 §0.1 을 근거로 안건을 다시 닫는 사고를 막을 수 있다.

---

## 4. 기존 계획과의 트레이드오프

`code_review.md` §9 의 확정 순서는 다음과 같다.

> 1. R-6 테마 호이스팅 → 2. R-2 경로 복사 → 3. 메커니즘 확인 실험 → 4. 가상화(E7) + 높이 캐시

단일 CM 이 안건에 오르면 **네 항목의 값어치가 전부 바뀐다.**

| # | 항목 | E7 경로에서 | 단일 CM 경로에서 | 판정 |
|---|---|---|---|---|
| 1 | **R-6** 테마 호이스팅 | 필수. 창 안 블록들이 그대로 치르는 상수 50% | **거의 무의미** — 인스턴스가 1개면 StyleModule 도 1개. 누수 자체가 소멸 | **지금 해도 손해 없음.** 변경 범위 좁고, 어느 경로든 해가 없다. 단 "어차피 해야 한다"는 §9 의 논거는 단일 CM 경로에서 **성립하지 않는다** |
| 2 | **R-2** `updateBlockContent` 경로 복사 | 필요. 입력당 비용, 다른 축 | **100% 폐기** — `updateBlockContent` 자체가 쓰기 경로에서 소멸(§2.3 확인) | **결정 전 착수 금지.** 순수 매몰 비용이 될 수 있다 |
| 3 | **메커니즘 확인 실험** (레이아웃 vs CM 뷰 관리 vs DOM 형제 수) | 유의미. 레이아웃 축이면 `content-visibility` 로 가상화를 회피할 수 있다 | **결정 가치 대부분 소멸** — 어느 메커니즘이든 N→1 이 답이다 | **결정 전 착수 금지.** 이 실험은 "E7 을 어떻게 싸게 만들까"를 묻는 실험이고, 단일 CM 이 채택되면 묻지 않는 질문이 된다 |
| 4 | **E7 가상화 + 높이 캐시** | 계획의 본체 | **직접 대체됨** — 라이브러리가 대신 한다 | **둘 중 하나만 짓는다.** 이것이 결정의 본체 |

**추가 상호작용 — Sprint 3(YAML 테마)와 R-6 의 수렴 (신규 발견)**

R-6 티켓(`BUG-20260831-02`)이 권고한 근본 처방은 *"(b) 설정 의존 값을 **CSS 변수**로 빼서 테마 자체를 상수화"* 다. 그런데 `implementation_plan.md` **Sprint 3** 이 만들려는 것이 정확히 **CSS 변수 기반 디자인 토큰 시스템**(`entities/theme/lib/injector.ts`)이다.

그리고 `createEditorTheme()` 의 설정 의존값은 **`fontFamily` · `fontSize` 둘뿐이다**(`BlockEditor.tsx:86-105`, 확인함). **CSS 변수 2개면 테마가 상수가 된다.**

> **함의**: R-6 를 독립 배치로 처리하면 Sprint 3 이 같은 것을 다시 짓는다. **Sprint 3 의 토큰 인젝터를 먼저 세우고 그 위에서 R-6 를 닫는 편이 총량이 적다** — 그리고 그 경우 `BlockEditor.tsx:144` 의 `reconfigure` 경로까지 함께 소멸한다. `code_review.md` §9 의 "R-6 를 1번에 둔다"는 이 수렴을 계산에 넣지 않았다. **재검토 대상이다.**

**남는 정합성**: `DEBUG_STEP_PLAN.md` §2 의 "Step 7-C 는 `advanced_rendering_optimization.md` Phase 1(`serialize()`/`hydrate()` 계약)과 동일 지점"은 **어느 경로에서도 유효**하다. 탭 스코프 스토어는 이미 착지했고(v0.8.49), 그 계약은 단일 CM 에서도 그대로 쓰인다(`rawContent` 하나로 오히려 단순해진다).

---

## 5. 두 경로의 정직한 비교

원인 축이 같으므로 **효과가 아니라 비용과 위험으로 갈린다.**

| | **E7 가상화 + 높이 캐시** | **단일 CM 전환** |
|---|---|---|
| 동시 생존 수 | 창 크기로 상한 (윈도 ±2 화면) | **1 로 고정** |
| 높이 맵 | **우리가 구현** — BUG-20260828-01(수직 이동 줄 스킵) 계열 재생산 위험이 명시된 항목 | **CodeMirror 가 소유** — 우리 코드에서 소멸 |
| 데코레이터 계층 | **손 안 댐** (블록당 doc 가 작다는 전제 유지) | **선행 재설계 필요** — 증분·뷰포트화, `StateField`/`ViewPlugin` 분리 (§2.4) |
| 카드 UI | **그대로** (DOM 중첩 유지) | **재현 필요** — 중첩 카드가 최대 미확인 항목 (§2.5) |
| IME 3중 래치 | 그대로 | **재검증 필요** (미확인) |
| Undo 범위 | 블록별 유지 | 문서 단위로 변경 (사용자 가시 동작 변경) |
| T1 회귀망 | 대체로 유지 | **최소 5종 재작성** |
| 삭제되는 복잡성 | 없음 (오히려 증가) | A1/A2 조정자 · `focusToken` · 블록 병합/포커스 이동 · R-2 · `CodeMirrorBlock` |
| 제품 전제 정합 | 유지 | **더 좋아짐** (`rawContent` 단일 진실) |
| 되돌리기 | 쉬움 (플래그로 끌 수 있는 형태 가능) | **어려움** — 되돌릴 수 없는 단계에 가깝다 |

**`lessons_that_cost_time_to_learn` 의 "되돌릴 수 없는 단계는 맨 뒤로"** 원칙이 여기 직접 적용된다. 그리고 그 원칙이 성공한 사례(7-C 를 C-1~C-5 로 쪼개 싱글턴 제거를 C-4 에 둔 것)가 **그대로 재사용 가능한 형태**다 — 단일 CM 은 `viewMode === 'write'` 분기 교체로 시작하므로, **읽기 모드와 기존 쓰기 경로를 남긴 채 병행 가능**하다.

---

## 6. 외부 리뷰 트레이드오프

### 6.1 문서의 성격

**FSD 레이어별 냉시작 전수 감사**다. 이는 `code_review.md` §0 이 **의도적으로 버린 방식**이다 — 이번 사이클의 심각한 결함 3건이 전부 리팩터 자신이 만든 것이었으므로 "바뀐 표면"으로 범위를 좁혔다. 두 문서는 **다른 질문에 답하고 있고, 어느 쪽도 틀리지 않았다.**

**다만 외부 리뷰는 프로젝트의 측정된 현실을 모른다**: A6(N=200 에서 7~17초 마운트), R-1(React 테스트 부재), StyleModule 누수, 디버그 사이클 전체를 참조하지 않는다. 그 결과 심각도 순위가 프로젝트 실태와 어긋난다 — 예컨대 "🏆 목적 지향 테스트 전략(Harness)"을 우수 사례로 꼽지만, **바로 그 하네스가 React 계층에 닿지 못한다는 것이 현재 1순위 결함(R-1)** 이다.

**따라서 이 문서를 목록째 채택하면 안 된다.** 확정된 원인 축이 미해결인 시점에 작업 표면을 다시 넓히는 형태가 된다.

### 6.2 취할 것 — 코드로 검증한 신규 결함

| 항목 | 검증 결과 | 판정 |
|---|---|---|
| **ERD 파싱 실패 시 빈 문서 덮어쓰기** (`ErdDesignerMainView.tsx:16-49`) | **실재.** `JSON.parse` 실패 → `parseErdDocument` 재시도 실패 → `createEmptyErdDocument()` → `setRawContent(normalized)` **+ `updateContentForTab()`**. 즉 메모리상 문서가 빈 것으로 교체되고 탭이 dirty 가 된다 | **🟠 신규 티켓.** 단, 심각도는 `new_view-point` 의 "영구 유실"보다 **한 단계 낮다** — **자동 저장이 실제로 배선돼 있지 않다**(§6.4). 디스크 유실은 사용자가 `Cmd+S` 를 눌러야 성립한다. 그럼에도 "손으로 고친 `.erd` 를 열었더니 빈 캔버스 → 습관적으로 저장 → 파일 소멸" 경로는 현실적이다 |
| **`settings/store.ts:79` · `recentStore.ts:79` 빈 `catch {}`** | **실재.** settings 는 파싱 실패 시 `DEFAULT_SETTINGS` 로 덮고 로그도 남기지 않는다 | **🟡 신규 티켓.** 비용이 거의 0 이고 데이터 유실 인접. recentStore 쪽은 "파일 없으면 첫 실행" 주석이 있어 **의도된 것**이므로 settings 만 실질 결함이다 |
| **ErrorBoundary 부재** (`grep` 0건) | **실재** | **기존 항목과 병합.** 이것은 신규가 아니라 **BUG-20260828-06**(위젯 `toDOM` 예외가 CM 뷰·React 서브트리를 붕괴)의 **완화 수단**이다. 별도 티켓 대신 그 티켓의 처방으로 편입하는 것이 맞다 |
| **Rust `close_splashscreen` 의 `unwrap()`** | 미대조(Rust 파일 직접 확인 안 함) | **기존 P2-7 "Rust panic" 과 중복 가능성.** 착수 시 대조 |

### 6.3 지금 열지 않을 것

| 항목 | 이유 |
|---|---|
| P1 `flattenTree` 메모이제이션 | **R-2 와 같은 대상.** `code_review.md` §3 의 경로 복사 처방이 더 구체적이고 정확하다. 그리고 §4 표대로 **결정 대기** 항목이다 |
| Q1 컴포넌트 분할 (BlockEditor 744줄) | **단일 CM 이 채택되면 이 파일은 대폭 축소된다.** 지금 쪼개면 매몰 비용 |
| A1 Cross-Slice 의존 해소 | 사용자 영향 0. 위생 항목. **Step 7-C 로 이미 구조가 바뀐 직후**라 지금 또 만지면 회귀 위험만 산다 |
| P2/P3/P4 (리사이저·MindView 드래그·FileExplorer memo) | 전부 **측정된 적 없는 성능 추정**이다. 이 프로젝트의 §3 규칙("처방을 측정보다 먼저 쓰지 말 것")에 정면으로 걸린다 |
| Q4 `any` 근절 · Q5 접근성 | 유효하나 **`function_roadmap` Phase 5 기술 부채 항목**으로 편입할 사안이지 지금 배치가 아니다 |

### 6.4 이 대조에서 나온 부수 발견 (양쪽 문서 모두 놓침)

1. **`autosaveDelay` 설정이 소비자 없이 존재한다.** `settings/model/types.ts:4,32` 에 정의되고 `SettingsModal.tsx:113-120` 에서 사용자가 조절할 수 있으나, **`grep` 상 이 값을 읽는 코드가 없다.** 즉 **사용자에게 노출된 설정이 아무 일도 하지 않는다.** (역설적으로 이것이 §6.2 ERD 결함의 심각도를 낮춘다.)
2. **T1 하네스 수 드리프트.** `code_review.md` §2 는 "14종", `DEBUG_STEP_PLAN` §5.1 은 "9 하네스", `package.json:15` 의 `test:t1` 실제 연결은 **13종**이다. 또한 `heading_caret_harness.ts` 는 존재하지만 **`test:t1` 에 연결돼 있지 않다** — 실행되지 않는 하네스다.

---

## 7. 확정된 순서 (2026-09-02 사용자 결정 반영)

### 7.1 게이트

| # | 항목 | 티켓 | 상태 |
|---|---|---|---|
| **0** | **R-1 — React 계층 검증 티어** (`@testing-library/react` + `jsdom`) | `REF-20260902-01` | 착수 승인 (D-3) |
| **1** | **스파이크 A — 중첩 카드 시각 재현** | `SPIKE-20260902-A` | Todo — **안건의 생사를 가른다** |
| **2** | **스파이크 B — 오케스트레이터 증분화** | `SPIKE-20260902-B` | Blocked (A 대기) |
| **3** | 판정 → 단일 CM 전환 **또는** E7 복귀 | — | 대기 |

**A → B 인 이유**(D-2): A 가 실패하면 B 를 잰 비용이 통째로 버려진다. A 는 CSS·데코레이션 실험이라 B 보다 싸고 판정이 육안으로 즉시 난다. **비싼 쪽을 뒤에 둔다.**

**R-1 을 0번에 둔 것의 단서**: 인프라는 단일 CM 과 독립이지만 **테스트 대상은 아니다.** 블록 경계 동작(포커스 이동·병합) 테스트는 단일 CM 채택 시 소멸한다. 그래서 `REF-20260902-01` 의 첫 대상을 **어느 경로에서도 살아남는 표면**(저장/dirty · viewMode 토글 · 탭·패널 전환 · FileExplorer)으로 한정했다. 블록 경계 테스트는 게이트 3 판정 후에 쓴다.

### 7.2 게이트와 무관하게 즉시 착수 가능

| ID | 항목 | 티켓 | 상태 |
|---|---|---|---|
| I-1 | ERD 파싱 실패 시 빈 문서 덮어쓰기 차단 | `BUG-20260902-01` | 발행됨 |
| I-2 | `settings/store.ts:79` 빈 `catch` — 로깅 + `.bak` 보존 | 미발행 | |
| I-3 | **R-3** `crypto.randomUUID()` 교체 | 미발행 | ReadView 재정렬 id 가 어느 경로에서도 살아남으므로 유효 |
| I-4 | 문서 드리프트 정정 | — | **완료** — `code_review.md` §10 |

### 7.3 보류 (게이트 3 판정까지)

- **R-2 경로 복사** — 단일 CM 채택 시 100% 매몰 비용
- **메커니즘 확인 실험** — 단일 CM 채택 시 묻지 않는 질문이 된다
- **E7 가상화 + 높이 캐시** — 경쟁 안건. A 또는 B 실패 시 확정된다

### 7.4 재배치된 것

- **R-6 테마 호이스팅 → Sprint 3 병합**(D-4). `createEditorTheme()` 의 설정 의존값이 `fontFamily`·`fontSize` 둘뿐이라, R-6 의 근본 처방과 Sprint 3 의 CSS 변수 토큰 시스템이 같은 물건이다. 독립 배치로 하면 Sprint 3 이 같은 것을 다시 짓는다.

---

## 8. 결정 기록 (2026-09-02)

| # | 결정 | 반영 위치 |
|---|---|---|
| **D-1** | **게이트를 연다** — 단일 CM 을 E7 의 경쟁 안건으로 정식 개봉 | `DEBUG_STEP_PLAN.md` §0.1 재개봉 블록 (원 판정은 `<details>` 보존) |
| **D-2** | **스파이크를 먼저 잰다. 순서는 A → B** | `SPIKE-20260902-A` (Todo) · `SPIKE-20260902-B` (Blocked) · `single_cm_transition.md` §6 게이트 다이어그램 |
| **D-3** | **R-1 을 게이트 0 으로 선행** | `REF-20260902-01` |
| **D-4** | **R-6 를 Sprint 3 에 병합** | `implementation_plan.md` Sprint 3 편입 블록 · `BUG-20260831-02` 티켓 · `function_roadmap.md` Phase 3.5 |
| **D-5** | **안건 문서로 승격 + 메모리 전제 정정** | `project/MEMORY_OPTIMIZATION.md` → `single_cm_transition.md` (§0 정정 · §0.1 기각 기록 · §3 주의 · §6 게이트 · §7.1 블로커 · §8 개정) |
| **D-6** | **문서 배치 정리** | `DOCUMENTS.md` 신설 — 루트=살아 있는 계획 / `project/`=앱 소스(문서 없음) / `claude-history/`=아카이브. 11건 이동, 중복 1건 삭제 |

**여전히 열려 있는 결정** (기존): `implementation_plan.md` Sprint 4A 사양 중복 · 퀵 캡처 미해결 5건 · 마인드뷰 메인 뷰 전환.

---

## 9. 계획 세션의 판단 한 줄

**단일 CM 은 "메모리 최적화"가 아니라 A6 의 정답 후보다.** 그리고 §0.1 이 그것을 닫아둔 근거는 §3-A-2 가 이미 무너뜨렸다. 그러나 문서가 약속한 "기존 데코레이터 그대로 재사용"은 **코드와 어긋나며**(§2.4), 그 어긋남이 이 제안의 진짜 가격표다. **두 스파이크로 그 가격을 먼저 재고 나서 E7 과 비교하는 것이 이 프로젝트가 Step 6 에서 비싸게 배운 순서다** — 처방을 측정보다 먼저 쓰지 않는다.
