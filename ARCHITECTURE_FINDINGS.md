# ARCHITECTURE_FINDINGS.md — 구조적 결함 분석 (2026-08-28)

> **성격**: `DEBUG_PLAN.md`(0.8.11 검증 게이트) 완주 과정에서 드러난 **개별 버그가 아닌 구조 문제**의 기록.
> 계획서는 완료되어 [`claude-history/debug/DEBUG_PLAN_20260828_203337.md`](claude-history/debug/DEBUG_PLAN_20260828_203337.md) 로 아카이브했고,
> **다음 작업의 출발점은 이 문서**다.
> 모든 주장은 이번 세션에서 실제로 **측정한 값**에 근거한다. 추정은 추정이라고 명시했다.

---

## 요약

이번에 고친 세 결함(BUG-20260828-01/02/03)은 서로 다른 증상이지만 **원인은 세 갈래로 수렴**한다.

| # | 구조적 결함 | 이번에 그것이 만든 증상 |
|---|---|---|
| **A1** | 편집 상태의 **진실 공급원이 둘**이고, 그 조정을 React 이펙트 실행 순서에 맡김 | BUG-20260828-02(병합 캐럿 소실), BUG-20260827-01/02 도 같은 계열 |
| **A2** | **문서 전체 치환**을 일상적 동기화 수단으로 사용 | BUG-20260828-02 의 직접 원인 |
| **A3** | CSS 가 CodeMirror 의 **레이아웃 불변식**을 암묵적으로 침범할 수 있는 구조 | BUG-20260828-01(수직 이동 줄 스킵) |
| **A4** | 검증 티어가 **선언만 되어 있고 실행 수단이 없음** | 세션마다 반복된 검증 부채, BUG-20260828-03 |
| **A5** | 게이트가 **최종 증상만** 관측하고 중간량을 관측하지 않음 | §9.6 이 T3 한 세션을 쓰고도 원인에 도달하지 못함 |
| **A6** | 블록당 EditorView 설계가 **자체 예산을 초과**하고 비용이 초선형 | G3-1 실측 실패 (아래) |
| **A7** | 헤딩 블록의 **정체성이 내용에서 파생** | G0 의 잔여 위험 — 아직 살아 있음 |

A1·A2 는 수정했다. A3 는 수정 + 불변식 문서화 + 회귀 하네스로 고정했다. A4 는 복구했다.
**A5·A6·A7 은 남아 있으며, 다음 사이클의 실제 작업 대상이다.**

---

## A1. 편집 상태의 진실 공급원이 둘이다

`useBlockStore`(`block.content`, `focusOffset`, `activeBlockId`)와 각 `EditorView`(`state.doc`, `state.selection`)가
**둘 다 권위를 주장**한다. 둘의 조정은 `project/src/widgets/BlockEditor/ui/BlockEditor.tsx` 안의 **서로 다른 두 이펙트**가 맡는데,

- 캐럿 복원은 `useLayoutEffect`, 의존성 `[isFocused, focusOffset]`
- 내용 동기화는 `useEffect`(passive), 의존성 `[block.content]`

React 는 **모든 layout effect 를 passive effect 보다 먼저** 돌린다. 따라서 캐럿은
*아직 교체되지 않은 옛 문서* 위에 놓인 뒤, 곧이어 문서가 통째로 갈리며 다시 밀려난다.
그리고 `focusOffset` 값 자체는 변하지 않았으므로 **복원 이펙트가 다시 돌지도 않는다.**

계측으로 확정한 실패(BUG-20260828-02):

```
Backspace 로 블록 병합
  store.focusOffset = 16   ← 병합 지점, 올바름
  view.selection.head = 31 ← 문서 끝, 틀림
  view.hasFocus = false
```

이것이 개별 버그가 아니라 **계열**이라는 근거: 최근 3건이 전부 같은 모양이다.

- `BUG-20260827-01` — 등록 이펙트를 생성 이펙트보다 **먼저 선언**해서 레지스트리가 영구히 비었다.
- `BUG-20260827-02` — `defaultKeymap` 을 `blockKeymap` 보다 **먼저 배치**해서 경계 핸들러가 죽었다.
- `BUG-20260828-02` — 캐럿 복원이 내용 교체보다 **먼저 실행**되어 캐럿이 날아갔다.

세 건 모두 "무엇을 하는가"가 아니라 **"어떤 순서로 선언·등록했는가"** 에 정확성이 걸려 있다.
그 순서는 코드 어디에도 명시되어 있지 않고, 주석으로만 방어된다(실제로 두 곳에 경고 주석이 있는데도 세 번째가 났다).

**처방**
1. 내용과 selection 은 **한 트랜잭션**으로 움직인다. (이번에 적용 — A2 참조)
2. 뷰↔스토어 조정을 **이펙트 두 개가 아니라 하나의 조정자**로 모은다.
   블록 하나의 "원하는 상태"(content + selection + focus)를 한 객체로 만들고,
   그것과 실제 `EditorView` 상태의 차이를 **한 곳에서** 한 번에 적용한다.
3. 불변식을 **런타임 단언**으로 만든다: 포커스된 블록에서 `store.focusOffset === view.selection.main.head`.
   개발 빌드에서 이 단언 하나만 있었어도 BUG-20260828-02 는 즉시 드러났다. (A5 참조)

---

## A2. 문서 전체 치환을 동기화 수단으로 쓴다

```ts
changes: { from: 0, to: currentDoc.length, insert: block.content }
```

이 한 줄은 "내용을 맞춘다"처럼 보이지만 실제로는 **문서를 파괴하고 다시 만든다.**
CodeMirror 에서 전체 치환은

- 치환 구간 안의 **커서를 삽입 텍스트 끝으로 이동**시키고 (← BUG-20260828-02 의 직접 원인),
- undo 히스토리의 입도를 뭉개고,
- 모든 데코레이션 위치를 무효화하며,
- 전체 재측정을 강제한다.

그런데 이것이 예외 경로가 아니라 **평상시 동기화 경로**이고, 방어 수단은 문자열 `!==` 비교 하나뿐이다.

**처방**
- 즉시(적용 완료): 치환 트랜잭션에 selection 을 **명시적으로 함께 지정**한다.
- 다음: 전체 치환 대신 **최소 차이(diff) 기반 변경**을 적용한다.
  블록 내용은 대개 한두 글자만 다르므로, 공통 접두/접미를 잘라낸 최소 범위 치환만으로 충분하다.
  이렇게 하면 커서·undo·데코레이션이 **자동으로** 보존되어 A1 의 조정 부담 자체가 줄어든다.

---

## A3. CSS 가 CodeMirror 의 레이아웃 불변식을 침범할 수 있다

BUG-20260828-01 의 본질은 "블록쿼트 버그"가 아니라 **계층 경계의 부재**였다.

CodeMirror 는 라인 높이를 `getBoundingClientRect().height` 로 재어 height map 에 쌓는다.
그 값에는 **margin 이 들어가지 않는다.** 그런데 `.cm-line` 에 붙는 클래스의 스타일은
**두 곳**에서 자유롭게 작성된다 — `project/src/shared/lib/editor/decorators/orchestrator.ts` 의 `decorationBaseTheme` 과 `project/src/app/styles/index.css`.
어느 쪽에도 "여기에 수직 margin 을 쓰면 안 된다"는 표시가 없었다.

측정된 결과: 헤딩 라인마다 (margin-top + margin-bottom) 만큼 실제 DOM 좌표와 height map 좌표가 어긋나고,
그 오차가 누적된다. 수직 이동은 두 좌표계를 섞어 쓰므로(실제 DOM 으로 목표 y 계산 → height map 으로 위치 역산)
오차가 라인 높이에 육박하면 **한 번에 두 줄을 건넌다.**

```
혼합 문서 drift 누적: 12 → 20 → 30 → 36.39 → 44.39 → 49.98 → 56.38 → 60.38(이후 평평)
                      ↑ 증가 지점은 전부 헤딩 라인. 다른 데코레이터 기여분은 정확히 0.
```

**처방**
- 적용 완료: margin → padding, 양쪽 파일에 불변식 주석, `drift === 0` 회귀 하네스.
- 다음: 라인 레벨 스타일의 **작성 지점을 하나로 모은다.** 지금은 base theme 과 전역 CSS 가
  같은 `.cm-*` 라인 클래스를 각자 건드릴 수 있고, 특이도 싸움까지 존재한다
  (헤딩 padding 이 `.cm-line { padding: 0 4px }` 를 이기는 것은 선택자 특이도 3 vs 2 라는 **우연**에 기대고 있다).

---

## A4. 검증 티어가 선언만 되어 있고 실행 수단이 없었다

`DEBUG_PLAN §2` 는 T0~T3 티어 모델을 정교하게 규정했다. 그러나 이번에 확인한 실상은:

| 티어 | 계획서상 | **실제 상태 (2026-08-28 확인)** |
|---|---|---|
| T1 (plain node) | "순수 로직은 T1 을 유지한다" | **전 하네스 실행 불가였다.** `--experimental-strip-types` 는 Node 22.6+ 전용인데 실행 환경은 Node 20 → `pnpm test:symbol`/`test:image` 모두 `bad option` 으로 즉사. **→ 런타임을 Node 24 LTS 로 고정하고 로더 훅을 붙여 복구 완료** |
| T2 (Chromium) | "playwright MCP 로 빠른 반복" | **앱을 띄우는 수단이 저장소에 없었다.** 실제로는 `project/src/shared/api/fs.ts` 의 `MockFileSystem` 덕분에 브라우저에서 앱이 그대로 돈다는 사실이 활용되지 않고 있었다 |
| T3 (WKWebView) | "전부 측정 가능" | `tauri-plugin-mcp-bridge` 를 **세션마다 붙였다 뗐다** 해야 함 → T3 증거는 재현도 회귀 고정도 불가능 |

`§9.1` 은 T1 문제를 "K1~K3 하네스 한 건의 경로/별칭 문제"로 진단했지만,
실제로는 **티어 전체가 죽어 있었다.** 그래서 "하네스를 커밋했다"와 "검증이 실행됐다"가 계속 분리되었고,
계획서 §8 이 세션마다 미실행 게이트를 이월한 구조적 원인이 이것이다.

더 일반화하면 이것은 **런타임 드리프트가 조용히 검증을 무력화한 사건**이다.
저장소에는 필요한 Node 버전을 명시한 곳이 **한 군데도 없었고**(`.nvmrc` 없음, `engines` 없음),
그래서 실행 환경이 요구 조건을 벗어난 사실이 아무 데서도 드러나지 않았다.
스크립트는 커밋되어 있고, 문서에는 실행법이 적혀 있고, 실제로는 한 번도 돌지 않는 상태가
버전 명시 하나가 없다는 이유로 무기한 유지될 수 있었다.

**처방**
- 적용 완료(런타임 고정): **Node 24 LTS 로 상향**하고 저장소에 못 박았다 —
  루트 `.nvmrc`(`24`) + `package.json` 의 `engines: { node: ">=22.6" }`.
  이제 버전 드리프트가 조용히 티어를 죽이는 대신 **설치 시점에 드러난다.**
  개발자는 저장소 어디서든 `nvm use` 한 번이면 맞는 런타임을 잡는다.
- 적용 완료(T1): `project/scripts/ts-hook.mjs` — 이미 devDependency 인 `typescript` 로 타입을 벗기고
  module hook 으로 `@/` 별칭·확장자 없는 상대 임포트를 해석한다. **새 의존성 없음.**
  Node 22.6+ 는 타입 스트리핑을 기본 제공하지만 **별칭·확장자 해석은 여전히 해주지 않으므로**
  훅은 계속 필요하다 — §9.1 의 지적사항을 실제로 푸는 것은 이쪽이다. `pnpm test:t1` 로 전체 실행.
  Node 20/24 양쪽에서 동작하므로 런타임을 또 옮겨도 티어가 죽지 않는다.
- 적용 완료(T2): `project/src/widgets/BlockEditor/__tests__/vertical_motion_t2.html` 하네스 + "실앱을 Chromium 에서 구동한다"는 경로 확립.
- **남음(T3)**: 붙였다 떼는 의식을 그만두고, **프로덕션 빌드에서 배제되는 상시 디버그 채널**을 만들 것.
  T3 가 일회성인 한 T3 에서 나온 결론은 회귀로 고정되지 않는다 — §9.6 의 T3 세션 결과가
  전부 롤백되어 아무것도 남지 않은 것이 그 증거다.

---

## A5. 게이트가 최종 증상만 관측한다

`§9.6` 은 T3 실기 한 세션을 통째로 쓰고도 원인에 도달하지 못했다. 이유는 관측량 선택에 있다.
그 세션은 **최종 증상(라인 델타)만** 보면서 가설 H1~H4 를 하나씩 지웠고, 전부 기각한 뒤
다음 후보로 keymap 조합과 orchestrator 전역 재계산을 지목했다 — **둘 다 오답이었다.**

이번에 같은 버그가 **한 번의 측정**으로 풀린 이유는 `drift`(실제 DOM top − height map top)라는
**원인에 한 단계 가까운 중간량**을 쟀기 때문이다. drift 를 재는 순간 가설 검증이 아니라 **원인 특정**이 된다.

계획서의 게이트는 거의 전부 최종 증상 단언이다 — "델타 ±1", "scrollTop 0px", "캐럿 정확".
이들은 **깨졌다는 것은 알려주지만 어디가 깨졌는지는 알려주지 않는다.**

**처방** — 각 게이트에 중간 불변식을 하나씩 붙인다.

| 최종 증상 게이트 | 붙여야 할 중간 불변식 |
|---|---|
| R10 수직 이동 델타 ±1 | `drift === 0` (적용 완료) |
| R3 병합 후 캐럿 유지 | `store.focusOffset === view.selection.main.head` ← 이것만 있었으면 즉시 잡혔다 |
| V1~V4 scrollTop 0px | 프레임 간 `block.top` 변화량 = 0 |
| G1 캐럿 안정성 | 뷰 인스턴스 동일성(리마운트 0회) |

---

## A6. 블록당 EditorView 설계가 자체 예산을 초과한다 — G3-1 실측

**T2(Chromium) 실측. WKWebView 는 이보다 느릴 가능성이 높으므로 하한으로 읽을 것.**

| N (블록) | 스토어 로직(순수 JS) | 마운트 완료까지 | 블록당 |
|---|---|---|---|
| 25 | 0.4 ms | 1,015 ms | 40.6 ms |
| 50 | 0.5 ms | 2,271 ms | 45.4 ms |
| 100 | 1.6 ms | 5,677 ms | 56.8 ms |
| **200** | **1.6 ms** | **7,024 ~ 17,423 ms** | **35 ~ 87 ms** |

계획서 예산은 **N=200 에서 300ms 이하**다. → **20~58배 초과. G3-1 실패.**
메모리는 블록당 42~98KB 로 예산(150KB) 내이므로, 문제는 **오직 시간**이다.

두 가지가 결정적이다.

1. **스토어 로직은 사실상 공짜다** (N=200 에서 1.6ms). 비용의 **거의 100%가 `EditorView` 생성**이다.
   즉 파싱·트리 구성 최적화는 아무 의미가 없다.
2. **블록당 비용이 N 에 따라 증가한다** (40ms → 87ms). 성장 지수 ≈ **1.37 로 초선형.**
   이는 각 인스턴스 생성이 **이미 마운트된 문서 전체에 비례하는 일**을 하고 있다는 뜻이다 —
   `EditorView` 생성자가 폰트/라인 높이를 재려고 레이아웃을 읽고, 그때마다 앞서 붙은 형제 전체가
   다시 레이아웃되는 전형적인 **레이아웃 스래싱** 형태다. (원인 귀속은 추정이며, 별도 프로파일 필요)

**판정**: `§6.2 G3-2` 의 규칙 "예산 초과 → **E7 가상화 즉시 착수**" 조건이 충족되었다.

다만 착수 전에 **한 가지를 먼저 측정할 것을 권한다.** 초선형 성분이 레이아웃 스래싱이라면,
가상화보다 훨씬 싼 수단(마운트 배치 처리, `content-visibility: auto`, 생성 중 측정 지연)으로
상당 부분이 회수될 수 있다. 선형으로만 되돌려도 N=200 이 8초 → 1초대가 된다.
가상화는 그 다음에 판단해도 늦지 않으며, **가상화는 공짜가 아니다** —
계획서가 이미 경고했듯 **높이 캐시 없는 가상화는 height map 불일치를 그대로 재생산한다.**
BUG-20260828-01 이 바로 그 불일치가 무엇을 만드는지 보여준 살아 있는 사례다.

---

## A7. 헤딩 블록의 정체성이 여전히 내용에서 파생된다

G0(BUG-20260826-08)은 **비헤딩** 블록을 위치 기반 키(`${parentId}-textblock-${index}`)로 바꿔 해결했다.
그러나 **헤딩 블록의 키는 아직 헤딩 라벨에서 파생된다**(`buildHeadingId(parsed.label, ...)`).

T1 로 확인:

```
'## 원래제목'  →  id x9937oh
'## 바뀐제목'  →  id wi2lrio      ← 제목만 고쳤는데 정체성이 바뀐다
(건드리지 않은 형제 H2 는 id 유지)
```

`project/src/widgets/BlockEditor/ui/BlockEditor.tsx` 의 렌더는 `key={rootBlock.id}` 다. 따라서 이 id 가 바뀌면
React 가 그 블록을 **언마운트 후 재마운트**하고, 정리 함수가 `view.destroy()` 를 호출한다.
G0 은 생성 이펙트의 **의존성 배열**에서 `block.id` 를 뺐지만, **React key 를 통한 파괴 경로는 그대로 열려 있다.**
즉 계획서 §3.2 가 경고한 피해("편집 중인 에디터가 사라짐 — 캐럿·undo 소실, IME 조합 강제 중단")의
경로가 헤딩에 한해 아직 살아 있다.

**완화 요인**(그래서 지금 당장 터지지는 않는다): 키 재파생은 헤딩 **개수**가 변할 때만 일어난다
(`handleBlockUpdate` 가 `oldHeadingCount !== newHeadingCount` 로 가드). 제목만 고치는 동안은 재파생이 없다.
**위험 창**은 "제목을 고친 뒤 어딘가에서 헤딩을 추가/삭제하거나 블록을 병합/분할하는 순간" 이며,
그때 그 블록의 undo 히스토리가 조용히 사라진다.

**처방**: 헤딩도 위치 기반 정체성으로 옮기고, 라벨은 **표시용 속성**으로만 둔다.
MindNode 링크가 헤딩 라벨 id 에 의존한다면(주석의 "Legacy support for MindNode linking"),
링크용 키와 **렌더 정체성 키를 분리**한다. 렌더 key 는 절대 내용에서 파생하지 않는다.

---

## 다음 사이클 권장 순서

1. **A7** — 헤딩 정체성 분리. 남아 있는 결함 중 유일하게 **데이터 손실(undo 히스토리)** 을 만든다.
2. **A6** — 먼저 레이아웃 스래싱을 프로파일하고, 그 다음에 가상화 여부를 판정한다.
   가상화에 착수하면 **높이 캐시 필수**이고 V1~V4 재실행 필수다.
3. **A5** — 게이트마다 중간 불변식 추가. 특히 개발 빌드의
   `store.focusOffset === view.selection.main.head` 단언은 비용이 거의 없고 회수가 크다.
4. **A4(T3)** — 상시 디버그 채널. 이것이 없으면 T3 결론은 계속 휘발된다.
5. **A1/A2 마무리** — 뷰↔스토어 조정자 일원화 + diff 기반 내용 동기화.
