# Devoras Design - Function Roadmap

## 🚀 개요 (Overview)
이 문서는 Devoras Design 프로젝트의 기능 구현 스텝과 현재 진행 위치를 보여주는 로드맵입니다.
현재 프로젝트는 **MVP Phase 2**의 주요 기능 구현을 완료했으며, 새로운 플랫폼(Claude Code)으로의 이전(Handover)을 준비 중인 상태입니다.

---

## 🎯 Phase 1: MVP 기초 및 구조 셋업 (✅ 완료)
- **[✅]** FSD (Feature-Sliced Design) 아키텍처 스캐폴딩
- **[✅]** Tauri 2 + React 18 + Vite 기반 데스크톱 앱 셋업
- **[✅]** H1/H2 블록 기반 마크다운 슬라이싱 (BlockEditor)
- **[✅]** 공간 좌표(spatial metadata) 및 인메모리 파서(`parser.ts`) 구현

## 🎯 Phase 2: 핵심 에디터 기능 및 UX 개선 (✅ 완료)
- **[✅]** 에디터 툴바 및 인라인 이미지 렌더링
- **[✅]** 읽기 모드(Read Mode) 및 쓰기 모드(Write Mode) 전환
- **[✅]** 폰트 사이즈 제어 및 동적 UI 반영
- **[✅]** Decorator Orchestrator 아키텍처 도입 (마크다운 구문 강조)
- **[✅]** 한글 IME 입력 간섭 해결 (3중 방어 래치 시스템)

## 🎯 Phase 3: 파일 시스템 및 워크스페이스 확장 (✅ 완료)
- **[✅]** 워크스페이스 열기 (`Cmd+O` 및 파일 메뉴)
- **[✅]** 트리 기반 파일 탐색기 및 폴더/파일 CRUD
- **[✅]** 파일 드래그 앤 드롭 이동 (Custom Pointer Events 기반 구현) 및 복사/붙여넣기
- **[✅]** ERD Designer (`@xyflow/react`) 네이티브 통합
- **[✅]** Splash Screen 도입 및 Tauri 초기 로드 화면 Deadlock 해소
- **[✅]** 단축키 UX 개선 (`Cmd+W` 탭 닫기 전용, `Cmd+Q` 앱 종료 분리, `Cmd+S` 애니메이션)

## 📍 현재 위치 (You are here)

> **출시 준비 단계 (현재 버전: 0.9.1) — 아키텍처 게이트는 동결됐다**
>
> **2026-09-04 결정 (`PM-20260904-01`, 사용자 승인)**: 8-D 단일 CM 전환 게이트를 **동결**하고
> 진행을 **출시 레인**으로 옮긴다. 조사 대상(`SingleDocEditor`)이 `DEFAULT_ENABLED = false` 인
> **출시되지 않은 경로**이고, 재현 입력이 팀 계측용 픽스처(1,013행·헤딩 201개)라 실사용을
> 대표하지 않으며, 측정 환경이 그 질문에 답할 수 없다는 것이 이미 두 번 기록됐기 때문이다.
> **산출물은 폐기하지 않는다** — 플래그 off 로 보존하고 출시 후 재개 판단한다.
> 상세: [`ticket/project/20260904_1800_release_lane_hold.yml`](ticket/project/20260904_1800_release_lane_hold.yml)
>
> ### 0.9.x 출시 스코프 (확정 순서)
>
> | # | 항목 | 티켓 | 왜 이 순서인가 |
> |---|---|---|---|
> | 1 | **ERD 빈 문서 덮어쓰기 차단** | `BUG-20260902-01` | P1 데이터 유실. 2 번의 전제 |
> | 2 | **편집 중 `EditorView` 재마운트 + `setContent` 이중 호출** | 미발행 (S1·S2) | 출시 경로 실결함. 국소 수정 |
> | 3 | **자동 저장 off/low/high 배선** (기본 `high`) | `FEAT-20260904-01` | 지금은 슬라이더에 **소비자가 없다**. 기본값이 켜져 있으므로 **1 번이 하드 게이트**다 |
> | 4 | **출시 엔지니어링** (Releases·CI) | `REL-20260904-01` | **이게 없으면 기능이 몇 개든 출시가 성립하지 않는다.** 서명·Homebrew 는 계정 확인까지 보류 |
> | 5 | **릴리스 번들 실기 검증** | `VERIFY_BY_HUMAN.md` §1·§2·§4 | §1 에 「빌드 앱 첫 실행 시 이미지 미렌더링」 **미재현 보고**가 있다 |
> | 6 | **R5-a 토큰 마이그레이션 + 다크/라이트 모드** | Sprint 3 (R-6 · `erd.css` 중복 흡수) | 남은 Phase 3.5 중 사용자 눈에 가장 크게 보인다. **YAML 파서는 불필요** |
> | 7 | **R6 워크스페이스 전역 검색** (**JS 우선**, Rust 는 실측 후) | — | 노트 앱 기본 기대치 |
> | 8 | **P1-9 위젯 타이포그래피** · 스플릿 뷰 세로 분할(진입점 1개) · 탭 드래그 재정렬 | — | 마무리 소품 |
>
> ### 실측으로 정정된 것 (2026-09-04, 코드 대조)
>
> - **마운트 비용은 문제가 아니다** — N=200 에서 10.9ms, 예산 300ms 대비 **27배 여유**(`DEBUG_PLAN.md` §5.11).
>   게이트를 연 A6 위기(7,024~17,423ms)가 실측에서 재현되지 않았다.
> - **Sprint 3 의 절반은 이미 끝났다** — Base64 이미지 폐기 → Tauri Asset Protocol 은 이미 적용돼 있다
>   (`ImageDecorator.ts:67`·`ReadView.tsx:121` 양쪽 `convertFileSrc`). 나머지 절반(다중 CM 통합/가상화)이
>   곧 동결된 8-D 이므로 **Sprint 3 는 출시 스코프에서 제외한다.**
> - **다중 탭·스플릿 뷰(가로)는 이미 동작한다** — `store.ts:429` `splitPane` + `WorkspacePage.tsx:329` 배선됨.
>   **세로 분할만 잔여**(store 는 지원, UI 가 `'horizontal'` 하드코딩).
>
> ### 출시 후로 미룬 것
>
> - **8-D 단일 CM 전환** — 동결. 남은 명분 4건(IME 블록 경계 · 헤딩 배지 · 문서 단위 undo · 편집 중 뷰 폭풍)은
>   전부 실재하지만 출시 차단이 아니고, 각각 더 싼 독립 처방이 있다.
> - **탭 Tear-off · 퀵 캡처** (Phase 6) — Multi-Window IPC 를 공유하므로 **함께 설계**한다.
> - **R5-b YAML 커스텀 테마** — R5-a 가 세운 토큰 계층 위에 얹는 일이라 나중에 해도 R5-a 가 매몰되지 않는다.
> - **Phase 4 3계층 하이브리드** (Rust 이관 · OffscreenCanvas) — **R6 전역 검색을 Rust 로 지으면 이걸 출시 앞으로 당기는 것이 된다.** JS 우선으로 확정.
>
> ### 디버그–구현 조율 규칙 (2026-09-04, 상시)
>
> 디버그를 끄지 않는다 — **출시 스코프에 귀속시킨다.** 전문은 `PM-20260904-01` §coordination_rule.
>
> | | 규칙 |
> |---|---|
> | **R0** | 모든 디버그 작업은 **출시 스코프 항목 1개에 귀속**되어야 착수한다. 귀속 없는 발견은 티켓 적재만 |
> | **R1** | 한 가설당 판별 실험 **2라운드 상한.** 3라운드가 필요하면 PM 판정 |
> | **R1-b** | **1라운드 착수 전에 관측 조건을 확정·기록한다**(플래그 상태·빌드 모드·페인 가시성·워크스페이스 규모). 조건이 안 적힌 관측은 라운드를 소비할 자격이 없다 |
> | **R2** | 「이 환경에서 못 잰다」가 확인된 축은 **재시도하지 않는다** → `VERIFY_BY_HUMAN.md` 로 |
> | **R3** | 구현 세션이 점유한 파일을 디버그가 동시에 만지지 않는다. 경로 지정 커밋 |
> | **R4** | 우선순위: **데이터 유실 > 출시 차단 > 사용자 가시 결함 > 내부 품질 > 미출시 경로** |
> | **R5** | 디버그가 구현을 막으면 **구현이 이긴다.** PM 에 보고 후 큐로 |
> | **R6** | 디버그 세션의 산출물은 **결함 티켓 또는 판정**이다. 새 가설 문서가 아니다 |
>
> **버전 규약은 그대로다** (`.agents/AGENTS.md` §2): 코드 커밋 = 패치 버전업 4파일 동시,
> 문서 전용은 버전 미상승, 코드가 한 줄이라도 섞이면 예외 아님. **1.0 승격은 이번 스코프에서 판단하지 않는다.**

## 🏃 Phase 3.5: 핵심 UX 확장 및 아키텍처 재정비 (진행 중 — **전량 0.9.x 출시 스코프**)
*(기존 기획서 및 긴급 성능 최적화)*
- **[x]** 시작 시 폴더 선택 런처 (최근 워크스페이스 목록, 즐겨찾기 핀 고정) — *참조: `functions/8_launcher_on_startup.md`*
- **[x]** 앱 설정 시스템 (폰트, 자동 저장, 줄 바꿈, 마인드맵 노드 스타일 등 영구 저장 및 실시간 핫 갱신) — *참조: `functions/5_app_settings.md`*
- **[~]** ~~**[New Sprint 3] 프론트엔드 렌더링 아키텍처 최적화**~~ — **항목 해체 (2026-09-04)**
  - **Base64 이미지 폐기 → Tauri Asset Protocol: ✅ 완료** (`ImageDecorator.ts:67` · `ReadView.tsx:121`)
  - **다중 CodeMirror 통합/가상화: 동결** — 이것이 곧 8-D 게이트다(`PM-20260904-01`). 마운트 비용은 예산 대비 27배 여유로 실측 통과했다
  - 대체 항목: S1·S2 — `handleBlockUpdate` 디바운스 + `setContent` 이중 호출 제거 (출시 경로 국소 수정)
- **[ ]** YAML 기반 커스텀 테마 및 다크/라이트 모드 전환 (실시간 핫 리로드) — **2026-09-05 분할: R5-a(토큰 이관+다크/라이트, 출시 스코프) / R5-b(YAML, 출시 후).** 사양서 전제(`index.css` 의 HSL 변수)가 코드에 없다 — 토큰 **0개**, 하드코딩 hex **33개**. 이관 총량 = tailwind 6 + `index.css` 33 + `erd.css` 7(+변수 9) + TSX/TS 14. 「개편」이 아니라 **신설**이다 — *참조: `functions/6_theme_yaml_custom.md`* · **R-6(`BUG-20260831-02`, StyleModule 누수) 병합됨** — CSS 변수 토큰 인젝터가 `createEditorTheme()` 를 상수화하면 누수와 `BlockEditor.tsx:144` reconfigure 경로가 함께 소멸한다 (사용자 결정 D-4, 2026-09-02)
- **[ ]** 워크스페이스 전역 검색 (파일명 + 본문 실시간 검색, 결과 하이라이팅, 점프 네비게이션) — **JS 우선 확정(D9).** `implementation_plan.md` §4A.3 은 Rust 를 기본 설계로 적었으나 그건 Phase 4 를 출시 앞으로 당기는 것이다 — *참조: `functions/4_global_search.md`*
- **[~]** 다중 파일 탭 지원 및 에디터/마인드맵 스플릿 뷰 — **다중 탭·가로 분할 완료.** 세로 분할은 **미구현이 아니라 미노출**(스토어·레이아웃·테두리 분기 전부 존재, `WorkspacePage.tsx:329` 버튼이 `'horizontal'` 하드코딩) → **진입점 1개**. 탭 드래그 재정렬만 실작업 — *참조: `functions/3_tabs_and_split_view.md`*

## 🏃 Phase 4: 성능 최적화 및 렌더링 아키텍처 (⏳ 예정)
*(3계층 하이브리드 아키텍처 기반. 상세 구현 스텝은 `architecture_stages.md` 참조)*
- **[ ]** Tier 1: Rust 백엔드 상태 이관 (AST 파서, 그래프 연산, 파일 인덱싱)
- **[ ]** Tier 2: 빈번한 뷰(에디터/마인드맵/ERD) OffscreenCanvas 전환 및 RAF Pause/Resume
- **[ ]** Tier 3: GPU 집약 뷰(3D 아키텍처) Lazy Multi-Window 격리
- **[ ]** `documentStore` 전역 상태 충돌 방지를 위한 탭별 로컬 캐시 도입 (Critical)

## 🏃 Phase 5: 리팩토링 및 기술 부채 상환 (⏳ 예정)
*(상세 내용은 `code_review.md` 참조)*
- **[ ]** `BlockEditor` 입력 시 O(N) 연산 병목 최적화
- **[ ]** `ReadView` 중첩 블록 포맷팅 버그(`applyFormat` 트리 순회) 수정
- **[ ]** 파일 시스템 스코프 전역 키보드 이벤트 간섭 해결

## 🏃 Phase 6: 고급 뷰 및 사용자 정의 기능 (⏳ 예정)
- **[ ]** 마인드 뷰 캔버스 미니맵 및 자유형 노드 추가
- **[ ]** 마인드 뷰 중첩 컨테이너(Nested Box) 모드 — H1 루트 계층 파싱 기반 아키텍처/모듈 포섭 뷰 — *참조: `functions/9_mind_view_nested_container.md`*
- **[ ]** 아키텍처 뷰용 3D Freeform 캔버스 도입
  - *유즈케이스 시각화*: 흐름에 따라 레이어 간 노드 외곽선 및 연결선(Edge) 순차 하이라이팅 기능
- **[ ]** 탭 드래그 분리(Tear-off) 및 독립 서브 윈도우 생성 (Tauri Multi-Window IPC 동기화) — *참조: `functions/7_tab_tearoff_new_window.md`*
- **[ ]** **퀵 캡처 — 단축키 호출 상시 최상위(always-on-top) 입력 모달**
  - *동기*: 메모 앱은 **마찰이 곧 사용 빈도**를 결정한다. 타이핑에만 집중된 가벼운 인터페이스는 자주·부담 없이 적게 되지만, 기능과 UI 가 두꺼운 앱은 "열어서 쓰는 것" 자체가 일이 된다. 본체 워크스페이스를 띄우지 않고도 떠오른 것을 즉시 적을 수 있는 **최소 표면**을 별도로 둔다.
  - 전역 단축키 한 번으로 어느 앱 위에서든 뜨는 **작고 상시 최상위인 창**. 파일 탐색기·탭 바·툴바 없이 **입력 영역만** 둔다(ChatGPT·Gemini 데스크톱 앱의 퀵 엔트리와 같은 성격).
  - 적고 나면 단축키/Esc 로 즉시 사라져 원래 하던 일로 돌아간다. **"열기 → 찾기 → 적기"가 아니라 "적기" 한 단계로 줄이는 것이 목적이다.**
  - *저장 위치(확정, 2026-08-31)*: **`.devoras/tmp/quick/`** (워크스페이스 내 Devoras 콘텐츠 디렉터리). 없으면 생성하고 파일명은 **`note_[datetime].md`**.
    - **datetime 은 초 단위까지 쓸 것**(`note_YYYYMMDD_HHMMSS.md`). 짧은 간격의 연속 사용이 전제인 기능이라 분 단위로는 같은 분 안에서 충돌한다.
    - 쓰기는 워크스페이스 루트 안쪽이므로 L3 `ensure_inside`(`src-tauri/src/lib.rs:33`)를 통과한다. Step 1-A scope 축소와 충돌 없음. `requireLiteralLeadingDot: false` 가 유지돼 있어야 `.devoras/` 접근이 성립한다.
  - *수명(TTL) — 48시간*: 이 디렉터리의 노트는 **임시**다. 48시간이 지나면 만료 대상으로 본다.
    - **런처 진입 시 또는 앱 알림으로 임시 노트의 존재를 알리고**, 정식 위치로 옮기도록 권한다. 알림에는 **만료 시각(expiration datetime)을 함께 표기**한다.
    - TTL 은 "적어두고 잊는" 것을 막기 위한 장치다 — 캡처는 쉽게, 정리는 강제되게.
  - *세션 이어쓰기 — 기본 10분(설정에서 변경 가능)*: 마지막 캡처로부터 일정 시간 안에 다시 호출하면 **직전 tmp 노트에 이어서 쓴다.** 생각이 이어지는 동안 파일이 쪼개지는 것을 막는다. 시간이 지나면 새 파일을 만든다.
  - *착수 전 확정 필요 — 미해결*:
    - **워크스페이스 미선택 상태**로 호출되면 저장할 루트가 없다(`lib.rs:110` 에서 거부). 런처를 거치지 않는 유일한 진입 경로이므로 반드시 정해야 한다 — 마지막 워크스페이스 자동 복원 / 저장 시점에 선택 요구 / 캡처 자체를 막기.
    - **만료된 노트를 어떻게 할 것인가.** 자동 삭제인지, 만료 표시만 하고 남기는지, 별도 보관함으로 옮기는지. 사용자가 적은 내용을 앱이 지우는 것은 되돌릴 수 없으므로 **자동 삭제라면 경고와 유예가 필요하다.**
    - **TTL 기준 시각**: 생성 시각인가 마지막 수정 시각인가. 이어쓰기가 있으므로 둘이 달라진다.
    - **알림 수단**: 런처 배너만인가, OS 알림도 쓰는가. OS 알림은 `tauri-plugin-notification` 추가가 필요하다(미도입).
    - **이어쓰기 판정 기준**: 마지막 캡처 시각 기준인가 파일의 mtime 기준인가. 사용자가 그 파일을 에디터에서 직접 편집한 경우도 이어쓰기 대상인지.
  - *기술 전제조건*: `tauri-plugin-global-shortcut` **미도입 — 신규 추가 필요**. 항상 최상위 창 플래그(`alwaysOnTop`)도 현재 `tauri.conf.json` 에 설정되어 있지 않다.
  - *연계*: 위 **탭 Tear-off 항목과 Tauri Multi-Window IPC 인프라를 공유**한다. 둘 중 먼저 착수하는 쪽이 창 생성·IPC 동기화 기반을 세우므로 **함께 설계하면 중복 구현을 피할 수 있다.**
  - *주의*: 본체 앱을 띄우지 않고 뜨는 창이므로 **워크스페이스 미선택 상태**를 정면으로 다뤄야 한다(런처를 거치지 않는 유일한 진입 경로가 된다).
- **[ ]** 다단 레이아웃 (Multi-column) 병렬 블록 파싱 (`|| parallel-left`)
- **[ ]** 명령어 시스템 (Slash Command 기반 `\:` 명령어 자동완성 팝업)
- **[ ]** 확장 마크다운 이미지 문법 (`![alt](url || left 300px)` 크기 및 정렬 제어)
- **[ ]** 이미지 상세 뷰어 (사이드 탭에서 이미지 줌인/줌아웃 및 패닝 지원) — *참조: `functions/image_side_view.pdf`*
- **[ ]** 애플리케이션 사용자 설정 파일(`.devoras/settings.json`) 저장 연동
- **[ ]** 단일 블록 포커스 에디팅 (Zen Mode)
- **[~]** 인라인 스마트 커스텀 심볼 (Inline Smart Custom Symbols)
  - *선구현 완료*: 내장 화살표 심볼 `->` `=>` 를 Read/Write Mode 양쪽에서 인라인 SVG 로 렌더링.
    심볼 정의는 `shared/lib/markdown/customSymbols.ts` 레지스트리가 단독 소유하며,
    `registerCustomSymbols()` 로 사용자 정의 심볼을 주입할 수 있게 열어 두었다.
  - 사용자가 심볼의 특성·기능·의미를 직접 정의하고, 에디터 내에서 미니멀한 위젯(뱃지)으로 렌더링
  - 커서를 일정 시간 호버링하면 정의된 의미가 툴팁으로 표시 (잊어버림 방지)
  - *구현 전제조건:* (1) 인터랙티브 상태 변경 가능 (클릭 시 토글 등), (2) 워크스페이스 전역 쿼리 시스템(PKM 메타데이터 Aggregation)이 준비될 때 재진행
  - *참조:* `functions/custom_symbol.pdf`

