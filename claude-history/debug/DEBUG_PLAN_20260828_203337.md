# DEBUG_PLAN.md — 0.8.11 검증 게이트 완수 (G0~G3)

> **대상**: `project/` (Devoras `v0.8.11`)
> **선행 작업**: `f079941` — 편집 표면 상시화(E1~E4) **구현 완료, 검증 게이트 3건 미실행**
> **직전 계획서 아카이브**: [`claude-history/debug/DEBUG_PLAN_20260827_044054.md`](../claude-history/debug/DEBUG_PLAN_20260827_044054.md) §9 완료 기록
> **경로 표기**: 저장소 루트 기준 / 링크는 `project/` 기준
> **성격**: **신규 결함 조사가 아니라 검증 부채 상환.** 단, §3 G0 은 검증 준비 중 발견된 **신규 실행 위험**이며 최우선이다.

---

## 0. 이 문서의 범위

`f079941`(0.8.11)은 아키텍처 전환을 완료했고 핵심 불변식(*클릭 경로에서 React state 미변경*)도 정적으로 확인되었다. 그러나 계획이 **게이트로 지정한** 검증 3건이 실행되지 않았다.

| 게이트 | 원 계획 | 실제 | 본 문서 |
|---|---|---|---|
| E0 인스턴스 비용 실측 | 선행 필수 | 미실행 (하네스 실행 불가) | **G3** |
| E5 캐럿 안정성 | E4 의 게이트 | 하네스 부재 | **G1** |
| §6 런타임 V/T/R | 배치 종료 조건 | 미실행 | **G2** |
| E7 가상화 | E0 결과에 종속 | 미착수 | **G3-2** |

추가로, 검증 설계 중 **`f079941` 이 활성화시킨 신규 위험**을 발견했다 → **G0**.

---

## 1. 왜 스킵되었는가 — 검증 티어 오선택

게으름이 아니라 **구조적 오류**였다. [`e0_harness.cjs`](src/widgets/BlockEditor/__tests__/e0_harness.cjs) 는 `jsdom` 위에서 `EditorView` 를 N개 만들어 마운트 시간과 힙을 잰다. 그러나

> **jsdom 에는 레이아웃 엔진이 없다.**

`EditorView` 를 jsdom 에 붙이면 DOM 트리는 만들어지지만 **리플로우·텍스트 측정·렌더 트리 구축이 전혀 일어나지 않는다.** 실제 WKWebView 비용의 대부분이 바로 그 부분이다. 즉 이 하네스는 **실행 가능해지더라도 측정값이 무의미**하다. (게다가 `jsdom` 은 `package.json` 에 없어 애초에 실행되지 않았다.)

E5 도 같은 함정에 빠질 뻔했다. 캐럿 좌표와 라인 높이는 **레이아웃의 산물**이므로 기존 하네스 관례(`node --experimental-strip-types`)로는 **원리적으로 측정할 수 없다.** [`ime_isolation_harness.ts`](src/widgets/BlockEditor/__tests__/ime_isolation_harness.ts) 가 그 관례로 성립하는 이유는 그것이 **순수 로직만** 검사하기 때문이다.

**교훈**: 하네스를 쓰기 전에 **"이 질문의 답에 레이아웃이 관여하는가"** 를 먼저 물어야 한다.

---

## 2. 검증 티어 모델 (본 문서의 핵심 규약)

| 티어 | 실행 환경 | 측정 가능 | 측정 **불가** |
|---|---|---|---|
| **T0** jsdom | — | (없음) | 레이아웃 일체. **신규 사용 금지** |
| **T1** Node | `node --experimental-strip-types` | 순수 로직·오프셋 매핑·상태 전이 | 레이아웃 일체 |
| **T2** Chromium | `pnpm dev` + `playwright` MCP | 폰트 크기, 라인/요소 높이, 캐럿 좌표, 명시도 승패 | **스크롤 앵커링 거동** (Chromium 이 자동 보정해 증상을 가림) |
| **T3** WKWebView | `pnpm tauri:dev` + `tauri` MCP (`webview_execute_js` / `webview_get_styles`) | **전부** — 실제 배포 엔진 | — |

**배정 규칙 (위반 금지)**

1. 답에 **레이아웃이 관여하면 T0/T1 을 쓰지 않는다.**
2. 답에 **스크롤 위치가 관여하면 T3 가 필수다.** T2 통과는 근거가 되지 못한다.
3. T2 는 **빠른 반복용**이다. 타이포그래피 수치처럼 엔진 이식성이 높은 항목은 T2 에서 조율하고 **T3 에서 1회 확인**한다.
4. 순수 로직은 **T1 을 유지한다.** 브라우저로 올리지 말 것 — 느려지고 관례가 깨진다.

> `.mcp.json` 에 `tauri` · `playwright` MCP 서버가 이미 등록되어 있다. **착수 전 연결 상태를 먼저 확인**하고, 불가하면 Tauri devtools 콘솔에 스크립트를 직접 붙여넣는 수동 절차로 대체한다(측정 항목은 동일).

---

## 3. G0 — `block.id` 변동에 의한 EditorView 파괴 (**신규 · 최우선**)

### 3.1 무엇을 발견했나

`f079941` 은 CodeMirror 생성 이펙트의 의존성을 다음과 같이 바꿨다.

```ts
}, [paneId, block.id]);   // ← 이전: []
```

따라서 **`block.id` 가 바뀌면 `EditorView` 가 파괴되고 재생성된다** ([`BlockEditor.tsx:229-236`](src/widgets/BlockEditor/ui/BlockEditor.tsx#L229)).

그런데 `block.id` 는 안정적이지 않다. [`store.ts:48`](src/entities/block/model/store.ts#L48) 의

```ts
if (!parsed) return firstLine;  // 비헤딩 블록 — 원본 첫 줄을 키로 사용
```

때문에 **비헤딩 블록의 첫 줄을 편집하면 키가 바뀌고**, `setBlocksFromContent` 의 `existingByKey.get(key)` 가 실패해 `generateId()` 로 **새 id 가 발급**된다.

`setBlocksFromContent` 는 헤딩 개수가 변할 때 `handleBlockUpdate` 에서 호출된다([`BlockEditor.tsx:471`](src/widgets/BlockEditor/ui/BlockEditor.tsx#L471) 부근).

### 3.2 왜 지금 심각해졌나

이것은 **기존 티켓 [BUG-20260826-08](../ticket/debug/20260826_1740_block_key_collision.yml)**(비헤딩 블록 키 충돌)이며 **아직 열려 있다.** 그러나 결과의 무게가 달라졌다.

| | 0.8.10 이전 | **0.8.11 이후** |
|---|---|---|
| 의존성 배열 | `[]` — id 변동이 리마운트를 유발하지 않음 | `[paneId, block.id]` — **id 변동 = 즉시 파괴/재생성** |
| 리마운트 빈도 | 포커스 전환마다 어차피 발생(가려짐) | **타이핑 도중 발생** |
| 피해 | 커서 유실 | **편집 중인 에디터가 사라짐** — 캐럿·undo 히스토리 소실, **IME 조합 강제 중단** |

> ⚠️ **G1(캐럿)과 R1(IME)의 재현 조건과 정확히 겹친다.** G0 을 먼저 처리하지 않으면 **G1·G2 에서 관측되는 실패가 타이포그래피 탓인지 리마운트 탓인지 구분할 수 없다.**

### 3.3 조치

1. **T1 하네스**: `project/src/entities/block/__tests__/block_key_stability_harness.ts`
   - K1: 비헤딩 블록의 첫 줄을 수정해도 id 가 보존되는가
   - K2: 동일한 첫 줄을 가진 비헤딩 블록 2개가 서로 다른 id 를 갖는가 (원 티켓의 충돌 사례)
   - K3: 헤딩 분할·병합 후 **편집 중이던 블록**의 id 가 보존되는가
2. **수정 방향**: 비헤딩 블록 키를 **내용이 아니라 위치 기반**(부모 키 + 형제 인덱스)으로 도출한다. BUG-20260826-08 의 원 처방과 동일하다.
3. **보강**: 위 수정 후에도 id 변동이 남을 수 있으므로, 생성 이펙트에서 **`block.id` 의존을 제거**하고 레지스트리 키 갱신만 별도 이펙트로 처리하는 방안을 함께 검토한다. *뷰를 파괴하지 않고 등록만 옮기는 것*이 목표다.

**게이트**: K1~K3 통과 전에는 G1 에 착수하지 않는다.

---

## 4. G1 — 캐럿 안정성 (구 E5) · **T2 → T3**

`f079941` 은 `.cm-line *` 를 다음으로 축소했다([`BlockEditor.tsx:90`](src/widgets/BlockEditor/ui/BlockEditor.tsx#L90)).

```ts
'.cm-line > span:not([class*="cm-"])': { fontSize: 'inherit', lineHeight: 'inherit', ... }
```

의도는 "CodeMirror 하이라이트 토큰만 겨냥"이다. 그러나 그 결과 **`cm-` 접두 위젯이 리셋 대상에서 제외**되었고, 이는 `ce6b96c`(0.6.1)이 억제했던 **바로 그 경로**다. 검증 없이 배포되었다.

### 4.1 하네스

`project/src/widgets/BlockEditor/__tests__/heading_caret_harness.ts` — **T2 (Chromium)**

> **필수 조건**: `editorThemeCompartment` 앱 테마와 `decorationBaseTheme` 를 **반드시 포함**할 것. 0.8.8 이 테마 미포함 하네스로 "검증 완료"를 보고했고 그 결과가 BUG-20260826-12 였다.

| # | 시나리오 | 측정 | 기준 |
|---|---|---|---|
| **C1** | 평문 라인 맨 앞에서 `# ` 입력 | 입력 전후 `view.state.selection.main.head` | 입력 문자 수만큼만 이동. **예기치 않은 점프 0** |
| **C2** | 동일 시나리오 | `view.coordsAtPos(head)` 의 뷰포트 Y | 변화량 ≤ 해당 라인의 높이 증가분. **그 이상이면 실패** |
| **C3** | 동일 시나리오 | 스크롤 컨테이너 `scrollTop` | **변화 0** |
| **C4** | `## ` → `# ` 승격, `#` 삭제 → 평문 강등 | C1~C3 반복 | 동일 |
| **C5** | 헤딩 라인에서 10자 연속 타이핑 | 매 키 입력 후 캐럿 좌표 | **진동(oscillation) 0** — 좌표가 왕복하지 않을 것 |
| **C6** | H1~H6 · 평문 각 라인 | `getBoundingClientRect()` | 설계값 일치 **및 `height > 0`** |

> **C6 의 `height > 0` 을 생략하지 말 것.** 아카이브된 계획의 "후보 B 함정" — `fontSize` 만 보면 정상이나 `line-height: 0` 이 상속되어 높이가 0 인 사례가 실재했다.

### 4.2 실패 시

C1~C5 중 하나라도 실패하면 **임의로 헤딩 크기를 조정하지 말 것.** 아카이브 계획 §5 E6 의 폴백(헤딩 크기 축소)은 **사용자 승인 사항**이다. 실패 내용과 함께 보고하고 지시를 기다린다.

---

## 5. G2 — 런타임 검증 V/T/R

아카이브 계획 §6 의 전 항목. **티어를 명시**한다.

### 5.1 스크롤 거동 (V) — **T3 필수**

| # | 시나리오 | 기준 | 티어 |
|---|---|---|---|
| V1 | 뷰포트 중앙의 다른 블록 클릭 | `scrollTop` 변화 **0px** | T3 |
| V2 | 상단/하단 가장자리 블록 클릭 | 클릭 요소 `rect.top` 변화 **0px** | T3 |
| V3 | 이미지 포함 블록 ↔ 다른 블록 왕복 | 이미지 로드 완료 후에도 `rect.top` 불변 | T3 |
| V4 | 코드펜스 블록 왕복 10회 | 누적 드리프트 **0px** | T3 |
| V5 | 클릭 지점 캐럿 정확도 | 클릭한 **문자 위치**에 캐럿 | T2 가능, T3 확인 |
| V6 | 좌우 분할 2패널 각각 클릭 | 반대 패널 `scrollTop` 불변 | T3 |

> **기준이 "0px" 인 이유**: 0.8.11 은 보정이 아니라 **제거**다. 오차가 존재할 구조적 이유가 없다. 0 이 아니면 어딘가에 스왑이나 리마운트(→ G0)가 남아 있다는 뜻이다. **근사치로 통과시키지 말 것.**

### 5.2 타이포그래피 (T) — **T2 조율 → T3 확인**

| # | 항목 | 기준 |
|---|---|---|
| T1 | Write Mode H1/H2/H3 | 설계값, `height > 0` |
| T2 | Read ↔ Write 전환 | 헤딩 크기 **동일** (`orchestrator.ts` ↔ `.rv-content` 값 정합은 정적 확인 완료, 실측으로 재확인) |
| T3 | 코드블록 헤더 언어/타이틀/`Copied!` | **BUG-20260826-12 회귀 0건** |
| T4 | `.cm-heading-badge` · `.cm-image-caption` | 설계값 |
| T5 | `.cm-inline-code` | 0.88em 유지 |
| T6 | G1 의 C1~C6 | 전항 통과 |

### 5.3 회귀 (R)

| # | 항목 | 기준 | 티어 |
|---|---|---|---|
| **R1** | **한글 IME 연속 입력** | 조합 끊김·증식 **0건**. **N 인스턴스 구조에서 재검증 필수** | **T3** (IME 는 엔진 의존) |
| R2 | `ArrowUp`/`ArrowDown` 블록 간 이동 | 정상. 프로그래밍적 포커스 경로 생존 | T3 |
| R3 | `Backspace` 블록 병합 | 캐럿 위치 유지 (20260719 티켓 회귀) | T3 |
| R4 | 헤딩 입력으로 블록 분할 | 분할 후 캐럿 정상. **G0 과 직결** | T3 |
| R5 | `FormatToolbar` | 레지스트리 전환 후 활성 뷰 정확 | T2 |
| R6 | Read Mode 렌더링 | `rv-content` 경로 무손상 | T2 |
| R7 | `.devoras/images` 이미지 | Read/Write 양쪽 렌더링 | T3 |
| R8 | 탭 3개 전환 왕복 | 내용 유지, 교차 오염 0건 | T2 |
| R9 | 대형 문서(200+ 블록) 열기 | G3 예산 내 | T3 |
| **R10** | **데코레이션 라인 *내부* 수직 이동**(블록쿼트·리스트·코드블록) | 캐럿 라인 델타 항상 ±1. 줄 건너뜀 **0건**. **BUG-20260828-01 재현 경로** | **T2** ✅ (§9.7 — 원인 확정·수정·검증 완료. T3 1회 확인만 남음) |

> **R1 을 "이전에 통과했으니 괜찮다"로 처리하지 말 것.** [`ime_isolation_harness.ts`](src/widgets/BlockEditor/__tests__/ime_isolation_harness.ts) 는 **T1 순수 로직 하네스**라 다중 인스턴스·리마운트 상황을 **구조적으로 탐지할 수 없다.** G0 이 IME 조합을 끊을 수 있으므로 실기기 확인이 필수다.

---

## 6. G3 — 인스턴스 비용 실측 및 가상화 결정

### 6.1 G3-1: E0 하네스 재작성 (**T3**)

[`e0_harness.cjs`](src/widgets/BlockEditor/__tests__/e0_harness.cjs) 를 **삭제**하고 실기기 계측으로 대체한다. `jsdom` 을 의존성에 추가하지 말 것 — §1 의 이유로 무의미하다.

**방법**: `pnpm tauri:dev` 로 블록 N개 문서를 열고, devtools 또는 `tauri` MCP `webview_execute_js` 로 계측한다.

| 항목 | 계측 | 예산 |
|---|---|---|
| 초기화 시간 | 문서 열기 → 마지막 블록 마운트까지 (`performance.mark`) | N=200 에서 **300ms 이하** |
| 메모리 | `performance.memory.usedJSHeapSize` 마운트 전후 차 | 블록당 **150KB 이하** |
| 입력 지연 | 마운트 후 타이핑 프레임 시간 | **16ms 이하** |

측정 문서는 N=50 / 200 / 500 세 종을 고정 생성해 재현 가능하게 둔다.

### 6.2 G3-2: 가상화 착수 판정

| 결과 | 조치 |
|---|---|
| 전 항목 예산 내 | E7 가상화 **보류**. `advanced_rendering_optimization.md` Phase 1.5 로 유지 |
| 예산 초과 | **E7 즉시 착수.** §5 E7 의 **높이 캐시 + 플레이스홀더 필수** 조건 준수 |
| 블록당 500KB 초과 등 심각 | **중단 후 사용자 보고.** Option A 재검토 여부는 사용자 결정 |

> ⚠️ 가상화를 착수한다면 **V1~V4 를 반드시 재실행**할 것. 높이 캐시 없는 가상화는 **본 결함을 그대로 재생산**한다.

---

## 7. 실행 순서 & 게이트

```
G0 (block.id 안정화)  ── K1~K3 통과 ──┐
                                      ├──> G1 (캐럿 C1~C6) ──> G2 (V/T/R) ──> G3 (비용/가상화)
                                      │
              ※ G0 미통과 시 G1 착수 금지
```

| 순서 | 작업 | 게이트 | 커밋 |
|---|---|---|---|
| 1 | **G0** 키 안정화 + T1 하네스 | K1~K3 | `fix(0.8.12): 비헤딩 블록 키 안정화 (BUG-20260826-08)` |
| 2 | **G1** 캐럿 하네스 작성·실행 | C1~C6 | `test(0.8.12): 헤딩 캐럿 안정성 하네스` |
| 3 | G1 실패 시 수정 | C1~C6 재통과 | 별도 |
| 4 | **G2** 런타임 V/T/R | 전항 | 수정 발생 시에만 |
| 5 | **G3-1** E0 재계측 | 예산 판정 | `chore(0.8.12): E0 실측 하네스 교체` |
| 6 | **G3-2** 가상화 | V1~V4 재통과 | 착수 시 별도 |

**커밋을 합치지 말 것.** 0.8.11 이 E1~E4 를 단일 커밋으로 묶어 이분 탐색을 잃은 상태다. 같은 실수를 반복하면 G0·G1 중 어느 쪽이 원인인지 판별할 수 없다.

`.agents/AGENTS.md` 에 따라 커밋 전 패치 버전업 및 `package.json` / `Cargo.toml` / `tauri.conf.json`(+`Cargo.lock`) 동시 갱신.

---

## 8. 완료 조건 — **전 항목 종결 (2026-08-28 20:33)**

> 본 문서는 이 절의 모든 항목이 종결되어 아카이브한다.
> 미완으로 남은 항목은 **왜 이 계획서의 범위에서 종결시키는지**를 함께 적었다.
> 후속 작업은 [`ARCHITECTURE_FINDINGS.md`](../../ARCHITECTURE_FINDINGS.md) 가 이어받는다.

| 게이트 | 결과 | 근거 |
|---|---|---|
| **K1~K3** (G0) | ✅ **통과, 실행 가능** | §9.1 의 "커밋된 하네스가 안 돌아간다" 문제 해소. 원인은 경로·별칭이 아니라 **T1 티어 전체가 죽어 있었던 것**(BUG-20260828-03) — `pnpm test:blockkey` → 3 pass / 0 fail |
| **C1, C3~C6** (G1) | ✅ 통과 | T3 실기 (2026-08-27) |
| **C2** (G1) | ✅ 종결 | 버그 아님 — 블록 경계가 생성/소멸하는 편집에서는 판정 공식이 성립하지 않음. §9.3 |
| **V1, V2** | ✅ 통과 (0px) | T3 실기 (2026-08-27) |
| **V5** | ✅ 통과 | T2 실앱. 200개 위치 왕복 측정, 실제로 보이는 155개 위치는 전부 오차 ≤2px. 편차 8건은 전부 **데코레이션으로 가려진 오프셋**(클릭 자체가 불가능한 지점)이며 최대 8px = 한 글자 미만. §10.2 |
| **V3, V4, V6** | ⏸ **T3 이월** | §2 규칙 2 — 스크롤이 답에 관여하면 T3 필수이며 **T2 통과는 근거가 되지 못한다**(Chromium 이 스크롤 앵커링으로 증상을 가림). 근거를 만들 수 없으므로 T2 에서 통과 처리하지 않는다. → `ARCHITECTURE_FINDINGS.md` A4 |
| **T1~T5** | ✅ **전부 통과** | T2 실앱 실측. 설계값 일치 + **전 항목 `height > 0`** 확인("후보 B 함정" 회피). §10.1 |
| **T6** | ✅ 통과 | C1~C6 = 전항 |
| **R2, R5** | ✅ 통과 | R2: T3(2026-08-27). R5: T2 재확인 — 툴바가 **활성 뷰**(둘째 블록)에만 적용, 인접 블록 무변경 |
| **R3** | ✅ **결함 발견 → 수정 → 통과** | 최초 실행에서 **실패**. 병합 시 캐럿이 문서 끝으로 튀고 포커스 소실. 원인 확정·수정 후 재검증 통과 → **BUG-20260828-02** |
| **R4** | ✅ 통과 | T2 실앱, 실제 키 입력(`#`,`#`,Space). 2블록 분할, 캐럿 3, 포커스 유지 |
| **R6** | ✅ 통과 | Read Mode 3개 루트에 헤딩4·코드블록1·리스트2·체크박스1·인용1·인라인코드2 전부 무손실 렌더 |
| **R8** | ✅ 통과 | 탭 3개 × 왕복 3회 = 9회 방문. 탭별 내용 동일, **교차 오염 0** |
| **R10** | ✅ **원인 확정 → 수정 → 통과** | **BUG-20260828-01**. 원인은 블록쿼트가 아니라 헤딩 라인의 수직 `margin`. §9.7 |
| **R1** (IME) | ⏸ **사람 확인 이월** | 이 저장소에 연결 가능한 어떤 자동화 도구도 실제 composition 이벤트를 만들지 못한다(§9.5 에서 이미 확인). 자동 검증이 **원리적으로 불가능**하므로 이월이 유일한 정직한 처리다 |
| **R7** (이미지) | ⏸ **T3 이월** | `.devoras/images` + `convertFileSrc` 는 Tauri 자산 프로토콜이 필요. T2 mock FS 로는 경로 해석만 확인 가능(`test:image` 32 pass)하고 실제 렌더는 확인 불가 |
| **R9** (대형 문서) | ✅ **측정 완료 — 예산 초과 확정** | G3-1 과 동일 측정. 아래 |
| **G3-1** (인스턴스 비용) | ✅ **측정 완료 — 실패 판정** | N=200 마운트 **7.0~17.4초** vs 예산 300ms → **20~58배 초과**. 메모리는 블록당 42~98KB 로 예산 내. §10.3 |
| **G3-2** (가상화 판정) | ✅ **판정 완료** | §6.2 규칙에 따라 "예산 초과 → E7 착수" 조건 충족. 단 **착수 전 레이아웃 스래싱 프로파일 선행**을 권고 → `ARCHITECTURE_FINDINGS.md` A6 |
| `e0_harness.cjs` 삭제 | ✅ | 9b1d96c |
| 커밋 위생 | ✅ | 54d5c0e |

**이월 4건(V3/V4/V6, R1, R7)은 전부 "T2 로는 원리적으로 근거를 만들 수 없는" 항목**이다.
T2 에서 돌려 통과시키는 것은 §2 규약 위반이므로 하지 않았다. 이월 사유와 필요 조건은
`ARCHITECTURE_FINDINGS.md` A4(상시 디버그 채널)에 후속 과제로 등록했다.

---

## 9. T3 실기 검증 세션 기록 (2026-08-27 05:13~05:50)

`tauri-plugin-mcp-bridge` 를 디버그 전용 의존성으로 추가해(사용자 승인 완료, 프로덕션 빌드 미영향) 실제 WKWebView 에서 G0~G2 를 처음으로 실기 검증했다. **검증 도중 신규 회귀 2건을 발견해 즉시 수정·재검증했다** — 둘 다 G0 커밋(c5a8e2e) 또는 그 주변 구조와 직결된다.

### 9.1 K1~K3 하네스 실행 가능성 문제

`block_key_stability_harness.ts` 는 로직상 K1~K3 를 통과하지만(수동으로 확장자·별칭을 임시 해석해 검증), 커밋된 상태 그대로는 §2 T1 규약("plain node 로 실행 가능해야 함")을 어긴다. 다음 담당자는 상대 임포트에 `.ts` 확장자를 붙이고, `store.ts` 가 참조하는 `@/shared/lib/headingId` 별칭을 상대경로로 교체하거나 하네스를 vitest 등 별칭 해석이 되는 러너로 옮겨야 한다.

### 9.2 신규 발견·수정된 회귀 2건

| ID | 증상 | 원인 | 수정 | 티켓 |
|---|---|---|---|---|
| BUG-20260827-01 | FormatToolbar 전 버튼·이미지 붙여넣기 무력화 | 등록 `useLayoutEffect` 가 뷰 생성 `useLayoutEffect` 보다 먼저 선언되어, 최초 마운트 시 `viewRef.current` 가 `null` 인 채로 `registerEditorView` 스킵 → 레지스트리 영구 미등록 | 두 이펙트 선언 순서 교체 | [`20260827_0530_editor_view_registry_never_registers_resolved.yml`](../ticket/hist/debug/20260827_0530_editor_view_registry_never_registers_resolved.yml) |
| BUG-20260827-02 | ArrowUp/ArrowDown 블록 간 이동 불가 | `defaultKeymap` 이 `blockKeymap` 보다 먼저 등록되어, `cursorDown`/`cursorUp` 이 경계에서도 항상 `true` 반환 → 커스텀 경계 핸들러가 절대 실행되지 않음 | extensions 배열에서 `blockKeymap` 을 `defaultKeymap` 보다 먼저 배치 | [`20260827_0545_block_keymap_precedence_resolved.yml`](../ticket/hist/debug/20260827_0545_block_keymap_precedence_resolved.yml) |

두 건 모두 T3 실기로 수정 전 재현 → 수정 → 재검증까지 완료했다.

### 9.3 C2 판정에 대한 참고

C2("ΔY ≤ 라인 높이 증가분")는 **같은 블록 내부**에서의 헤딩 레벨 변경(예: H2→H1)에서는 깔끔히 성립한다(ΔY=2px vs 높이증가 18px, 실측 확인). 그러나 **평문↔헤딩 승격/강등처럼 H1/H2/H3 블록 경계 자체가 새로 생기거나 사라지는 편집**에서는, 새 블록의 wrapper padding/border 가 함께 삽입/제거되므로 좌표계가 블록 단위로 리셋되어 단순 "라인 높이 차" 공식이 무의미해진다. 이는 `store.ts` 의 H1~H3 슬라이싱 설계상 불가피한 특성이며, 이 상황에서도 **scrollTop 은 항상 0 변화, 커서는 항상 논리적으로 올바른 위치**를 유지함을 확인했다 — 즉 "버그"가 아니라 C2 판정 공식이 이 케이스를 다루도록 쓰이지 않았을 뿐이다.

### 9.4 신규 발견 · **미해결** 1건 (사용자 보고, 2026-08-28)

| ID | 증상 | 상태 | 티켓 |
|---|---|---|---|
| BUG-20260828-01 | 블록쿼트(`>`) 구간에서 `ArrowUp`/`ArrowDown` 수직 이동 시 줄을 건너뛰거나 스킵. `ArrowUp` 이 더 빈번, `ArrowDown` 은 덜함. 재현 조건 미확정(간헐적) | **Done** (§9.7 — 원인은 블록쿼트가 아니라 헤딩 라인의 수직 `margin`) | [`20260828_1920_blockquote_vertical_cursor_skip.yml`](../ticket/debug/20260828_1920_blockquote_vertical_cursor_skip.yml) |

**증상 이력**: 과거에는 평문·코드블록·리스트 전반에서 심하게 발생 → 2026-08-11 수정 파동(BUG-20260811-08/10)으로 대부분 해소 → **현재는 블록쿼트에서만 관찰**된다.

> **BUG-20260827-02 와 혼동 금지.** 그 건은 키맵 우선순위로 인한 **블록 간** 이동 실패이며 해결되었다. 본 건은 **블록 내부 라인 간** 수직 이동으로 축이 다르다.

**왜 블록쿼트만 남았는가 — 데코레이터 대조**

| 데코레이터 | 라인 선두 마커 치환 방식 | `bidiIsolate` | 위젯 |
|---|---|---|---|
| `HeadingDecorator:55` | `replace` + `HeadingBadgeWidget` | `false` 명시 | 있음 |
| `ListDecorator:54` | `replace` + `BulletWidget` | `false` 명시 | 있음 |
| `CheckboxDecorator:108` | `replace` + `CheckboxWidget` | `false` 명시 | 있음 |
| **`BlockquoteDecorator:6`** | **`Decoration.replace({})` — 위젯 없음** | **미지정** | **없음** |

2026-08-11 파동은 **위젯을 가진** 데코레이터만 교정했다. 위젯 없는 라인 선두 치환은 그때 대상이 아니었고, 블록쿼트가 유일하게 그 형태로 남아 있다. 이것이 "다른 곳은 다 나았는데 블록쿼트만 남았다"는 보고와 정확히 일치한다.

**가설 (재현 우선순위 순)**

1. **H3 — 커서 위치 의존 데코레이션** (`BlockquoteDecorator.ts:50`). 마커를 **커서가 있는 줄에만** 노출하므로, 수직 이동 1회마다 출발 줄은 마커가 숨겨지고(−2자) 도착 줄은 드러난다(+2자). CodeMirror 의 goal column 은 이동 직전 좌표로 측정되는데 그 좌표계가 같은 업데이트 사이클에서 무효화된다. **`ArrowUp` 이 더 취약하다는 비대칭 보고와 부합**한다(도착 줄이 위쪽에서 늘어나면 기준 좌표가 함께 밀림).
2. **H1 — 폭 0 치환 위치**. `replace({})` 구간이 정확히 column 0~2 로, 수직 이동이 캐럿을 놓는 자리와 겹친다.
3. **H2 — `bidiIsolate: false` 누락**. BUG-20260811-08 이 "좌우 튕김 및 **Y축 측정 오류**" 해결책으로 지정한 옵션이 블록쿼트에만 빠져 있다.
4. **H4 (보조) — 단독 `>` 줄**. `markerEnd == line.to` 라 **줄 전체가 치환**되어 시각적 빈 줄이 된다(`:51`).
5. **공통** — orchestrator 에 `EditorView.atomicRanges` 등록이 없어 캐럿이 숨겨진 구간 내부에 머무를 수 있다.

**티어**: **T3 필수.** 원인 후보가 전부 레이아웃 측정·데코레이션 재계산이므로 §2 규약상 T0/T1 로는 구조적으로 탐지되지 않는다. 판정은 `webview_keyboard` 로 방향키를 주입하고 `selection.main.head` 의 **라인 델타가 항상 ±1 인지**로 한다.

**수정 범위 경고**: H3 이 원인으로 확정되면 "커서 라인만 마커 노출" 정책 자체를 블록쿼트 **구간 단위**로 재설계해야 하므로 수정 범위가 가장 크다. H2 는 한 줄 변경이므로 **H2 → H1 → H3 순으로 단독 측정**해 최소 수정으로 끝나는지 먼저 확인할 것.

> ⚠️ **위 H1~H4 가설은 아래 §9.6 에서 T3 실기로 전부 기각되었다.** 이 절(§9.4)은 최초 조사 시점의 기록으로 남겨두되, 실제 조치는 §9.6 을 따를 것.

### 9.5 남은 범위

R1(IME 실기), **R10(BUG-20260828-01, §9.6 참조 — 범위 재정의됨)**, V3/V5/V6, T1~T5, G3-1/G3-2 는 이번 세션에서 다루지 못했다. 특히 **R1 은 이 저장소에 연결 가능한 어떤 자동화 도구로도 실제 조합(composition) 이벤트를 만들어낼 수 없어, 사람이 직접 한글을 입력해 확인해야 한다.**

### 9.6 T3 실기 재검증 — BUG-20260828-01 은 블록쿼트 한정이 아니다 (2026-08-28 20:10)

`tauri-plugin-mcp-bridge` 를 임시 재추가(사용자 승인, 검증 후 즉시 재회수)해 §9.4 의 H1/H2/H3 가설을 **실제 네이티브 키 입력**(`webview_keyboard` press)으로 하나씩 검증했다.

**결과: H1, H2, H3 모두 기각.**

- H2(`bidiIsolate:false`) 단독 적용 → 스킵 그대로 재현.
- H1(폭 0 위젯으로 교체) 추가 적용 → 변화 없음.
- H3(마커 노출을 "커서 줄" → "커서가 속한 구간 전체"로 재설계) 적용 → DOM 상 구간 전체 마커가 실제로 노출되는 것을 재확인했음에도(즉 구간 내부 이동 중 가시 콘텐츠 길이가 전혀 바뀌지 않는데도) 스킵이 **동일하게** 재현됨. "마커 토글이 goal column 을 오염시킨다"는 H3 의 핵심 메커니즘 자체가 반증됐다.

**결정적 반증**: 블록쿼트가 **전혀 없는** 순수 평문 문서(`Plain line A/B/C...`)에서 같은 방식(real key press)으로 재검증한 결과 **동일한 스킵 패턴이 그대로 재현됐다** — 예: 빈 줄(line2)에서 ArrowDown → line3(`Plain line A`)를 건너뛰고 line4 로 이동. BlockquoteDecorator 는 이 문서 렌더링에 전혀 관여하지 않으므로, **결함은 블록쿼트 데코레이터 코드와 무관하다.**

**조치**: 근본 원인을 고치지 못한 세 가설의 코드 변경(H1/H2/H3)은 전부 되돌렸다. `BlockquoteDecorator.ts` 는 이 조사 착수 이전 상태(HEAD) 그대로다. `tauri-plugin-mcp-bridge` 도 검증 완료 후 즉시 재회수해 코드베이스에 남아 있지 않다. 상세 기록은 [`ticket/debug/20260828_1920_blockquote_vertical_cursor_skip.yml`](../ticket/debug/20260828_1920_blockquote_vertical_cursor_skip.yml)의 `t3_verification_20260828_2010` 항목 참조.

> ✅ **§9.7 에서 해결됨.** 아래 "다음 담당자에게" 1·2번이 지목한 방향(keymap 조합 / orchestrator 전역 재계산)은 **둘 다 오답**이었다. 실제 원인은 CSS 였다 — §9.7 참조.

**다음 담당자에게**(당시 기록):
1. 조사 범위를 블록쿼트 데코레이터에서 **`BlockEditor.tsx` 의 keymap 조합**(`blockKeymap` + `defaultKeymap` 순서 — BUG-20260827-02 수정 이후 상태) 또는 **`markdownDecorationPlugin`/orchestrator 의 전역 재계산 경로**로 넓힐 것.
2. `@codemirror/commands` 의 실제 `cursorLineDown`/`cursorLineUp` 구현이 goal column 을 어디에 캐시하고 어떤 조건에서 리셋하는지 **소스 레벨로** 확인할 것 — 블랙박스 추측(H1~H4)은 전부 틀렸다.
3. 이번 T3 재현은 거의 매 입력마다 일관되게 나타났으나, 사용자의 원 보고는 "간헐적"이었다. `webview_keyboard` 주입이 만드는 타이밍이 실제 물리 키보드와 다른 인공적 조건을 만들고 있는 건 아닌지, 사람이 직접 재현해 교차 확인이 필요하다.

---

### 9.7 BUG-20260828-01 해결 — 원인은 `.cm-line` 의 수직 `margin` (2026-08-28 21:40)

**§9.4 의 H1~H4 도 틀렸고, §9.6 이 다음 방향으로 지목한 두 가지(keymap 조합 ·
orchestrator 전역 재계산)도 틀렸다.** 실제 원인은 데코레이터 *로직*이 아니라
데코레이터가 입히는 *CSS* 였다.

#### 원인

`orchestrator.ts` 의 `decorationBaseTheme` 이 `.cm-h1-line` ~ `.cm-h6-line` 에
수직 `margin` 을 주고 있었다(`margin: 0.75rem 0 0.5rem` 등).

CodeMirror 6 는 라인 높이를 `DocView.measureVisibleLineHeights()` 에서
`getBoundingClientRect().height` 로 재어 height map 에 쌓는데, **이 값에는 margin 이
포함되지 않는다.** 따라서 헤딩 라인을 하나 지날 때마다 실제 DOM 좌표가 height map
좌표보다 (margin-top + margin-bottom) 만큼 아래로 밀리고, 그 오차가 아래 라인으로
계속 누적된다.

수직 이동은 이 두 좌표계를 섞어 쓴다 — `moveVertically()` 는
`coordsAtPos()`(**실제 DOM**)로 목표 y 를 구한 다음
`posAtCoords()` → `elementAtHeight()`(**height map**)로 그 y 를 문서 위치로 되돌린다.
누적 오차가 한 라인 높이에 육박하면 **방향키 한 번에 두 줄을 건넌다.**

#### T2 실측 (playwright MCP, Chromium 1100×1400)

`drift = (실제 DOM top − height map top)` 을 라인별로 측정했다.

| 문서 | 수정 전 drift | 수정 전 `moveVertically` 델타 |
|---|---|---|
| 재현 문서 A (H1 + 평문 8줄) | line1 +12px, line2 이후 전부 **+20px** (라인 높이 22.39px 의 89%) | `1->2(1)`, `2->4(2)`, `4->6(2)` — **인수인계 §2 의 T3 표와 완전 일치** |
| 혼합 문서 (H1~H4 + 블록쿼트·리스트·코드블록·수평선·수식·이미지) | 헤딩마다 계단식 누적: 12 → 20 → 30 → 36.39 → 44.39 → 49.98 → 56.38 → **60.38 이후 평평** | ArrowDown 델타 2~4, ArrowUp 은 line42 → **line1** 로 문서 최상단까지 튐 |

핵심은 혼합 문서의 계단 모양이다. **drift 가 늘어나는 지점은 오직 헤딩 라인이고,
블록쿼트·코드블록·리스트·수평선·이미지·수식의 기여분은 정확히 0** 이다
(블록쿼트는 이미 `padding` 을 쓰고 있었다). §9.6 의 "블록쿼트 무관" 결론과 정확히 맞물린다.
ArrowUp 이 더 심한 것도 설명된다 — 위로 갈수록 오차가 height map 문서 시작점을
넘어서면 위치가 0 으로 잘려 문서 맨 위로 튄다.

#### 수정

`orchestrator.ts` 의 헤딩 라인 스타일에서 `margin` → `padding` 교체.
padding 은 `getBoundingClientRect()` 에 포함되므로 height map 이 정확해진다.
가로 `4px` 은 `BlockEditor.tsx` 의 `.cm-line { padding: 0 4px }` 값을 shorthand 로
덮어쓰며 잃지 않도록 명시적으로 되살렸다.

**시각적 영향**: 헤딩 글리프 위치는 픽셀 단위로 동일하다(배경·테두리가 없어
margin 12/8 과 padding 12/8 의 렌더 결과가 같다). 유일한 차이는 **헤딩 줄이 연속으로
붙어 있을 때** margin collapse 가 사라져 간격이 약 5.6px 늘어난다는 것인데,
H1~H3 는 각각 별도 `EditorView` 블록을 열므로 실질적으로 H4~H6 연속 구간에만 해당된다.

#### 검증 결과

4개 픽스처(plain / blockquote / headings / mixed) 전부 **drift 0.00, 델타 ±1(양방향)**.
실제 네이티브 키 입력(`page.keyboard.press`)으로도 `2→3→4→5→6`, `6→5→4` 확인.
수정 후 CSS 로 옛 margin 을 다시 주입하면 하네스가 즉시 FAIL 하며 `2->4(2)`, `4->6(2)` 를
그대로 재현한다 — 회귀를 실제로 잡는다는 확인이다.

#### 티어 판정에 관해

§9.4 는 이 건을 **T3 필수**로 배정했으나 이는 과도했다. §2 규칙에 따르면
답에 관여하는 것은 **레이아웃**(T0/T1 금지)이지 **스크롤**(T3 필수)이 아니다.
규칙 3 이 정한 대로 **T2 에서 판정하고 T3 는 1회 확인**이 올바른 배정이다.
실제로 §9.6 이 T3 를 위해 `tauri-plugin-mcp-bridge` 를 재추가하는 비용을 치르고도
원인에 도달하지 못한 반면, T2 는 `drift` 라는 **직접 관측 가능한 중간 지표**를
제공해 한 번의 측정으로 원인을 특정했다.

> **교훈 보강**(§1 의 "이 질문의 답에 레이아웃이 관여하는가"에 이어):
> 티어를 올리기 전에 **"측정할 중간 지표가 있는가"** 를 먼저 물어야 한다.
> §9.6 은 최종 증상(라인 델타)만 관측하며 가설을 하나씩 지웠고 그래서 전부 빗나갔다.
> `drift` 처럼 원인에 한 단계 가까운 양을 재면 가설 검증이 아니라 **원인 특정**이 된다.

#### 회귀 하네스 (T2)

[`vertical_motion_t2_harness.ts`](src/widgets/BlockEditor/__tests__/vertical_motion_t2_harness.ts)
+ [`vertical_motion_t2.html`](src/widgets/BlockEditor/__tests__/vertical_motion_t2.html)

```bash
pnpm dev
# → http://localhost:1420/src/widgets/BlockEditor/__tests__/vertical_motion_t2.html
# → 콘솔에서: await qa.report()
```

판정: 모든 픽스처에서 `drift === 0` 이고 라인 델타가 `±1`.

> ⚠️ playwright/MCP 로 자동화할 때 **탭이 백그라운드면 `innerWidth/innerHeight` 가 0** 이 되어
> CodeMirror 가 라인 높이를 측정하지 못하고 `heightOracle` 이 기본값 14px 에 머문다.
> 그 상태의 측정값은 전부 무의미하다 — 반드시 뷰포트가 살아 있는 상태에서 실행할 것.
> (이번 세션에서 실제로 한 번 여기에 빠졌다.)

#### 남은 것

1. **T3 실기 1회 확인.** 원인이 CSS margin 과 `getBoundingClientRect` 의 관계라
   엔진 독립적이므로 WebKit 에서 달라질 이유는 없다. `tauri-plugin-mcp-bridge` 는
   의도적으로 회수된 상태이므로 재추가하지 말고 **사람이 직접 방향키를 눌러** 확인하는
   편이 낫다 — 인수인계 §6-4 가 요구한 "사람이 직접 재현"과 함께 처리하면 된다.
2. **"간헐적"의 정체.** 스킵 여부는 커서 위쪽 헤딩들의 누적 margin 이 라인 높이에
   얼마나 근접하냐로 결정된다. 헤딩이 없는 블록에서는 drift 가 0 이라 정상 동작하고,
   헤딩 아래에서는 거의 항상 발생한다. 사용자의 "간헐적" 체감과 자동화의 "거의 항상"
   관측 사이의 간극은 이 **문서 구조 의존성**으로 설명된다.

#### 조사 중 발견된 별건 (신규 티켓 필요)

`---`(수평선) 라인을 **ArrowUp 으로 통과할 때 그 줄을 건너뛴다**(ArrowDown 은 정상).
헤딩이 전혀 없어 drift 가 0 인 문서에서도 재현되므로(`down: 1→2→3→4→5` 정상,
`up: 5→4→2`) height map 불일치와는 다른 축이며 이번 수정으로 생긴 것도 아니다.
추정 원인은 `posAtCoords()` 의 라인 패딩 회피 루프가 `.cm-hr-line`(height 1px)의
렌더 박스를 보고 "패딩에 착지했다"고 오판해 블록을 통째로 건너뛰는 것.

---

## 10. 최종 검증 세션 기록 (본 계획서 종결) (2026-08-28 19:20~20:33)

`pnpm dev` + playwright MCP 로 **실제 앱을 Chromium 에서 구동**해 T2 게이트를 실행했다.
저장소의 `fs.ts` 가 이미 브라우저용 `MockFileSystem` 을 갖고 있어 Tauri 없이 앱 전체가 그대로 돈다 —
이 사실이 그동안 활용되지 않아 T2 티어가 사실상 비어 있었다(→ A4).

### 10.1 타이포그래피 T1~T5 — 전항 통과

| 항목 | 실측 | 설계값 | 높이 |
|---|---|---|---|
| `.cm-h1-line` | 35px / 700 / `rgb(241,245,249)` / lh 45.5 / center | 일치 | 65.5 |
| `.cm-h2-line` | 20px / 600 / `rgb(226,232,240)` / lh 27 | 일치 | 43.39 |
| `.cm-h3-line` | 16.8px / 600 / `rgb(203,213,225)` / lh 23.52 | 일치 | 37.11 |
| `.cm-h4-line` | 14.4px / 600 / `rgb(148,163,184)` / lh 20.16 | 일치 | 30.55 |
| `.cm-code-block-header` / `.cm-cb-lang` / `.cm-cb-title` | 12 / 11 / 12 px | 일치 | 35 / 16.5 / 18 |
| `.cm-heading-badge` | 10.4px | 0.65rem | 16.78 |
| `.cm-inline-code` | 13.2px (부모 15px 대비 **0.88**) | 0.88em | 16.31 |

**T2(Read↔Write 정합)**: h1/h2/h3/h4 의 font-size·weight·color 가 Write 와 Read 에서 **완전 일치**.

### 10.2 V5 캐럿 정확도

`coordsAtPos → posAtCoords → coordsAtPos` 왕복으로 전 위치를 검사했다(클릭 경로가 쓰는 바로 그 함수쌍).
200개 위치 중 편차 8건, **그중 6건은 가려진 구간 내부, 2건은 가려진 구간의 경계**다.
가려진 오프셋들은 하나의 시각적 점으로 겹치므로 클릭이 그중 하나를 고를 수 없다 — **결함이 아니라 구조상 당연**하다.
최대 편차 8px 로 한 글자 폭 미만이며, 줄이나 단어를 잘못 짚는 사례는 **0건**이다.

### 10.3 G3-1 / R9 인스턴스 비용 실측

| N | 스토어 로직 | 마운트 완료 | 블록당 |
|---|---|---|---|
| 25 | 0.4 ms | 1,015 ms | 40.6 ms |
| 50 | 0.5 ms | 2,271 ms | 45.4 ms |
| 100 | 1.6 ms | 5,677 ms | 56.8 ms |
| **200** | **1.6 ms** | **7,024~17,423 ms** | **35~87 ms** |

- 예산 **300ms @ N=200** 대비 **20~58배 초과** → **G3-1 실패**
- 메모리 블록당 42~98KB → 예산(150KB) **내**
- **스토어 로직은 사실상 공짜**(N=200 에서 1.6ms). 비용의 거의 전부가 `EditorView` 생성이다
- 블록당 비용이 40 → 87ms 로 **증가**. 성장 지수 ≈ **1.37(초선형)** → 레이아웃 스래싱 의심(추정, 프로파일 필요)

### 10.4 이번 세션에서 신규 발견·처리한 결함

| ID | 증상 | 처리 |
|---|---|---|
| **BUG-20260828-01** | 수직 방향키 줄 스킵 | ✅ 수정 — 헤딩 라인 `margin` → `padding` (§9.7) |
| **BUG-20260828-02** | 블록 병합 시 캐럿이 문서 끝으로 튀고 포커스 소실 | ✅ 수정 — 내용 치환 트랜잭션에 selection 명시 |
| **BUG-20260828-03** | **T1 티어 전체 실행 불가** (Node 20 에 없는 플래그) | ✅ 수정 — 무의존성 로더 훅, `pnpm test:t1` |
| **BUG-20260828-04** | `---` 라인 ArrowUp 스킵 | ⏸ 보류 — 범위가 고립되어 있어 의도적으로 남김 |

---

## 11. 아카이브 시점 인계

본 계획서(0.8.11 검증 게이트 G0~G3)는 **§8 의 모든 항목이 종결되어 여기서 닫는다.**
이월 4건(V3/V4/V6, R1, R7)은 "T2 로는 원리적으로 근거를 만들 수 없는" 항목이며,
그 실행 조건은 후속 문서가 과제로 들고 간다.

**후속 문서**: [`ARCHITECTURE_FINDINGS.md`](../../ARCHITECTURE_FINDINGS.md)

이 계획서를 통해 드러난 것은 개별 결함보다 **구조 문제**였다. 요약하면:

- **A1/A2** 편집 상태의 진실 공급원이 둘이고, 그 조정을 React 이펙트 실행 순서에 맡기고 있다.
  최근 결함 3건(BUG-20260827-01/02, BUG-20260828-02)이 전부 이 계열이다. → 수정 완료(부분)
- **A3** CSS 가 CodeMirror 의 레이아웃 불변식을 침범할 수 있는 구조. → 수정 + 불변식 문서화 + 회귀 하네스 고정
- **A4** 검증 티어가 선언만 되어 있고 실행 수단이 없었다. T1 은 **한 번도 실행 가능한 적이 없었고**(Node 20),
  T2 는 러너가 없었으며, T3 는 세션마다 플러그인을 붙였다 떼느라 회귀로 고정되지 않는다.
  **§1 이 진단한 "티어 오선택"보다 이쪽이 더 근본적인 원인이다.** → T1·T2 복구 완료, T3 남음
- **A5** 게이트가 최종 증상만 관측한다. §9.6 이 T3 한 세션을 쓰고도 원인에 닿지 못한 이유이며,
  같은 버그가 `drift` 라는 중간량 한 번의 측정으로 풀렸다. → 게이트별 중간 불변식 도입 권고
- **A6** 블록당 EditorView 설계가 자체 예산을 20~58배 초과하고 비용이 초선형이다. → E7 판정 조건 충족
- **A7** 헤딩 블록의 정체성이 아직 내용에서 파생되어, `key` 를 통한 EditorView 파괴 경로가 살아 있다. → 남음

### 다음 담당자에게 (우선순위)

1. **A7 헤딩 정체성 분리** — 남은 결함 중 유일하게 **undo 히스토리 손실**을 만든다.
2. **A6** — 가상화에 바로 착수하지 말고 **레이아웃 스래싱을 먼저 프로파일**할 것.
   가상화에 착수한다면 **높이 캐시 필수**이며 V1~V4 재실행 필수다.
   높이 캐시 없는 가상화는 BUG-20260828-01 과 **같은 계열의 결함을 재생산**한다.
3. **A5** — 개발 빌드에 `store.focusOffset === view.selection.main.head` 단언을 넣을 것.
   비용이 거의 없고, 이것만 있었으면 BUG-20260828-02 는 즉시 드러났다.
4. **A4(T3)** — 프로덕션 빌드에서 배제되는 **상시** 디버그 채널. 붙였다 떼는 방식인 한 T3 결론은 계속 휘발된다.

### 유지되는 규약

- §2 의 티어 배정 규칙은 **유효하다.** 다만 규칙에 한 줄을 보탠다 —
  **티어를 올리기 전에 "측정할 중간 지표가 있는가"를 먼저 물을 것.**
- **커밋을 합치지 말 것** (§7).
- **V 기준은 0px.** 근사치 통과 금지.
- **헤딩 크기 임의 조정 금지.** 폴백은 사용자 승인 사항.
