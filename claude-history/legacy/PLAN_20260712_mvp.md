# Devoras 데스크톱 MVP 기획 구체화

## 요약

Tauri 2 기반 데스크톱 앱에서 사용자가 폴더 워크스페이스를 열고 Markdown 문서를 블록 에디터로 작성하면, 헤딩 구조가 마인드 뷰에 즉시 투영되는 MVP를 만든다. 마인드 뷰는 노드 위치만 드래그로 수정하며, Markdown이 내용과 계층의 유일한 원본이다.

## 핵심 기능과 기획 확정

- 워크스페이스: 사용자가 로컬 폴더를 열고 그 안의 `.md` 파일을 탐색·선택·저장한다.
- 에디터: 여러 CodeMirror 6 블록을 한 문서처럼 사용한다. `Enter`는 현재 블록을 분할하고, 빈 블록에서 `Backspace`는 이전 블록과 병합한다. 방향키·포커스 이동·붙여넣기·Undo/Redo의 블록 경계 동작을 명세화한다.
- 문서 해석: `H1`~`H6`을 마인드 노드 계층으로 사용한다. 헤딩 아래의 일반 본문은 해당 노드의 노트이며, 헤딩 순서와 수준이 트리 구조를 결정한다.
- 마인드 뷰: Markdown 변경 시 즉시 트리를 재생성하고 SVG/가벼운 DOM으로 렌더링한다. 제목·계층 수정은 지원하지 않고, 노드 드래그로 저장되는 `x/y` 좌표만 수정한다.
- 저장: Markdown 원문을 보존하고 좌표는 `<!-- devoras:spatial {...} -->` 형식의 HTML 주석으로 기록한다. 각 노드에 안정적인 식별자를 부여해 제목 변경과 좌표를 안전하게 연결한다.
- 화면 구성: 좌측 파일 탐색기, 중앙 블록 에디터, 우측 마인드 뷰의 3패널을 기본으로 하며 패널 크기 조절과 문서 전환 시 저장 상태를 표시한다.

## 구현 기반 및 인터페이스

- 현재 React/Vite/Zustand/TypeScript 기반에 Tauri 2, Tailwind CSS, CodeMirror 6, Markdown 파서 의존성을 추가한다.
- Tauri 명령은 워크스페이스 선택, Markdown 목록 조회, 파일 읽기·쓰기만 노출하고 모든 경로를 선택된 워크스페이스 내부로 제한한다.
- 프런트엔드 상태는 `workspace`, `document`, `editorBlocks`, `mindMapLayout`, `ui`로 분리한다. 저장되지 않은 편집 내용과 디스크 원본을 구분한다.
- 문서 파서의 공개 결과는 `DocumentAst`, `HeadingNode`, `SpatialMetadata`, `ParseDiagnostic`으로 고정한다. 잘못된 헤딩 순서나 손상된 메타데이터는 편집을 막지 않고 경고로 표시한다.
- Rust/WASM Markdown·ERD 파서는 이후 단계로 둔다. MVP에서는 TypeScript 파서로 투영 안정성을 검증하고, 파서 인터페이스를 분리해 교체 가능하게 만든다.
- ERD DSL, 이미지/첨부 분석, 마인드 뷰에서의 구조 편집은 MVP 범위에서 제외한다.

## 검증 기준

- 폴더 열기, 파일 목록 표시, 생성·수정·저장·재열기 후 Markdown과 좌표가 보존된다.
- 헤딩 추가·삭제·이동·수준 변경이 마인드 트리에 즉시 반영되며 비정상 헤딩 구조도 앱이 중단되지 않는다.
- 블록 분할·병합·블록 간 키보드 이동·여러 블록에 걸친 붙여넣기·Undo/Redo가 커서와 내용 손실 없이 동작한다.
- 노드 드래그 후 저장·재시작 시 위치가 복원되고, 텍스트 변경 후에도 동일 노드의 위치가 유지된다.
- TypeScript 엄격 검사, ESLint, 빌드와 핵심 파서·상태·에디터 상호작용 테스트를 통과한다.

## 기본 가정

- 첫 지원 플랫폼은 macOS이며, 워크스페이스에는 일반 UTF-8 Markdown 파일만 포함한다.
- 자동 레이아웃을 기본값으로 제공하고, 사용자가 옮긴 노드만 좌표 메타데이터로 저장한다.
- 외부에서 파일이 수정된 경우에는 자동 병합 대신 변경 감지와 새로고침/덮어쓰기 선택을 제공한다.
