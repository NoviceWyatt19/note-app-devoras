# 🐛 FIX TICKET
type: "FIX_TICKET"
id: "BUG-20260826-08"
# ━━━
update: "2026-08-26 17:40"
project_ref: "PRJ-DEVORAS-MVP"
subject: "비헤딩 블록이 첫 줄 원문을 키로 사용해 블록 간 ID 가 뒤바뀌고 포커스/IME 조합이 끊기는 문제"
status: "Todo"
severity: "P1"
review_ref: "code_review.md §P1-4"
verified_at: "v0.8.5"
# ━━━
bug_report:
  symptom: "빈 줄이 여러 개이거나 '- 항목' 처럼 같은 문구로 시작하는 블록이 여럿인 문서에서, 편집 중 커서가 다른 블록으로 튀거나 한글 조합이 중간에 끊긴다."
  cause: "deriveBlockKey 가 level 0 블록에 대해 첫 줄 문자열을 그대로 키로 반환한다. 빈 블록은 전부 '' 로 충돌하며, existingByKey 는 첫 매치만 보존하므로 리빌드 시 뒤쪽 블록이 앞쪽 블록의 id 를 물려받는다. 결과적으로 엉뚱한 CodeMirrorBlock 이 리마운트된다."
  location:
    - "project/src/entities/block/model/store.ts:48 (deriveBlockKey — return firstLine)"
    - "project/src/entities/block/model/store.ts:147-157 (existingByKey 매칭 및 usedIds 처리)"
  related_history:
    - "ticket/debug/fix_block_split_cursor_jump_to_first_h2.md"
    - "ticket/debug/fix_duplicate_id_and_coordinate_reset.md"
    - "ticket/hist/debug/20260811_1754_codemirror_cursor_skip.yml"
# ━━━
architecture_guidelines:
  - "Positional Identity: 헤딩이 아닌 블록의 정체성은 '문구'가 아니라 '부모 + 순번'이다."
  - "No Silent Steal: 키가 충돌해도 서로 다른 블록이 같은 id 를 넘겨받는 일이 없어야 한다."
  - "Heading Path Untouched: 헤딩 블록의 buildHeadingId 경로는 마인드맵 좌표 매칭과 공유되므로 변경하지 않는다."
# ━━━
tasks:
  - "[ ] deriveBlockKey 의 비헤딩 분기를 부모키 + '#text' + 순번 형태로 변경 (siblingCountMap 재사용)"
  - "[ ] deriveKeysWithParentStack 에서 비헤딩 순번 카운터가 부모 전환 시 올바르게 동작하는지 확인"
  - "[ ] 기존 문서 호환성 확인 — 키 포맷 변경이 마인드맵 spatialData 키에 영향을 주지 않는지 검증(헤딩 키는 불변이어야 함)"
# ━━━
harness:
  path: "project/src/entities/block/__tests__/block_key_harness.ts"
  run_command: "node --experimental-strip-types src/entities/block/__tests__/block_key_harness.ts"
  cases:
    - "빈 줄 3개가 연속된 문서 → 생성된 키 3개가 모두 서로 다른지"
    - "'- 항목' 이 동일하게 반복되는 리스트 문서 → 블록 id 가 리빌드 전후로 1:1 유지되는지"
    - "헤딩 블록의 키가 변경 전후 동일한지(마인드맵 좌표 회귀 방지)"
# ━━━
dod:
  - "하네스 3케이스 통과"
  - "빈 줄이 많은 문서에서 연속 타이핑 시 커서 점프 0건"
  - "한글 입력 중 블록 리빌드가 발생해도 조합이 끊기지 않음"
