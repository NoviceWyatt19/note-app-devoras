# ⚙️ FEATURE TICKET
type: "FEATURE_TICKET"
id: "FEAT-20260810-03"
# ━━━
update: "2026-08-10 17:53"
project_ref: "PRJ-DEVORAS-MVP"
subject: "앱 초기 로딩 시 White Flash 제거 및 사전 렌더링(Start Hidden & Reveal) 적용"
status: "Todo"
# ━━━
architecture_guidelines:
  - "Start Hidden: Tauri 앱 실행 시 네이티브 윈도우를 숨김(hidden) 상태로 시작하여 미완성된 하얀 화면(White Flash) 노출을 원천 차단한다."
  - "Reveal on Ready: React 프론트엔드에서 DOM 렌더링과 테마 적용이 완전히 끝난 직후, Tauri Rust 백엔드에 신호를 보내 윈도우를 부드럽게 노출시킨다."
# ━━━
spec:
  as_is:
    - "앱 실행 시 윈도우가 즉시 표시되며, React와 CodeMirror가 렌더링을 마칠 때까지 일시적으로 눈부신 하얀색 빈 화면이 노출되어 몰입감을 떨어뜨림."
  to_be:
    - "사용자가 앱을 실행하면 백그라운드에서 완벽하게 렌더링(어두운 테마 및 에디터 로딩)을 마친 후, 완성된 상태의 윈도우만 즉시 화면에 나타남."
# ━━━
tasks:
  - "[ ] `tauri.conf.json` 수정: `windows` 배열의 기본 윈도우 속성에 `\"visible\": false` 설정 추가"
  - "[ ] Rust 백엔드 (`src-tauri/src/main.rs` 혹은 `lib.rs`)에 프론트엔드로부터 완료 신호를 받아 윈도우를 표시(`window.show()`)하는 네이티브 커맨드(예: `show_main_window`) 추가"
  - "[ ] React 프론트엔드 최상위 컴포넌트(`App.tsx` 또는 `BlockEditor.tsx` 마운트 시점)에 `useEffect`를 추가하여, 초기 렌더링 완료 후 `invoke('show_main_window')` 호출"
