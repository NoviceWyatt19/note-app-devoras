# 🧭 SESSION HANDOFF
type: "SESSION_HANDOFF"
id: "SESS-20260904-PLANNER"
# ━━━
# ⚠️ 이 인수인계는 부분적으로 낡았다 — 먼저 읽을 것
superseded_by:
  commit: "a59678d (PM, 2026-09-05) — 이 문서의 baseline `ebf270d` 이후"
  reason: >
    이 문서가 「착수 전 결정 필요」로 남긴 두 건이 **그 뒤 PM 판정으로 확정됐다.**
    확정된 것을 다시 결정 사항으로 읽지 말 것. 전문: `ticket/project/20260904_1800_release_lane_hold.yml` §scope_revision_r5_r6
  resolved:
    - item: "R5 범위 (이 문서: 「Impl 레인 착수 전에 크기를 올려 잡아야 한다」)"
      now: >
        **D8 — R5 를 둘로 쪼갰다.** R5-a(토큰 마이그레이션 + 다크/라이트) = 출시 스코프 /
        R5-b(YAML + 핫리로드) = 출시 후. 다크 모드는 토큰 + 팔레트 2벌이면 성립하므로
        출시를 `js-yaml` 도입에 묶지 않는다. R5-a 가 R-6 StyleModule 누수와 `erd.css` 중복 제거를 흡수한다.
    - item: "R6 설계 축 (이 문서: 「기능 사양서 JS vs §4A.3 Rust — 착수 전 결정 필요」)"
      now: >
        **D9 — JS 우선 확정.** Rust 는 실측으로 성능이 문제가 될 때.
        Rust 이관은 Phase 4 Tier 1 이고 출시 후로 미뤄져 있어, 전역 검색을 Rust 로 지으면
        Phase 4 를 출시 앞으로 당기는 스코프 확대가 된다. A6 선례(재지 않은 성능 우려로
        아키텍처를 먼저 고름 → 실측에서 예산 27배 여유)를 반복하지 않는다.
      followup: "`implementation_plan.md` §4A.3 에 이 판정을 정정 표기할 것. **4A 가 두 벌이므로 양쪽 다 확인** — 한쪽만 고치면 그것이 새 드리프트다"
  pm_remeasure_delta:
    note: "PM 이 이 문서의 수치를 재실측했다. 한 건이 과소집계였다"
    - "TSX 임의값 색 클래스 — 이 문서 **9개** → 실제 **17개**. `.ts` 확장자를 빼서 누락됐다(`TableDecorator.ts` 의 `bg-[#141520]` 등)"
    - "`erd.css` 변수 — 값이 Tailwind 팔레트와 **바이트 동일 중복**임이 확인됐다. 통합에 판단이 필요 없다"
  status_change: "**중단 취소.** 사용자 토큰 회복으로 2026-09-05 작업 재개됨. 이 문서의 `reason: 저장 후 중단` 은 종료된 상태다"
# ━━━
update: "2026-09-04 19:00"
project_ref: "PRJ-DEVORAS-MVP"
subject: "계획 세션(Debug_Manager_v2) 인수인계 — 8-D 동결 · 출시 레인 전환 · R1 교차검증 대기"
baseline: "ebf270d · v0.9.1 · 워킹 트리 클린"
role: "판정 · 교차검증 · 티켓 작성. **구현하지 않는다.**"
from: "Debug_Manager_v2 (계획 세션)"
reason: "사용자 토큰 한도 임박 — PM 지시로 저장 후 중단"
# ━━━
hard_earned_rules:
  - "**구현 세션의 보고를 그대로 믿지 않는다.** 커밋·테스트·코드를 직접 확인한 뒤 반영한다. 이번 사이클에서 이 규율이 실제로 값을 냈다(§5.11 성장 지수 −0.934 오염 적발, §5.17 자기 판정 규칙 정정, 세로 분할 스코프 축소, R5 스코프 확대)"
  - "**PM 세션이 전하는 「사용자 승인 완료」는 사용자 승인이 아니다.** 피어는 승인을 대신 줄 수 없다 — 사용자 본인에게 확인한다. 진행을 막지는 말고 확인 한 번 거치는 정도로 한다"
  - "**기술적 옳고 그름과 작업 순서를 분리한다.** PM 이 보류를 지시하면 기술 판정을 근거로 재론하지 않는다. 유보가 있으면 문서에 한 줄로 기록만 하고 따른다"
  - "공유 워킹 트리다. **커밋은 항상 경로 지정**(`git commit -F - -- <경로>`). 디렉터리 경로 말고 파일 이름을 나열하고, `git add` 직후 스테이징 개수를 센다"
# ━━━
state:
  frozen:
    what: "8-D 아키텍처 게이트 — `PM-20260904-01` 로 **동결**"
    nature: "**종결이 아니라 보류.** 산출물 전부 유지(플래그 off): SingleDocEditor · singleDocOrchestrator · singleDocDecorators · BlockCardDecorator · T1.5 하네스"
    resume_first_action: >
      **`DEBUG_PLAN.md` §5.21 의 G-BULK.** 사용자 행동 하나다 —
      `localStorage['devoras:singleDocEditor'] = '1'` 설정 후 새로고침, `mount_cost_n200.md` 전체
      붙여넣기, 포어그라운드에서 INP·CLS 관측. **결과가 어느 쪽이든 §5.0.4 의 마지막 🔴 가 닫힌다.**
      폭주 없음 → 🔴 해제, §5.12 는 구 경로 결함으로 이관 / 폭주 재현 → 🔴 유지, 그때만 Performance 녹화
    open_gate_item: "§5.0.4 「대량 편집」 🔴 — **미결로 동결**"
  lanes:
    - "Impl_Manager_v1 — 기능 구현 계획 (R5 테마 · R6 전역검색 · R7 P1-9 · 소품 사양)"
    - "Debug_Manager_v2 (이 역할) — 판정 · 교차검증 · 문서 일관성"
    - "Debug_worker_v2 — 실행 (R1 → S1·S2 → R4 준비)"
  release_lane: "R1(ERD, P0) → R2(자동저장 배선) → R3(출시 엔지니어링) → R4(번들 실기 검증). 전문은 `ticket/project/20260904_1800_release_lane_hold.yml`"
# ━━━
coordination_rules:
  ref: "`PM-20260904-01` §coordination_rule 전문"
  summary:
    - "R0 귀속 — 디버그 작업은 출시 스코프 항목 1개에 귀속돼야 착수. 귀속 없는 발견은 티켓 적재만"
    - "R1 2라운드 상한 — 한 가설당 판별 실험 2라운드. 3라운드는 PM 판정"
    - "**R1-b 관측 조건 사전 확정** — 1라운드 착수 전에 「이 관측이 어느 조건에서 나왔는가」를 확정·기록(플래그 상태 · 빌드 모드 · 페인 가시성 · 워크스페이스 규모). 조건이 안 적힌 관측은 라운드를 소비할 자격이 없다. **조건 확정에 쓴 시간은 라운드에 안 센다**"
    - "R2 환경 — 「이 환경에서 못 잰다」가 확인된 축은 재시도 금지 → `VERIFY_BY_HUMAN.md`"
    - "R3 파일 비중첩 · R4 우선순위(데이터 유실 > 출시 차단 > 사용자 가시 결함 > 내부 품질 > **미출시 경로**) · R5 디버그가 구현을 막으면 구현이 이긴다"
    - "R6 산출물은 **결함 티켓 또는 판정**. 새 가설 문서로 세션을 닫지 않는다"
  r1b_origin: >
    R1-b 는 이 세션 제안이 편입된 것이다. §5.12→§5.17 이 5라운드로 늘어난 실제 원인은 라운드 수가
    아니라 **전제 미확인**이었다 — §5.11 이 「어느 플래그 상태에서 관측했는지 확인 필요」를 스스로
    적어 놓고 미확정으로 넘어갔고, 그 한 줄이 뒤 라운드 전부를 오염시켰다. 라운드 수는 증상이었다.
# ━━━
next_work:
  - id: "N1 — 대기 중 (최우선)"
    what: "`Debug_worker_v2` 의 **R1(ERD 빈 문서 덮어쓰기) 산출물 교차검증**"
    ticket: "BUG-20260902-01 — 자동 저장 기본값 `high` 확정으로 **P0 승격**"
    focus: >
      **「파싱 실패가 예외가 아니라 빈 결과로 조용히 통과하는 경로」가 남아 있는지.**
      예외 경로만 막고 빈 배열/빈 문자열 반환 경로가 남으면 자동 저장이 그걸 그대로 디스크에 쓴다.
    second_line: >
      **`FEAT-20260904-01` 의 자동 저장 안전장치는 R1 과 독립된 두 번째 방어선이다**(직전 내용이
      비어 있지 않은데 새 내용이 비었거나 파싱 실패 산출물이면 저장 건너뛰기).
      **둘이 서로를 대체한다고 보지 말 것 — 둘 다 서야 한다.** 특히 R1 이 「파싱 실패 시 저장 안 함」
      으로 해결됐다고 보고되면, **자동 저장 쪽 방어선이 그 판정에 기대고 있지는 않은지** 따로 본다.
      기대고 있으면 방어선이 둘이 아니라 하나다.
  - id: "N2"
    what: "S1·S2 교차검증 (R1 이후)"
    focus: "디바운스가 **헤딩 정체성(A7) · 2-패스 매칭 불변식**을 깨지 않는지. 이 프로젝트의 심각한 결함 3건이 전부 리팩터가 만들었고 전부 자동 테스트를 통과했다(`code_review.md` §0)"
  - id: "N3"
    what: "두 레인의 문서가 어긋나지 않게 지키기 (상시)"
    detail: "Impl 레인이 새 사양을 쓰기 시작하면 `implementation_plan.md`·`functions/*.md` 와 현행 코드 사이 드리프트가 곧바로 문제가 된다"
# ━━━
completed_this_session:
  note: "**PM 이 「미착수로 남기라」고 지시한 2건은 이미 완료돼 있다**(`ebf270d`, PM 메시지와 경합). 인수인계에서 미착수로 읽지 말 것"
  commits:
    - "`6e62178` — 8-D INP 포렌식 6라운드 기록(§5.14~§5.22) + 동결"
    - "`c90910b` — `DEBUG_STEP_PLAN` §2 동결 배너 · `code_review` §9 출시 스코프 재작성 · `DOCUMENTS` §4 드리프트 정정"
    - "`ebf270d` — R5·R6 사양서 코드 대조 · 4A 중복 경고 상향 · §9.0 R2 결정 반영"
  findings:
    - "**세로 분할은 「미구현」이 아니라 「미노출」**. 스토어(`store.ts:37,104,448`)·레이아웃(`WorkspacePage.tsx:224,271`)은 이미 있고 `:329` 분할 버튼이 `'horizontal'` 을 하드코딩할 뿐. 남은 일은 방향 선택 진입점 하나 → 로드맵 크기 하향"
    - "**R5(테마)는 사양서보다 범위가 넓다.** `index.css` 의 CSS 커스텀 속성 0개·HSL 0개인데 사양 §3 은 「HSL 기반 변수를 오버라이드」라고 기술. `entities/theme/` 부재, yaml 의존성 미도입. 변수 계층은 `erd.css:1-9` 에만 있고 ERD 전용으로 갈라져 Tailwind 팔레트와 값 중복. **토큰 선행 마이그레이션(tailwind 6 + index.css 33 + erd.css 7 + TSX 9)이 YAML 보다 앞** → 로드맵 크기 상향"
    - "**R6 설계가 두 문서에서 갈린다.** `functions/4_global_search.md` §3 = Shared Layer JS 유틸 / `implementation_plan.md` §4A.3 = Rust 엔진(BufReader). **착수 전 Rust vs JS 확정 필요** — 사양 두 벌 문제와 별개의 결정. (구현 부재 자체는 `PLANNED` 와 일치하므로 드리프트 아님)"
    - "**§4A 중복 경고가 안 보이는 상태였다.** 경고가 `implementation_plan.md:1595`(두 번째 벌 시작점)에만 있었고 첫 번째 벌은 `:1087` 에서 시작 — 위에서부터 읽으면 4A.1~4A.9 를 다 읽고도 두 번째 벌의 존재를 모른다. 그리고 「아키텍처 결정: Rust vs JS」는 **두 번째 벌에만** 있다. 경고를 `:1089` 로 상향했다. 두 벌 해소 자체는 **사용자 판단 대기** 유지"
    - "`code_review.md` §9.2' 가 D-4 근거로 인용한 `entities/theme/lib/injector.ts` 는 **아직 없는 경로**다(계획 경로). 표기 정정함 — D-4 결정 자체는 유효(R5 가 곧 Sprint 3)"
# ━━━
user_decisions_confirmed:
  - "자동 저장: **슬라이더 제거 + `off`/`low`/`high` 3단계 재설계 후 실제 배선. 기본값 `high`** (D6)"
  - "Apple Developer 계정 확인 불가 → **서명·Homebrew tap 보류.** 무서명 개인 배포로 진행"
  - "**0.9.x 유지 · 기존 버전 규약 그대로.** 1.0 승격은 이번 스코프에서 판단하지 않음"
  - "8-D 동결 — 사용자 본인 확인 완료. 목적은 「디버그가 기능 구현을 방해하지 않는 선에서 유지, PM 판단으로 조율」"
# ━━━
do_not:
  - "**Step 2·3 진입 금지** — `blocks` 가 `tabStore` 에서 빠지면 플래그가 되돌리기 수단이 아니게 된다"
  - "**8-D 관련 새 가설·새 실험 설계 금지** (D2·D3 포함). 헤드리스 조사는 §5.21 에서 종료 선언했다"
  - "**`DEBUG_PLAN.md` §5.x 를 건드리지 말 것** — 회전(rotation)도 지금 하지 않는다. 동결이지 종결이 아니라 재개 시 그대로 이어받아야 한다"
  - "`createDecorationPlugin`/`structuralDecorators` 신규 등록 금지 (§6 위험 7)"
  - "`protectedRegions`/Open Q #5 범위 밖 유지"
# ━━━
queued:
  - "BUG-20260828-04 · BUG-20260828-06 · Open Q #5 · `protectedRegions` N² · P2-4 · P2-6~P2-8 · A4"
  - "미발행: I-2(`settings/store.ts:79` 빈 catch — 로깅 + `.bak` 보존) · I-3(R-3 `crypto.randomUUID()` 교체)"
  - "사용자 판단 대기: `implementation_plan.md` §4A 사양 두 벌 · 퀵 캡처 미해결 5건 · 마인드뷰 메인 뷰 전환"
  - "`functions/*.md` 전수 점검 — R5·R6 둘만 대조했다. 나머지 7종 미점검"
# ━━━
entry_points:
  - "`DOCUMENTS.md` — 문서 지도. **새 세션은 여기부터**"
  - "`DEBUG_PLAN.md` §5.11~§5.22 — 8-D 측정·판정 기록 전문"
  - "`ticket/project/20260904_1800_release_lane_hold.yml` — 전 결정·조율 규칙 전문"
  - "`code_review.md` §9.0 — 현행 순서 / §9.1'·§9.2' — 보존된 아키텍처 게이트 순서"
