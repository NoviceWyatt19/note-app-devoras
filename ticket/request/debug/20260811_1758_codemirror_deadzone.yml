# 🚑 HOTFIX TICKET
type: "FIX_TICKET"
id: "BUG-20260811-05"
# ━━━
update: "2026-08-11 17:54"
project_ref: "PRJ-DEVORAS-MVP"
subject: "CodeMirror 커서 스킵(건너뜀) 및 클릭 데드존 버그 수정 (DOM Measurement 붕괴 복구)"
status: "Todo"
# ━━━
architecture_guidelines:
  - "No Block Elements in Inline Widgets: `Decoration.replace`로 생성되는 모든 위젯의 루트 요소는 반드시 `<span>`이어야 함. `HorizontalRuleDecorator`의 `<div>`를 `<span>`으로 교체하고 CSS `display: inline-block; width: 100%;`로 우회하여 렌더링 엔진의 높이 맵핑 붕괴를 막을 것."
  - "Event Passthrough: `ListDecorator`, `HeadingDecorator`, `HorizontalRuleDecorator` 등 텍스트를 치환하는 모든 `WidgetType` 클래스에 `ignoreEvent() { return false; }` 메서드를 오버라이딩하여, 위젯을 클릭해도 주변 텍스트로 커서가 정상 진입할 수 있도록 조치할 것."
# ━━━
tasks:
  - "[ ] `HorizontalRuleDecorator.ts`: `HRWidget`의 `document.createElement('div')`를 모두 `'span'`으로 변경하고, `ignoreEvent() { return false; }` 추가."
  - "[ ] `ListDecorator.ts`: `BulletWidget` 클래스에 `ignoreEvent() { return false; }` 메서드 추가."
  - "[ ] `HeadingDecorator.ts`: `HeadingBadgeWidget` 클래스에 `ignoreEvent() { return false; }` 메서드 추가."
  - "[ ] `BlockquoteDecorator.ts` (및 기타 위젯 생성부): `WidgetType`을 상속받는 모든 클래스에 `ignoreEvent() { return false; }`가 누락되었는지 확인하고 일괄 추가."
