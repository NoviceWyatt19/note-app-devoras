# 🐛 FIX TICKET
type: "FIX_TICKET"
id: "BUG-20260810-03"
# ━━━
update: "2026-08-10 18:04"
project_ref: "PRJ-DEVORAS-MVP"
subject: "초기 로딩 시 윈도우 무한 숨김(Stay Hidden) 버그 수정"
status: "Todo"
# ━━━
bug_report:
  symptom: "앱 실행 시 윈도우가 노출되지 않고 50초 이상 백그라운드 무한 대기 상태에 빠짐."
  cause: "`App.tsx`의 `<header>` 영역에 바인딩된 `onMouseDown` 이벤트와 내부의 `startWindowDrag()` 강제 호출이 윈도우 포커스 권한을 붙잡거나 블로킹을 유발하여, `revealWindow()`의 정상적인 실행을 방해함."
# ━━━
architecture_guidelines:
  - "Native Drag Region: Tauri 2는 HTML 태그의 `data-tauri-drag-region` 속성만으로 OS 네이티브 수준의 창 드래그를 완벽히 지원한다. 따라서 자바스크립트를 통한 수동 드래그 개입은 지양한다."
# ━━━
tasks:
  - "[ ] `App.tsx` 내부에 선언된 `startWindowDrag` 비동기 함수 로직 전체 삭제"
  - "[ ] `App.tsx`의 `<header>` 태그에 붙어있는 `onMouseDown` 이벤트 리스너 속성 제거 (단, 네이티브 드래그 처리를 위한 `data-tauri-drag-region` 속성과 기본 클래스명은 그대로 유지할 것)"
