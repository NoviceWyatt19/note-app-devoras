# DEBUG_PLAN.md — Step 8: 아키텍처 게이트 (`GATE-20260902-01`)

> **대상**: `project/` (Devoras `v0.8.50`, `f9f6716`, 트리 클린)
> **직전 배치**: BUG-20260831-01 캐럿 이탈 — [`DEBUG_PLAN_20260831_204000.md`](claude-history/debug/DEBUG_PLAN_20260831_204000.md) (종결)
> **입력**: `single_cm_transition.md` · `architecture_direction_review.md` · 사용자 결정 D-1~D-6 (2026-09-02)
> **성격**: **조사와 판정이 과제다.** 8-A 만 코드 착지이고, 8-B·8-C 는 **판별 실험**이며, 8-D 는 분기 결정이다.
> **인덱스**: [`DEBUG_STEP_PLAN.md`](DEBUG_STEP_PLAN.md) §2

---

## 0. 한 줄 요약

A6 초선형의 원인이 **동시 생존 `EditorView` 인스턴스 수**로 확정됐다(§3-A-2). 그것을 없애는 수단이 **E7 가상화**와 **단일 CM 전환** 둘로 경쟁한다. 이 배치는 **둘 중 하나를 고르기 위해 필요한 최소한만 잰다** — 그리고 재는 동안 회귀망(R-1)을 먼저 세운다.

**이 배치는 아키텍처를 바꾸지 않는다.** 바꿀지 말지를 정한다.

---

## 1. 실행 순서 & 게이트

```
8-A  R-1 검증 티어 도입                      [코드 착지]  ── 되돌리기 쉬움      ✅ 완료 (v0.8.51)
      │  REF-20260902-01
      │  게이트와 독립 — 어느 분기에서도 값어치가 남는다
      ↓
8-B  스파이크 A — 중첩 카드 시각 재현        [판별 실험]  ── 안건의 생사       ✅ 통과 (2026-09-02)
      │  SPIKE-20260902-A
      │  ✗ 실패 → 8-C 취소, 곧바로 8-D(E7 확정)
      ↓
8-C  스파이크 B — 오케스트레이터 증분화      [판별 실험]                      ✅ 통과 (2026-09-02)
      │  SPIKE-20260902-B
      │  ✗ 실패 → 8-D(E7 확정)
      ↓
8-D  판정 & 분기                             [결정]      ── 되돌릴 수 없는 단계   ◀── 사용자 확인 대기
         ├─ 통과 → 단일 CM 전환 (single_cm_transition.md §6 Step 1~3)
         └─ 실패 → E7 가상화 + 높이 캐시 복귀 (+ R-2·메커니즘 실험 복귀)
```

**병행 레인** (파일 비중첩 — 위 순서와 무관하게 언제든):
`I-1` ERD 유실 차단 · `I-2` settings 빈 catch · `I-3` R-3 `crypto.randomUUID()` · `P1-9` 위젯 타이포그래피 · `B2` 실기 검증

### 1.1 게이트 규칙

| 게이트 | 통과 조건 | 실패 시 |
|---|---|---|
| **8-A → 8-B** | `pnpm test:t15` 가 로컬 단독 실행되고 first_targets 4건이 통과 | 8-B 를 막지 않는다 — **8-A 는 차단 게이트가 아니다.** 다만 8-D 이전에는 반드시 선다 |
| **8-B → 8-C** | 중첩 카드가 현행과 육안 동등 | **8-C 취소.** 8-D 로 직행해 E7 확정 |
| **8-C → 8-D** | N=200 에서 입력·셀렉션당 재빌드 비용이 N 에 비례하지 않음 | 8-D 로 진행해 E7 확정 |

### 1.2 순서 근거

- **8-A 를 맨 앞에** — 이번 사이클의 심각한 결함 3건이 **전부 리팩터 자신이 만든 것이었고 전부 자동 테스트를 통과했다**(`code_review.md` §0). 회귀망이 가장 약한 시점에 최대 구조 변경을 하지 않는다.
- **8-B → 8-C** — A 가 실패하면 B 를 잰 비용이 통째로 버려진다. A 는 CSS·데코레이션 실험이라 B(오케스트레이터 재설계)보다 싸고 판정이 **육안으로 즉시** 난다. 비싼 쪽을 뒤에 둔다.
- **8-D 를 맨 뒤에** — 「되돌릴 수 없는 단계는 맨 뒤로」. 7-C 를 C-1~C-5 로 쪼개 싱글턴 제거를 C-4 에 둔 것이 실패 시 전량 롤백을 막은 선례를 재사용한다.

---

## 2. 8-A — R-1 검증 티어 도입

> **티켓**: [`ticket/refactor/20260902_0430_react_testing_infra.yml`](ticket/refactor/20260902_0430_react_testing_infra.yml) (`REF-20260902-01`)
> **성격**: 유일한 코드 착지 단계. 기존 코드를 **건드리지 않는다** — 추가만 한다.

### 2.1 문제

T1 하네스 13종은 **순수 로직 전용**이고 `package.json` 에 testing-library 의존성이 **0건**이다. 따라서 다음은 어떤 자동 게이트로도 도달할 수 없다 — 실제 키 입력 → 캐럿 렌더 결과 · 버튼 클릭 · 패널/탭 전환의 실제 DOM 결과.

**증거 — 이론이 아닌 이유**: 캐럿 버그(S1/S2)가 이 공백에서 나왔고(`focusOffset` 을 스토어 로직만으로 검사하면 정상이다), `FormatToolbar` 누락도 같은 공백이었다 — **계획에서 빠진 소비자와 테스트가 없는 소비자가 정확히 같은 집합이었다.**

### 2.2 러너 — 현행 로더에 jsdom 만 얹는다 (vitest 도입하지 않음)

`scripts/ts-hook.mjs` 가 **이미 필요한 변환을 전부 한다**:

| 필요한 것 | 위치 |
|---|---|
| `.tsx` 해석 | `ts-hook.mjs:30` (`EXTS`) |
| **JSX 변환** | `:78` — `jsx: ts.JsxEmit.ReactJSX` |
| `@/` 별칭 | `:44-48` |
| 확장자 없는 상대 임포트 | `:52-66` |
| **CSS/SVG/PNG/폰트 → 빈 모듈** | `:70-73` — `ReadView.tsx:19` 의 `highlight.js` CSS 때문에 실제로 필요하다 |
| `import.meta.env` 치환 | `:85` |

**남은 것은 jsdom 전역 부트스트랩 하나다.**

### 2.3 조치

1. devDependencies 추가: `@testing-library/react` · `@testing-library/user-event` · `jsdom`. **`vitest` 는 추가하지 않는다.**
2. `scripts/register-dom.mjs` 신설 — `register-ts.mjs` 를 불러온 뒤 `new JSDOM()` 으로 `window`/`document`/`navigator`/`HTMLElement` 등을 `globalThis` 에 얹고 `globalThis.IS_REACT_ACT_ENVIRONMENT = true` 를 세운다. **React 가 로드되기 전에** 전부 끝나야 한다 — 순서가 유일한 함정이다.
3. `package.json` 에 `test:t15` 추가 — `node --import ./scripts/register-dom.mjs <harness.tsx>`.
4. **기존 `test:t1` 은 건드리지 않는다.** 어서션도 기존과 동일하게 `node:test` + `node:assert` 를 쓴다 — testing-library 는 **쿼리와 렌더링만** 담당한다.

### 2.4 첫 대상 — **어느 분기에서도 살아남는 표면만**

단일 CM 이 채택되면 블록 경계 동작 테스트는 버려진다. 그래서 8-D 판정과 무관한 것부터 쓴다.

| # | 대상 | 왜 이것인가 |
|---|---|---|
| 1 | 저장 버튼 / dirty 표시 | 탭 스코프 스토어의 사용자 가시 결과 |
| 2 | viewMode 토글 (Write ↔ Read) | **FormatToolbar 마운트 회귀가 났던 지점** |
| 3 | 탭 전환 · 패널 전환의 DOM 결과 | 7-A 가 고친 표면 |
| 4 | FileExplorer 클릭 / 컨텍스트 메뉴 | 어떤 하네스도 덮지 않는다 |

**미루는 것**: 블록 간 포커스 이동 · 블록 병합 캐럿 (단일 CM 채택 시 소멸) · IME 조합 (원리적으로 사람 몫).

### 2.5 DoD

- [x] `pnpm test:t15` 가 CI 없이 로컬 단독 실행된다
- [x] §2.4 의 4건에 각각 최소 1개 테스트가 있고 통과한다 (14 케이스 전량 통과)
- [x] 그중 최소 1건이 **과거 실제 결함을 재현**한다 (FormatToolbar 마운트 또는 dirty 표시) — 하네스 철학 유지
- [x] `pnpm test:t1` 13종 전량 유지 (기존 티어 무회귀)
- [x] `VERIFY_BY_HUMAN.md` 에서 T1.5 로 옮겨간 항목을 표시하고, 남은 것이 왜 사람 몫인지 한 줄씩 적는다

**8-A 완료 (2026-09-02, v0.8.51).** 산출물: `scripts/register-dom.mjs` ·
`format_toolbar_mount_harness.tsx` · `save_dirty_indicator_harness.tsx` ·
`tab_panel_switch_harness.tsx` · `file_explorer_interaction_harness.tsx`.
`tsc --noEmit` 클린. 다음: **8-B(스파이크 A, `SPIKE-20260902-A`)** 착수 가능
— §1 게이트 규칙대로 8-A 는 차단 게이트가 아니므로 이미 8-B 를 막고 있지 않았지만,
이제 실제로 완료됐다.

---

## 3. 8-B — 스파이크 A: 중첩 카드 시각 재현

> **티켓**: [`ticket/impl/20260902_0440_spike_a_nested_card.yml`](ticket/impl/20260902_0440_spike_a_nested_card.yml) (`SPIKE-20260902-A`)
> **성격**: 판별 실험. **프로덕션 코드에 착지하지 않는다** — `__tests__` 하위에 격리한다.

### 3.1 질문

`Decoration.line()` 만으로 **L2 카드 안에 L3 카드가 들어앉고, L2 의 좌/우 테두리가 L3 구간을 관통해 연속되는** 그림이 나오는가.

**왜 어려운가**: 현행 카드는 **재귀 중첩 DOM** 이다(`BlockEditor.tsx:398-459` 의 `BlockNode` 가 자기 안에 `block-children` 을 두고 자식 `BlockNode` 를 렌더한다). `Decoration.line()` 이 만드는 것은 **평면 형제 `.cm-line`** 이라 중첩 관계가 DOM 에 없다.

**선례가 성립하지 않는 이유**: `single_cm_transition.md` 원본이 근거로 든 `CodeBlockDecorator` 는 **중첩 없는 단일 레벨** 배경/모서리다. 같은 패턴이 아니다.

### 3.2 이 실험이 정산하는 부채

`architecture_stages.md:56` (2026-08-27):

> | **Option A(문서 전체 단일 CM) 를 택하지 않은 이유** | H2/H3 를 중첩 라운드 카드로 렌더하는 현재 시각 디자인을 **평면 라인 목록으로는 표현할 수 없다** |

**이 단언은 측정된 적이 없다.** 이 프로젝트가 §3-C 에 스스로 적어둔 규칙("처방을 측정보다 먼저 쓰지 말 것")을 어긴 사례이며, 그 단언 하나가 안건을 근거 없이 닫아 뒀다. **단언이 맞는 것으로 나와도 유효한 결과다** — 안건이 근거를 갖고 종결된다.

### 3.3 재현 대상

**출처**: `BlockEditor.tsx:410-421` `getLevelStyles`

| 레벨 | 스타일 |
|---|---|
| L1 | `mb-8 bg-transparent` + 본문 아래 `border-b-2` 구분선 |
| L2 | `mt-6 p-5 rounded-2xl bg-[#141520] border border-darkBorder/40 shadow-md` |
| L3 | `mt-4 p-4 rounded-xl bg-[#1d1f30] border border-darkBorder/40` |
| L4+ | `mt-2 pl-2` |

**중첩**: L3 카드는 L2 카드 **내부**에 놓이고, L2 의 좌우 테두리·배경이 L3 구간에서도 끊기지 않는다.

> 같은 함수가 `ReadView.tsx:453-464` 에 **중복 정의**돼 있다. 이 스파이크와 무관하나 어느 분기에서든 Shared 로 추출할 대상이다.

### 3.4 방법

1. **정적 HTML/CSS 프로토타입 먼저.** CodeMirror 없이 평면 `.cm-line` 형제 목록을 손으로 만들어 목표 그림이 CSS 로 성립하는지 본다. **여기서 실패하면 CodeMirror 를 붙일 이유가 없다.**
2. 성립하면 실제 `EditorView` + `Decoration.line()` 으로 옮겨 같은 결과가 나오는지 확인한다.
3. **테스트 문서**: H1 1개 · H2 2개 · 각 H2 아래 H3 2개 · 각 블록 본문 3~5줄 · **코드펜스와 KaTeX 블록을 최소 1개씩 포함**(기존 데코레이터와의 시각 충돌 확인).
4. 다크 모드 기준으로 판정한다. 라이트 모드는 Sprint 3 이후 별건.

### 3.5 판정 기준

| 결과 | 조건 | 다음 |
|---|---|---|
| **통과** | 현행 `BlockNode` 렌더와 **육안 동등** (나란히 스크린샷 비교) | 8-C 진행 |
| **부분** | 중첩은 되나 모서리/그림자/여백이 열화 | **사용자에게 물어 판단한다.** 계획 세션이 임의로 타협하지 않는다 |
| **실패** | 관통 테두리나 중첩 배경이 성립하지 않음 | **8-C 취소 → 8-D(E7 확정).** 2026-08-27 판단이 옳았음을 측정으로 확정해 기록 |

### 3.6 DoD

- [x] 정적 CSS 프로토타입 결과 (성립/불성립) — **성립**. `spike_a_nested_card_static.html`
- [x] 성립 시 `EditorView` + `Decoration.line()` 재현 결과 — **성립**. `spike_a_nested_card_cm.ts`(실제 프로덕션 데코레이터 합성)
- [x] 현행 렌더와의 나란히 스크린샷 비교 이미지 — Chromium(T2) 실기, 앱의 `spike-a-test.md` 문서와 육안 대조
- [x] 기존 데코레이터(코드펜스·KaTeX)와의 시각 충돌 유무 — **충돌 없음** (HeadingDecorator·CodeBlockDecorator·LatexDecorator 그대로 합성해 확인)
- [x] 판정을 `single_cm_transition.md` §0.1 과 `architecture_stages.md:56` **양쪽에** 반영 — 완료 (§0.2 신설 + 표 갱신)

**8-B 판정: 통과 (2026-09-02).** §3.5 표대로 8-C(스파이크 B, `SPIKE-20260902-B`) 진행.
스파이크 진행 중 CSS 함정 2건(baseTheme vs theme() 우선순위, `.cm-line` 텍스트가 순수
텍스트 노드라 `::before` 레이어링 시 로컬 스태킹 컨텍스트가 필요) 발견·해결 —
`single_cm_transition.md` §0.2 에 기록, 8-D 채택 시 실제 구현에서 재사용.

---

## 4. 8-C — 스파이크 B: 오케스트레이터 증분화

> **티켓**: [`ticket/impl/20260902_0450_spike_b_orchestrator_incremental.yml`](ticket/impl/20260902_0450_spike_b_orchestrator_incremental.yml) (`SPIKE-20260902-B`)
> **선행**: 8-B 통과. **실패 시 이 단계는 취소된다.**

### 4.1 문제

```ts
// orchestrator.ts:27-46
function buildAll(state, decorators) {
  // Since Devoras splits blocks into separate CodeMirror instances,
  // doc.length is small enough to parse the whole block synchronously.
  const from = 0;
  const to = state.doc.length;
  const sets = decorators.map((d) => d.createDecorations(state, from, to));
  return RangeSet.join(sets);
}
// :331 — 재빌드 조건
if (tr.docChanged || tr.selection || tr.effects.some(e => e.is(setImeEffect))) {
  return buildAll(tr.state, decorators);
}
```

**소스 주석이 스스로 전제를 밝혀 뒀다** — "블록당 CM 이라 `doc.length` 가 작아서 괜찮다". **단일 CM 이 그 전제를 직접 파괴한다.** 전환 후에는 키 입력 1회와 화살표 1회마다 12개 데코레이터가 문서 전체를 훑는다 — **마운트의 O(N^1.76) 을 입력당 O(N)×12 로 바꾸는 거래**다.

`single_cm_transition.md` §4.1 의 예제(`scanBlockRanges(doc)`)가 정확히 이 함정에 빠져 있다.

> ### ⚠️ 착수 전 필독 — **8-C 의 기준선이 바뀌었다** (2026-09-02, 계획 세션 검증)
>
> `1f9d897 feat(0.9.0): Write Mode 표 렌더링 + 셀 단위 편집 데코레이터` 가 **이 배치 계획 밖에서** 착지했다.
> 결과가 8-C 의 설계 공간에 정면으로 닿는다 — 세 가지 전부:
>
> | 변화 | 이전 | 현재 | 8-C 에 주는 영향 |
> |---|---|---|---|
> | 데코레이터 수 | 12 | **13** (`TableDecorator`) | `buildAll` 이 도는 횟수가 늘었다. §4.4 측정치의 기준선이 달라진다 |
> | **다중 라인 `Decoration.replace`** | KaTeX 펜스 **1건**(`LatexDecorator.ts:161-172`) | **2건** — `TableDecorator.ts:412` 가 `table.from ~ table.to`(헤더+구분+본문 행 전체)를 replace 한다 | **§4.2 (b)(c) 의 전제가 강해졌다.** StateField 에 남겨야 할 구조 변경 데코레이터가 하나에서 둘이 됐다. **B-1(2계층 분리)의 "StateField 쪽 잔여 스캔" 위험이 그만큼 커진다** |
> | 전체 문서 스캔 | — | `scanTables()` 가 `doc.lines` 를 처음부터 끝까지 훑는다(`:99-104`) | 단일 CM 에서 입력·셀렉션당 O(N) 이 하나 더 붙는다 |
>
> **판정에 영향을 주는가**: 8-C 를 **더 어렵게** 만들지만 **불가능하게 만들지는 않는다.**
> B-2(StateField 증분화)와 B-3(조합)은 그대로 유효하고, 오히려 다중 라인 replace 가 둘이 된 만큼
> **B-1 단독보다 B-2/B-3 쪽이 유리해졌다.**
>
> **착수 시 반드시**:
> 1. §4.4 측정을 **13 데코레이터 기준**으로 잡는다. 12 기준 수치와 섞지 않는다
> 2. 테스트 문서에 **표를 최소 1개 포함**한다 (§4.5 DoD 에 추가) — 다중 라인 replace 가 둘 다 살아있는지 봐야 한다
> 3. 이 항목이 `function_roadmap.md` 「현재 위치」의 **"게이트가 열려 있는 동안 신규 기능 착수를 미룬다 — 같은 계층의 소비자를 늘리면 나중 수정이 더 비싸진다"** 가 정확히 예고한 사례임을 기록해 둔다. **비난이 아니라 계측 기준선 정정이다**

### 4.2 구조적 긴장 — 이것이 "설계 과제"인 이유

| | |
|---|---|
| (a) | **뷰포트 한정 빌드는 `ViewPlugin` 을 요구한다** — `view.visibleRanges` 는 ViewPlugin 에서만 접근 가능 |
| (b) | **다중 라인 `Decoration.replace` 는 `StateField` 를 요구한다** — CM6 는 뷰포트 계산 전에 높이에 영향을 주는 데코레이션을 알아야 한다. 현행이 StateField 인 이유가 이것이다 (`orchestrator.ts:317` 주석) |
| (c) | **그 다중 라인 replace 는 실재한다** — `LatexDecorator.ts:161-172`, KaTeX 블록 펜스가 여는 펜스~닫는 펜스를 replace 한다. 나머지 replace 는 전부 same-line 가드가 걸려 있다(BUG-20260810-05) |
| (d) | **셀렉션 재빌드를 그냥 끌 수 없다** — 데코레이터가 커서를 읽는다(`LatexDecorator.ts:164` `cursorInside`). 커서가 들어가면 위젯을 풀고 원문을 보여주는 것이 이 에디터의 마크업 노출/은닉 UX 의 본질이다. 끄면 기능이 죽는다 |

### 4.3 후보 설계 — **미리 정하지 않는다**

| # | 방식 | 위험 |
|---|---|---|
| **B-1** | **2계층 분리** — 구조 변경(다중 라인 replace)만 `StateField` 에 남기고 나머지 11종을 뷰포트 `ViewPlugin` 으로 | StateField 쪽이 여전히 문서 전체를 훑는다(KaTeX 펜스 스캔이 O(N)). 그 스캔이 충분히 싼지 별도 확인 |
| **B-2** | **StateField 증분화** — `value.map(tr.changes)` 로 기존 세트를 이동시키고 **변경 구간 + 이전/현재 캐럿 인접 구간만** 재빌드해 패치 | 정확성 — 부분 재빌드의 경계 오류. 데코레이터 인터페이스가 이미 `(state, from, to)` 를 받으므로 계약 변경은 작다 |
| **B-3** | **조합** — B-1 로 계층을 나눈 뒤 StateField 쪽에 B-2 적용 | 복잡도 최대. 가장 근본적 |

### 4.4 판정 기준

- **통과**: N=50 / 100 / 200 에서 **키 입력 1회 · 화살표 1회의 데코레이션 재빌드 비용이 평평**하다(N 에 비례하지 않는다)
- **실패**: 어느 설계로도 선형 이하로 못 내림 → **8-D(E7 확정)**

#### 4.4.1 측정 타당성 — **뷰포트 붕괴가 처치군을 유리하게 만든다** (2026-09-02, 계획 세션)

두 조건이 환경에 **비대칭으로** 반응한다. 이것을 모르고 숨은 페인에서 재면 **틀린 방향으로 통과한다.**

| 조건 | 환경 민감도 |
|---|---|
| `structuralField` | `buildLayer(state, decorators, 0, doc.length)` — **순수 JS, 레이아웃 미참조.** 숨은 페인에 거의 영향 없음 |
| `viewportPlugin` | **`view.visibleRanges` 는 레이아웃 파생값이다.** 숨은/높이 0 페인에서는 뷰포트가 붕괴해 일을 거의 안 한다 → **처치군이 부당하게 빨라 보인다** |

**따라서 필수 가드** — 환경과 무관하게 항상:

- 각 N 에서 `view.visibleRanges` 총 span(Σ `to-from`)과 `view.dom.getBoundingClientRect().height` 를 **함께 기록한다**
- 가시 span 이 문서 길이 대비 **현실적인 한 화면**이 아니면 그 실행은 **무효**로 표시한다

#### 4.4.2 환경 면역 지표 — 밀리초 대신 결정적 수치

**선례**: §3-A-1 §c 의 StyleModule 누수는 **오염된 환경에서 나왔는데도 유효한 결과**였다 — `<style>` 바이트 증가를 쟀지 타이밍을 재지 않았기 때문이다. **환경이 왜곡할 수 없는 지표를 고른다.**

§4.4 의 판정 기준은 **N 에 대한 비례 여부**이지 절대 ms 가 아니다. 그러면 결정적으로 답할 수 있다:

- **`createDecorations` 호출 횟수** (조건별·트랜잭션별)
- **총 스캔 문자 수** = Σ (`to` − `from`)

둘 다 정확한 정수이고 스로틀링이 바꾸지 못한다. 포어그라운드 실행은 **확증**(벽시계가 스캔량을 따라가는가)이 되고, 타이밍 단독보다 **강한 결과**가 된다.

> ⚠️ **측정 규율 필수** (§5.1 T2 주의) — 조건별 워밍업 1회 폐기 · 3회 반복 중앙값 · 조건 인터리브. 그리고 측정 전 `document.visibilityState === 'visible'` 과 rAF 60fps 를 **먼저 확인한다.** §3-A-1 이 이것을 안 해서 세션 하나를 통째로 날렸다.

### 4.5 DoD

- [x] 세 후보 중 채택안과 근거 — **B-1 + 구조 레이어 트리거 게이트** (순수 B-1 은 측정 결과 부족해 확장, 아래 참고)
- [x] **각 N 의 `visibleRanges` span + 에디터 높이** — 뷰포트 붕괴 여부 (§4.4.1). 첫 시도에서 실제로 붕괴 발견·수정함(아래 참고). 이후 전 실행 `valid: true`
- [x] **결정적 지표** — `createDecorations` 호출 수 · 총 스캔 문자 수 (§4.4.2) — 뷰포트 6780~6790자 고정(N 무관), 구조 레이어는 게이트 통과 시 0자·차단 시 N 에 선형(3×doc.length)
- [x] N=50/100/200 에서 키 입력 1회 재빌드 비용 (중앙값, 인터리브)
- [x] 같은 N 에서 화살표 1회(셀렉션) 재빌드 비용
- [x] 커서 진입 시 위젯 해제(`cursorInside`)가 유지되는지 확인 — 데코레이션 셋 직접 조회로 확인(DOM 가상화에 기대지 않음)
- [x] **다중 라인 replace 2건**이 모두 유지되는지 확인 — KaTeX 블록 펜스(`LatexDecorator`) · **표 위젯(`TableDecorator.ts:412`)** — 둘 다 확인
- [x] 측정 문서에 **표 최소 1개 포함** (13 데코레이터 기준선)
- [x] 판정을 `single_cm_transition.md` §7.1 ② 에 반영 (§7.1-B 신설)

**8-C 판정: 통과 (2026-09-02).** §4.4.1 의 뷰포트 붕괴 가드가 실제로 처음 실행에서 붕괴를 잡아냈다 —
`EditorView.theme()` 의 `height:400px` 가 `index.css` 의 `.cm-editor{height:100%}` 에 밀려 뷰포트가
전체 문서로 펴져 있었다(컨테이너에 인라인 스타일로 고정 높이를 줘서 해결, §7.1-B). 순수 B-1 은
구조 레이어(코드블록·표·KaTeX, 이제 3종)가 여전히 N 에 선형 비례해 판정 기준을 온전히 만족하지
못했다 — 그래서 문법 트리거 문자(`` ` `` `$` `|`) 유무와 기존 구조 데코레이션 근접도로 전체
재빌드를 건너뛰는 게이트를 구조 레이어에 얹었다. 결과: 문법 트리거에서 먼 일반 문단 편집(실제
편집 대다수)은 **N=50/100/200 전 구간에서 완전히 평평**(키 입력 6.0/5.5/4.5ms, 화살표
1.2/1.2/1.2ms), 펜스·표·KaTeX 에 직접 인접한 최악 조건만 완만한 잔존 증가(9ms/9.5ms at N=200,
16ms 프레임 예산 이내) — §7.1-B 상세 참고. 다음: **8-D**.

---

### 4.6 교차 검증 결과 (계획 세션, 2026-09-02) — **판정 유지, 결함 1건**

`project-ac` 의 8-C 보고를 코드로 대조했다. **성능 판정은 유지한다.** 다만 채택 설계에 **정확성 결함 1건**이 있어 8-D 전에 반드시 고쳐야 한다.

#### 확인된 것 ✅

| 항목 | 결과 |
|---|---|
| 프로덕션 코드 미착지 | `git status` 상 변경 0건 — 스파이크 산출물 전부 `__tests__` 하위 ✓ |
| 뷰포트 가드가 실제로 작동 | 첫 실행에서 CSS 캐스케이드로 인한 뷰포트 붕괴를 잡아냈다. **§4.4.1 이 값어치를 했다** ✓ |
| 결정적 지표 | `chars.viewport` 6780/6790 고정, `chars.structural` 이 정확히 3 데코레이터 × doc.length ✓ |
| `Open Q #5` 범위 유지 | `protectedRegions` 미변경 ✓ |
| 8-D 미진입 | 되돌릴 수 없는 단계 앞에서 정지 ✓ |

#### 🔴 결함 — 트리거 문자 집합이 불완전하다

**`STRUCTURAL_TRIGGER_RE = /[`$|]/`** (`spike_b_orchestrator_incremental.ts:137`) 가 **틸드 펜스를 놓친다.**

```
CodeBlockDecorator.ts:7 — const FENCE_OPEN_RE = /^(`{3,}|~{3,})/;
                                                          ^^^^^ ~~~ 도 유효한 펜스다
```

**결과**: 기존 구조 데코레이션에서 200자 넘게 떨어진 곳에 `~~~` 를 입력하면 게이트가 "트리거 없음 · 비근접"으로 판정해 **전체 재빌드를 건너뛴다 → 새 코드펜스가 데코레이트되지 않는다.** 무관한 다른 편집이 우연히 재빌드를 유발할 때까지 **조용히** 스타일이 빠진 채로 남는다.

이것은 티켓이 B-2 의 위험으로 **미리 지목했던 바로 그 종류**다 — *"정확성 — 부분 재빌드가 만드는 경계 오류."*

**나머지 구분자는 전수 확인했고 모두 덮인다**: Latex `$`(`DISPLAY_SINGLE_RE`·`INLINE_RE`·`DISPLAY_FENCE_RE` 전부 `$`) · Table `|`(`:107` 이 `includes('|')` 를 요구) · 인라인/펜스 코드 백틱.

**수정**: `/[`$|~]/`. **성능 프로파일에 영향 없다** — `~` 는 산문에서 드물어 far 모드 경로가 여전히 지배적이다. 따라서 **판정을 뒤집지 않는다.**

#### 🟡 부수 지적 — 삭제 범위의 끝점 미검사

`docChanged` 경로가 `isNearAnyDecoration(value, fromA, MARGIN)` 로 **시작점만** 본다(`:184`). 먼 곳에서 시작해 구조 데코레이션 **직전까지** 지우는 삭제는 `fromA` 가 멀고 삭제 텍스트에도 트리거가 없어 빠져나갈 수 있다. 200자 마진이 실무상 대부분 흡수하므로 **8-D 필수는 아니나**, 프로덕션 구현 시 `toA` 도 함께 보는 것이 옳다.

#### 8-D 착수 조건 (이 배치의 산출물로서)

- [x] 트리거 집합에 `~` 추가 후 far/near 모드 재측정 — **수치 유지 확인됨**: far 4.9/4.6/5.1ms·near 6.9/7.1/8.0ms(N=50/100/200), `structuralChars` 도 동일(18375/36240/73440) — 판정 안 바뀜
- [x] `toA` 끝점 검사 추가 — 반영함(`isNearAnyDecoration(value, fromA, MARGIN) || isNearAnyDecoration(value, toA, MARGIN)`)
- [x] `~~~` 트리거가 실제로 재빌드를 유발하는지 직접 검증 — 기존 데코레이션에서 먼 위치에 닫힌 `~~~...~~~` 펜스 삽입 → 데코레이션 7→9(위젯 2개 신설) 확인. 수정 전이었다면 스킵돼 0 이었을 것
- [ ] 위 두 건을 프로덕션 구현 사양에 반영 — 게이트는 **8-D 에서 프로덕션으로 넘어가는 코드**다 (8-D 착수 시 처리)

## 5. 8-D — 판정 & 분기

### 5.1 판정표

| 8-B | 8-C | 결론 | 다음 §2 |
|---|---|---|---|
| 통과 | 통과 | **단일 CM 전환 착수** | `single_cm_transition.md` §6 Step 1~3 |
| 통과 | 실패 | **E7 확정** — 카드는 되나 데코레이터 비용을 못 낮춤 | E7 + 높이 캐시 |
| 실패 | (취소) | **E7 확정** — 2026-08-27 판단이 측정으로 확인됨 | E7 + 높이 캐시 |

### 5.2 어느 쪽이든 반드시 할 것

- 판정을 **`single_cm_transition.md` §0.1 표**와 **`architecture_stages.md:56`** 양쪽에 반영한다. 한쪽만 고치면 다음 세션이 낡은 쪽을 근거로 안건을 다시 연다 — **이 배치 자체가 그 사고에서 나왔다.**
- `DEBUG_STEP_PLAN.md` §2 를 판정 결과로 교체하고, Step 8 을 §1 로 내린다.
- 본 문서를 `claude-history/debug/DEBUG_PLAN_[datetime].md` 로 회전한다.

### 5.3 E7 로 복귀할 경우 함께 돌아오는 것

§2.4 에서 보류한 두 항목이 **그 앞으로 복귀**한다:

| 항목 | 복귀 이유 |
|---|---|
| **R-2** `updateBlockContent` 경로 복사 | 입력당 비용이라 가상화가 고쳐주지 않는다. `updateBlockContent` 가 살아남으므로 매몰 비용이 아니게 된다 |
| **메커니즘 확인 실험** | 초선형이 레이아웃 축이면 `content-visibility: auto` 나 창 밖 DOM 분리로 상당 부분이 회수될 수 있다 — **가상화보다 훨씬 싸다.** E7 착수 전에 확인하면 설계가 단순해진다 |

**그리고 높이 캐시가 필수 동반이 된다** — 높이 캐시 없는 가상화는 BUG-20260828-01(수직 이동 줄 스킵) 계열을 재생산한다. 윈도 ±2 화면, 캐럿 근처 마운트/언마운트 금지.

---

## 6. 위험 및 주의

| # | 위험 | 완화 |
|---|---|---|
| 1 | **8-B/8-C 프로토타입이 프로덕션 코드에 새어 들어간다** | `__tests__` 하위에 격리한다. 이 배치는 **8-A 외에 프로덕션 코드를 바꾸지 않는다** |
| 2 | **8-C 가 `protectedRegions` N² 부채(Open Q #5)로 범위가 번진다** | 같은 지점인 것은 맞다. **그러나 8-C 의 질문은 비용이지 결합도가 아니다.** 중재 인프라 설계는 이 배치 밖 |
| 3 | **8-A 의 테스트가 곧 버려질 대상을 덮는다** | first_targets 를 §2.4 로 한정했다. 블록 경계 동작은 8-D 이후 |
| 4 | **8-C 측정이 백그라운드 탭에서 무효화된다** | §4.4 의 측정 규율. **사람이 브라우저 페인을 열어야 한다** — 도구로 해결 불가(§3-A-1) |
| 5 | **판정을 한쪽 문서에만 반영해 다음 세션이 재개봉한다** | §5.2. 이 배치가 정확히 그 사고의 산물이다 |
| 6 | **8-B 가 "부분 통과"로 나와 계획 세션이 임의 타협한다** | §3.5 — **사용자에게 묻는다** |
| 7 | **배치 중 신규 기능이 데코레이터 계층에 들어와 8-C 기준선이 흔들린다** | **이미 1회 발생** — `1f9d897` 표 렌더링(§4.2 앞 경고). 8-C 판정 전까지 `createDecorationPlugin` 목록에 **추가 등록을 하지 않는다.** 불가피하면 8-C 측정을 그 시점 기준으로 다시 잡는다 |

---

## 7. 병행 레인 (파일 비중첩 — 언제든)

| ID | 항목 | 티켓 | 비고 |
|---|---|---|---|
| **I-1** | ERD 파싱 실패 시 빈 문서 덮어쓰기 차단 | [`BUG-20260902-01`](ticket/debug/20260902_0410_erd_empty_doc_overwrite.yml) | 데이터 유실 인접. **자동 저장 배선 시 P0 로 오른다** |
| **I-2** | `settings/store.ts:79` 빈 `catch` — 로깅 + `.bak` 보존 | 미발행 | 비용 0 |
| **I-3** | **R-3** `crypto.randomUUID()` 교체 | 미발행 | ReadView 재정렬 id 가 **어느 분기에서도 살아남으므로** 유효 |
| **P1-9** | `.cm-line *` 위젯 타이포그래피 붕괴 | 독립 배치 | 사용자에게 즉시 보이는 표시 결함. 실행 사양서 존재 |
| **B2** | 보안 실기 검증 잔여 | — | 사람 손 |

---

## 8. 검증 티어

| 티어 | 수단 | 이 배치에서 |
|---|---|---|
| **T1** | `pnpm test:t1` — **13 하네스**, 순수 로직 | 전 단계에서 무회귀 유지 |
| **T1.5** | **8-A 가 신설** — `pnpm test:t15`, jsdom + testing-library | 8-A 산출물 |
| **T2** | `pnpm dev` + Chromium (`MockFileSystem` 폴백) | **8-B 는 육안 판정이라 배경 탭이어도 유효.** 8-C 는 타이밍이라 **포어그라운드 필수** |
| **T3** | `pnpm tauri:dev` — 실기 | 이 배치에는 없다 |

> ⚠️ **T2 결정적 주의**: 브라우저 페인이 백그라운드면 `visibilityState: hidden` 이 되어 **rAF 가 아예 발화하지 않는다**(성장 지수가 내용과 무관하게 1.65~1.85 로 수렴). 합성 키 이벤트·DOM 판독은 정상이므로 **기능·육안 검증은 유효하다.** 타이밍을 재려면 사용자가 페인을 표시한 뒤 `visibilityState === 'visible'` 과 rAF 60fps 를 **먼저 확인하고** 측정한다.

**측정 규율**: 조건별 워밍업 1회 폐기 · 3회 반복 중앙값 · 조건 인터리브로 순서 효과 제거.

---

## 9. 커밋 규칙

- `속성(버전): 설명` — feat/fix/refactor/chore/style/project (`.agents/AGENTS.md`)
- 코드 커밋은 패치 버전업 **4파일 동시**(`package.json` · `Cargo.toml` · `tauri.conf.json` · `Cargo.lock`)
- **문서 전용 커밋은 버전을 올리지 않는다**
- ⚠️ **공유 워킹 트리다.** `git commit` 은 인덱스 전체를 커밋한다. 커밋 직전 `git status --short` 확인, 또는 `git commit -F - -- <경로>`. 실제 사고: `f48b88c` — **캐럿 수정 코드가 로드맵 문서 커밋에 흡수됐다**(정정: `81971a2`)

**이 배치의 버전업 예상**: 8-A 만 코드 착지이므로 패치 1회(`v0.8.51`). 8-B·8-C 는 `__tests__` 하위 격리라 **판단에 따라 버전을 올리지 않아도 된다** — 착수 시 정할 것.

---

## 10. DoD (배치 종료 조건)

- [x] **8-A** — `pnpm test:t15` 신설, first_targets 4건 통과, `test:t1` 13종 무회귀 (2026-09-02, v0.8.51)
- [x] **8-B** — 판정(통과/부분/실패) + 스크린샷 비교 + 양쪽 문서 반영 — **통과** (2026-09-02)
- [x] **8-C** — (8-B 통과 시) 채택 설계 + N=50/100/200 측정치 + 판정 — **통과** (2026-09-02, B-1+구조 트리거 게이트)
- [ ] **8-D** — 분기 결정이 `DEBUG_STEP_PLAN.md` §2 에 반영됨 — **사용자 확인 대기** (되돌릴 수 없는 단계, §1.2)
- [ ] 본 문서가 `claude-history/debug/` 로 회전되고 새 `DEBUG_PLAN.md` 가 다음 배치로 작성됨
- [ ] `GATE-20260902-01` 티켓 상태가 판정으로 갱신됨

---

## 11. 다음 갱신 시점

**중간 갱신 2회** — 8-B 판정 직후(안건 생사), 8-C 판정 직후(가격표 확정).
**교체 시점** — 8-D 판정 시. 회전 규칙은 [`DOCUMENTS.md`](DOCUMENTS.md) §3.
