# 🛠 IMPLEMENT TICKET
type: "IMPLEMENT_TICKET"
id: "SESS-20260803-01"
# ━━━
update: "2026-08-03 17:18"
project_ref: "PRJ-DEVORAS-MVP"
subject: "에디터 이미지 첨부(Drag&Paste) 및 동적 워크스페이스 저장 정책 연동"
status: "Done"
# ━━━
spec:
  inputs:
    - "CodeMirror paste / drop 이벤트 (이미지 바이너리)"
    - "WorkspaceConfig.imageSavePolicy ('workspace-root-hidden' | 'current-file-relative' | 'custom-folder')"
  outputs:
    - "로컬 디스크 내 이미지 파일 저장 (타임스탬프 난수 파일명)"
    - "CodeMirror 커서 위치에 상대 경로 마크다운 태그(![img](path)) 자동 주입"
  internal_logic:
    - "1. entities/workspace/model/types.ts: ImageSavePolicy 유니온 타입 + WorkspaceConfig 인터페이스 신규 정의"
    - "2. shared/lib/fs/imageAsset.ts: saveImageAssetWithPolicy() — 정책별 저장 경로 동적 계산 + fileSystemRepository.saveImageAsset 호출"
    - "3. shared/api/fs.ts: saveImageAsset(basePath, data, fileName, subDir) 시그니처 확장 (Mock/Tauri 양쪽)"
    - "4. entities/workspace/model/store.ts: config: WorkspaceConfig 상태 + setConfig() 액션 추가"
    - "5. BlockEditor.tsx: paste/drop 핸들러 → saveImageAssetWithPolicy 유틸 호출로 교체, 현재 파일 경로 전달"
    - "6. ReadView.tsx: resolveAssetPaths() 정규식 확장 → 3가지 정책 경로 모두 asset:// URL 변환"
# ━━━
harness:
  path: "project/src/shared/lib/fs/__tests__/image_asset_harness.ts"
  run_command: "pnpm build"
  result: "✅ exit code 0 — TS 컴파일 에러 없음, Vite 번들 성공 (2026-08-03 17:15)"
# ━━━
tasks:
  - "[x] WorkspaceConfig 이미지 정책 타입 정의 (entities/workspace/model/types.ts)"
  - "[x] 동적 상대 경로 계산 및 저장 유틸리티 작성 (shared/lib/fs/imageAsset.ts)"
  - "[x] saveImageAsset API subDir 파라미터 확장 (shared/api/fs.ts)"
  - "[x] WorkspaceStore에 config 상태 및 setConfig 액션 추가 (entities/workspace/model/store.ts)"
  - "[x] BlockEditor paste/drop 핸들러를 saveImageAssetWithPolicy 유틸로 교체 (widgets/BlockEditor/ui/BlockEditor.tsx)"
  - "[x] ReadView resolveAssetPaths 정규식 기반 확장 (widgets/BlockEditor/ui/ReadView.tsx)"
  - "[x] pnpm build 검증 완료 및 git commit (feat(0.4.6))"